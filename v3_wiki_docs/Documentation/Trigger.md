 Trigger - Victoria 3 Wiki              

[Forum list](https://forum.paradoxplaza.com/forum/forums/) [Trending](https://forum.paradoxplaza.com/forum/threads/trending) [Latest](https://forum.paradoxplaza.com/forum/threads/latest) [New posts](https://forum.paradoxplaza.com/forum/threads/newest)

Paradox

[Store](https://www.paradoxplaza.com/) [Mods](https://mods.paradoxplaza.com/) [Forum](https://forum.paradoxplaza.com/forum/forums/) [Launcher](https://play.paradoxplaza.com/) [PDXCON 2019](https://pdxcon.paradoxplaza.com/?utm_source=pdxplaza-owned&utm_medium=web-owned&utm_content=topmenu-banner&utm_campaign=pc18_pdxcon_20190412_cawe_ann)

[

Paradox Wikis

](/Victoria_3_Wiki)

Victoria 3 Wiki

Active Wikis

[Age of Wonders 4](https://aow4.paradoxwikis.com/) [Empire of Sin](https://eos.paradoxwikis.com/Empire_of_Sin_Wiki) [Cities: Skylines 2](https://cs2.paradoxwikis.com/Cities_Skylines_II_Wiki) [Crusader Kings 3](https://ck3.paradoxwikis.com/Crusader_Kings_III_Wiki) [Europa Universalis 5](https://eu5.paradoxwikis.com/Europa_Universalis_5_Wiki) [Hearts of Iron 4](https://hoi4.paradoxwikis.com/Hearts_of_Iron_4_Wiki) [Hunter: The Reckoning](https://htr.paradoxwikis.com/) [Imperator: Rome](https://imperator.paradoxwikis.com/Imperator_Wiki) [Millennia](https://millennia.paradoxwikis.com/Millennia_Wiki) [Prison Architect](https://prisonarchitect.paradoxwikis.com/) [Stellaris](https://stellaris.paradoxwikis.com/Stellaris_Wiki) [Surviving Mars](https://survivingmars.paradoxwikis.com/Surviving_Mars_Wiki) [Surviving the Aftermath](https://sta.paradoxwikis.com/Surviving_The_Aftermath_Wiki) [Werewolf: the Apocalypse](https://wta.paradoxwikis.com/Werewolf_The_Apocalypse_Wiki) [Vampire: The Masquerade](https://vtm.paradoxwikis.com/VTM_Wiki) [Victoria 3](https://vic3.paradoxwikis.com/Victoria_3_Wiki)

Legacy Wikis

[AoW: Planetfall](https://aowplanetfall.paradoxwikis.com/AoW_Planetfall_Wiki) [Cities: Skylines](https://skylines.paradoxwikis.com/Cities:_Skylines_Wiki) [Crusader Kings 2](https://ck2.paradoxwikis.com/Crusader_Kings_II_Wiki) [Arsenal of Democracy](https://aod.paradoxwikis.com/Main_Page) [Europa Universalis 2](https://eu2.paradoxwikis.com/Main_Page) [Europa Universalis 3](https://eu3.paradoxwikis.com/Europa_Universalis_3_Wiki) [Europa Universalis 4](https://eu4.paradoxwikis.com/Europa_Universalis_4_Wiki) [Europa Universalis: Rome](https://eurome.paradoxwikis.com/Europa_Universalis:_Rome_Wiki) [Hearts of Iron 2](https://hoi2.paradoxwikis.com/Main_Page) [Hearts of Iron 3](https://hoi3.paradoxwikis.com/Hearts_of_Iron_3_Wiki) [Tyranny](https://tyranny.paradoxwikis.com/Tyranny_Wiki) [Victoria 1](https://vic1.paradoxwikis.com/Main_Page) [Victoria 2](https://vic2.paradoxwikis.com/Victoria_2_Wiki)

### Search

   

### Personal tools

Log in

### Personal tools

-   [English](#)
-   Not logged in
-   [Talk](/Special:MyTalk "Discussion about edits from this IP address [n]")
-   [Contributions](/Special:MyContributions "A list of edits made from this IP address [y]")
-   [Log in](/index.php?title=Special:UserLogin&returnto=Trigger "You are encouraged to log in; however, it is not mandatory [o]")

## Navigation menu

### Namespaces

-   [Page](/Trigger "View the content page [c]")
-   [Discussion](/Talk:Trigger "Discussion about the content page [t]")

 English

### Views

-   [Read](/Trigger)
-   [Edit](/index.php?title=Trigger&veaction=edit "Edit this page [v]")
-   [Edit source](/index.php?title=Trigger&action=edit "Edit this page [e]")
-   [View history](/index.php?title=Trigger&action=history "Past revisions of this page [h]")

 More

[](/Victoria_3_Wiki "Visit the main page")

### Navigation

-   [Victoria 3 Wiki](/Victoria_3_Wiki "Visit the main page [z]")
-   [Recent changes](/Special:RecentChanges "A list of recent changes in the wiki [r]")
-   [Random page](/Special:Random "Load a random page [x]")

### Paradox Wikis

-   [\- Main portal](https://paradoxwikis.com/)
-   [\- Wikis discord](https://discord.gg/rUWSHuUwnY)
-   [\- Contact us](/Contact_us)
-   [\- Style guidelines](/Victoria_3_Wiki:Style)

### V3 links

-   [\- Forums](https://forum.paradoxplaza.com/forum/forums/victoria-3.1095/)
-   [\- Game discord](https://discord.com/invite/victoriagamePDX)
-   [\- Mods](https://mods.paradoxplaza.com/)
-   [\- Store](https://www.paradoxinteractive.com/games/victoria-3/about?utm_source=pdxwiki-owned&utm_medium=social-owned&utm_content=link&utm_campaign=vic_vic_20220504_past_noty)
-   [\- Twitch](https://www.twitch.tv/paradoxinteractive/)

### Tools

-   [What links here](/Special:WhatLinksHere/Trigger "A list of all wiki pages that link here [j]")
-   [Related changes](/Special:RecentChangesLinked/Trigger "Recent changes in pages linked from this page [k]")
-   [Special pages](/Special:SpecialPages "A list of all special pages [q]")
-   [Printable version](javascript:print\(\); "Printable version of this page [p]")
-   [Permanent link](/index.php?title=Trigger&oldid=34516 "Permanent link to this revision of this page")
-   [Page information](/index.php?title=Trigger&action=info "More information about this page")

### braze

# Trigger

From Victoria 3 Wiki

[Jump to navigation](#mw-head) [Jump to search](#searchInput)

 

Please help with verifying or updating older sections of this article.  
At least some were last verified for [version](/Victoria_3_Wiki:Versioning "Victoria 3 Wiki:Versioning") 1.10.

Triggers act as conditions by reading the current game state – such as the ideology of a character, the ownership of a state, and much else.

Triggers come in two basic types, inline and block. Inline triggers take a simple target, such as a scope, script value, or defined type. Block triggers are more complex and often take multiple targets, such as a scope and script value.

All triggers require a certain [scope](/Scope "Scope"). Some triggers can be used in any scope (noted as "none" in the following tables), others only function when in the correct scope. Some triggers change the current scope.

The tables below are generated from the script documentation (*script\_docs* console command).

## Contents

-   [1 Comparison triggers](#Comparison_triggers)
    -   [1.1 Event targets as triggers](#Event_targets_as_triggers)
-   [2 Iterator triggers](#Iterator_triggers)
-   [3 Flow triggers](#Flow_triggers)
-   [4 Log triggers](#Log_triggers)
-   [5 Variable triggers](#Variable_triggers)
-   [6 Triggers by scope](#Triggers_by_scope)
    -   [6.1 None/any scope](#None/any_scope)
    -   [6.2 Building scope](#Building_scope)
    -   [6.3 Character scope](#Character_scope)
    -   [6.4 Civil war scope](#Civil_war_scope)
    -   [6.5 Company scope](#Company_scope)
    -   [6.6 Country scope](#Country_scope)
    -   [6.7 Culture scope](#Culture_scope)
    -   [6.8 Diplomatic play scope](#Diplomatic_play_scope)
    -   [6.9 Interest group scope](#Interest_group_scope)
    -   [6.10 Institution scope](#Institution_scope)
    -   [6.11 Journal entry scope](#Journal_entry_scope)
    -   [6.12 Market scope](#Market_scope)
    -   [6.13 Military formation scope](#Military_formation_scope)
    -   [6.14 New combat unit scope](#New_combat_unit_scope)
    -   [6.15 Party scope](#Party_scope)
    -   [6.16 Political lobby scope](#Political_lobby_scope)
    -   [6.17 Political movement scope](#Political_movement_scope)
    -   [6.18 Pop scope](#Pop_scope)
    -   [6.19 Power bloc scope](#Power_bloc_scope)
    -   [6.20 State scope](#State_scope)
    -   [6.21 State goods scope](#State_goods_scope)
    -   [6.22 State region scope](#State_region_scope)
    -   [6.23 Treaty scope](#Treaty_scope)
    -   [6.24 War scope](#War_scope)
-   [7 All triggers](#All_triggers)
-   [8 References](#References)

## Comparison triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=1 "Edit section: Comparison triggers") | [edit source](/index.php?title=Trigger&action=edit&section=1 "Edit section: Comparison triggers")\]

Comparisons are a common type of trigger, comparing two numerical values or game objects. Many triggers in the tables below are a comparison trigger (indicated with target `value`), but comparisons can also be made directly between values or objects using [script values](/Script_value "Script value"), [variables](/Variable "Variable"), and [event targets](/Event_target "Event target").

Comparisons use one of the comparison operators:

Operator

Meaning

Inverted meaning

Use

`<`

(Strict) less than

Greater than or equals

Left side is strictly less than right side

`<=`

Less than or equals

(Strict) greater than

Left side is less than or equal to right side

`=`

(Strict) equals

Not equals

Left side is exactly equal to the right side (usable with non-numerical values)

`!=`

Not equals

(Strict) equals

Left side is not equal to the right side (usable with non-numerical values)

`>`

(Strict) greater than

Less than or equals

Left side if strictly greater than the right side

`>=`

Greater than or equals

(Strict) less than

Left side is greater than or equal to the right side.

If a comparison is used inside a negative block (e.g. `NOT = { }`), it uses its inverted meaning.

Note that `=` is also used as the operator for non-comparison triggers, effects, and blocks. In these cases, the `=` represents an assignment rather than a comparison.

### Event targets as triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=2 "Edit section: Event targets as triggers") | [edit source](/index.php?title=Trigger&action=edit&section=2 "Edit section: Event targets as triggers")\]

*Main article: [Event target](/Event_target "Event target")*

Event targets can be used as either the left or right side of a comparison trigger. For example, `army_size > c:FRA.army_size` returns true if the army size of the current scope is larger than the army size of France. For event targets that return a scope, only equality can be checked, while event targets that return a value can be used with inequality comparisons, too.

List of value event targets

Event target

Description

From scope

ai\_army\_comparison

Scope to comparative army strength between two countries (example: value = ai\_army\_comparison:root)

country

ai\_gdp\_comparison

Scope to comparative GDP between two countries (example: value = ai\_army\_comparison:root)

country

ai\_ideological\_opinion

Scope to AI ideological opinion of scope country on target country (example: value = ai\_ideological\_opinion:root)

country

ai\_navy\_comparison

Scope to comparative navy strength between two countries (example: value = ai\_navy\_comparison:root)

country

ai\_state\_value

Scope to target country AI desire to own scope state (example: value = ai\_state\_value:root)

state

ai\_subject\_value

Scope to AI desire of target country to have scope country as a subject (example: value = ai\_subject\_value:root)

country

ai\_transit\_rights\_value

Scope to AI desire of to have transit rights through target country (example: value = ai\_transit\_rights\_value:root)

country

ai\_treaty\_export\_value

Scope to target country AI desire to export scoped market good (example: value = ai\_market\_export\_value:root)

market\_goods

ai\_treaty\_fairness

Scope to AI evaluation of how fair (to both sides) it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_fairness\_score:root)

country

ai\_treaty\_import\_value

Scope to target country AI desire to import scoped market good through treaties (example: value = ai\_market\_import\_value:root)

market\_goods

ai\_treaty\_port\_value

Scope to target country AI desire to have a treaty port in scope state (example: value = ai\_treaty\_port\_value:root)

state

ai\_treaty\_trade\_value

Scope to target country AI desire to import and export scoped market good (example: value = ai\_market\_trade\_value:root)

market\_goods

ai\_treaty\_value

Scope to AI evaluation of how favorable to itself it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_value:root)

country

army\_size

Number of battalions of scope country, excluding currently raised conscripts.

country

army\_size\_including\_conscripts

Number of battalions of scope country, including currently raised conscripts.

country

array\_define

Reference the value of a numeric value in an array define: array\_define:Namespace|Name|Index. Index is 0-based.

none

average\_defense

Returns the average defense of units on Front on the same side as Country

front

average\_expected\_sol

Get the average expected Standard of Living of the scope

country, state

average\_offense

Returns the average offense of units on Front on the same side as Country

front

average\_sol

Get the average Standard of Living of the scope

country, state

base\_price

Get the base price of the scoped good

goods

binding\_period

Get the original binding period in days when the treaty was signed for the scoped treaty

treaty, treaty\_options

building\_levels

Get the total number of non-subsistence building levels of the scoped country

country

cached\_ai\_coastal\_population

Scope to total coastal population recorded in AI spending variables (example: value = cached\_ai\_coastal\_population)

country

cached\_ai\_incorporated\_coastal\_population

Scope to total incorporated coastal population recorded in AI spending variables (example: value = cached\_ai\_incorporated\_coastal\_population)

country

cached\_ai\_incorporated\_population

Scope to total incorporated population recorded in AI spending variables (example: value = cached\_ai\_incorporated\_population)

country

cached\_ai\_overseas\_subject\_population

Scope to total population in direct non-adjacent subjects recorded in AI spending variables (example: value = cached\_ai\_overseas\_subject\_population)

country

cached\_ai\_subject\_population

Scope to total population in direct subjects recorded in AI spending variables (example: value = cached\_ai\_subject\_population)

country

cached\_ai\_total\_population

Scope to total population recorded in AI spending variables (example: value = cached\_ai\_total\_population)

country

cached\_ai\_unincorporated\_coastal\_population

Scope to total unincorporated coastal population recorded in AI spending variables (example: value = cached\_ai\_unincorporated\_coastal\_population)

country

cached\_ai\_unincorporated\_population

Scope to total unincorporated population recorded in AI spending variables (example: value = cached\_ai\_unincorporated\_population)

country

colonial\_growth\_per\_colony

Scope to the country's colonial growth per colony value (example: colonial\_growth\_per\_colony > 0)

country

combat\_width

Scope to combat width multiplier of scope province

province

command\_limit\_num\_units

Get the baseline number of units from their military formation a commander would normally get, depending on their command limit

character

compare\_complex\_value

A comparison trigger that needs a parsable string parameter that will return its value in the context it is used eg: scope:root.number\_of(armies)

varies

compare\_value

A comparison trigger that will return its value in the context it is used eg: root.gold

varies

credit

Scope to the current amount of max credit the country can take

country

decree\_cost

Scope to a state's cost for a certain decree

authority > decree\_cost:decree\_road\_maintaintenance

state

defense

Returns the unit's defense rating (including impact from their commander)

new\_combat\_unit

define

Reference the value of a numeric or color define: define:Namespace|Name

none

demoralized

Returns the unit's number of demoralized manpower

new\_combat\_unit

expenses

Get the current weekly expenses of the scoped country

country

fixed\_expenses

Get the current weekly fixed expenses of the scoped country

country

front\_length

Returns the number of provinces in Front

front

gdp

Get the scoped country's yearly predicted GDP

country, state

global\_productivity

Scope to global productivity values (example: global\_productivity:high\_threshold)

none

goal\_value

Scope to the journal entry's goal value

journal\_entry

government\_size

Get the scoped country's current number of Parties & independent IGs in government

country

income

Get the current weekly income of the scoped country

country

income\_transfer\_expenses

Scope to the amount of money scope country is transferring to other countries via treaties & pacts (example:income\_transfer\_expenses >= 100)

country

income\_transfer\_relative\_expenses

Scope to the fraction of their income country is transferring to other countries via treaties & pacts (example:income\_transfer\_relative\_expenses >= 0.2)

country

infamy

Scope to infamy of scope country (example: infamy >= 2)

country

infamy\_threshold

Scope to infamy level threshold for comparison purposes (infamy\_threshold:infamous)

none

input\_quantity

Scope to the quantity input in a treaty article

treaty\_article, treaty\_article\_options

investment

Scope to current investment level in an institution (example: investment < investment\_max)

institution

investment\_max

Scope to the country's current maximum investment level in an institution (example: investment < investment\_max)

institution

investment\_pool\_income

Get the current weekly investment pool income of the scoped country

country

legitimacy

Scope to the current amount of legitimacy in country

country

level

Scope to the level of a building

building

level\_after\_queued\_constructions

Scope to the level of a building after all queued constructions finish

building

lobby\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies targeting a specific country (lobby\_foreign\_anti\_clout:root >= 0.2)

country

lobby\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies targeting a specific country (lobby\_foreign\_pro\_clout:root >= 0.2)

country

lobby\_in\_government\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_anti\_clout:root >= 0.2)

country

lobby\_in\_government\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_pro\_clout:root >= 0.2)

country

lobby\_join\_weight

Scope to the join weight of an interest group to a particular lobby (lobby\_join\_weight:root >= 5)

interest\_group

lobby\_war\_opposition

Scope to the total clout of all lobbies that oppose the scoped war (lobby\_war\_opposition:root >= 0.2)

country

lobby\_war\_support

Scope to the total clout of all lobbies that support the scoped war (lobby\_war\_support:root >= 0.2)

country

manpower

Returns the unit's manpower

new\_combat\_unit

market\_export\_cap

Get the maximum amount of goods the market can export based on it's production of the scoped MarketGoods

market\_goods

market\_import\_cap

Get the maximum amount of goods the market can import based on it's consumption of the scoped MarketGoods

market\_goods

migration\_pull

Get the migration pull for a state or the mass migration pull for a country.

country, state

military\_expenses

Get the current military expenses (slaves, wages and goods) of the scoped country

country

military\_expenses\_share

Get the current share of military expenses (slaves, wages and goods) relative to the total expenses of the scoped country

country

mobilization

Returns the unit's mobilization value between 0 and 1

new\_combat\_unit

modifier

Scope to the value of the modifier type of specified key belonging to the current object, example: modifier:country\_influence\_add

battle\_side, building, character, country, interest\_group, market, power\_bloc, state

momentum

Get the scoped party's raw momentum

party

morale

Returns the unit's morale

new\_combat\_unit

mutual\_trade\_value\_with\_country

Scope to the total base value of goods traded in trade routes between the two markets (example: mutual\_trade\_value\_with\_country:root >= 1000)

country

named\_script\_value

A script value that will calculate and returns its value in the context it is used

none

navy\_size

Number of flotillas of scope country.

country

num\_active\_declared\_interests

Scope to the number of active declared interests a country has

num\_active\_declared\_interests > 5

country

num\_active\_interests

Scope to the number of active interests a country has num\_active\_interests > 5

country

num\_active\_natural\_interests

Scope to the number of active natural interests a country has num\_active\_natural\_interests > 5

country

num\_active\_plays

Get the current number of active Diplomatic Plays the scoped country started

country

num\_admirals

Scope to the country's number of admirals

country

num\_alliances

Get the total number of alliances of the scoped country (example: num\_alliances:root >= 2)

country

num\_alliances\_and\_defensive\_pacts\_with\_allies

Scope to the number of alliances target country has with allies of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_allies:root >= 2)

country

num\_alliances\_and\_defensive\_pacts\_with\_rivals

Scope to the number of alliances & defensive pacts target country has with rivals of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_rivals:root >= 2)

country

num\_battalions

Returns the number of active battalions under a commander

character

num\_characters

Scope to the country's number of characters

country

num\_colony\_projects

Get the scoped country's current number of incomplete / in progress colony projects

country

num\_commanderless\_units

Number of units in a MilitaryFormation which currently don't have a commander due to commanders Command Limit.

-   Obs: defeated commanders do not command units, i.e., the more defeated commanders, the more commanderless units.

military\_formation

num\_commanders

Scope to the country's number of commanders

country

num\_convoys\_available

Get the scoped country's current number of available convoys

country

num\_convoys\_required

Get the scoped country's current number of required convoys

country

num\_declared\_interests

Scope to the number of declared interests a country has (including inactive)

num\_declared\_interests > 5

country

num\_defending\_battalions

Returns the total number of units in battle on Front on the same side as Country, example: num\_defending\_battalions(root)

front

num\_defensive\_pacts

Scope to the number of defensive pacts target country has in total (example: num\_defensive\_pacts >= 2)

country

num\_garrison\_units

Returns the number of units garrisoned in a HQ

hq

num\_generals

Scope to the country's number of generals

country

num\_income\_transfer\_pacts

Scope to the number of income-transferring (to others) pacts target country has in total (example: num\_income\_transfer\_pacts >= 2)

country

num\_income\_transfer\_treaty\_articles

Scope to the number of income-transferring (to others) treaty articles target country has in total (example: num\_income\_transfer\_treaty\_articles >= 2)

country

num\_income\_transfers

Scope to the number of income-transferring (to others) pacts and treaty articles target country has in total (example: num\_income\_transfers >= 2)

country

num\_incorporated\_states

Get the scoped country's current number of incorporated states

country

num\_interests

Scope to the number of interests a country has (including inactive)

num\_interests > 5

country

num\_mandates

Scope to the number of available mandates of the scoped power bloc (power\_bloc.num\_mandates > 0)

power\_bloc

num\_mobilized\_battalions

Returns the number of fully mobilized units under a General

character

num\_natural\_interests

Scope to the number of natural interests a country has (including inactive)

-   num\_natural\_interests > 5

country

num\_obligations\_earned

Scope from a country to the number of obligations others owe them (=which they have earned)

country

num\_pending\_events

Scope to the number of pending events of given category in scope country

-   num\_pending\_events:enactment > 0

country

num\_politicians

Scope to the country's number of politicians

country

num\_positive\_relations

Get the current number of countries with positive Relation with the scoped country

country

num\_provinces

Get the scoped entity's current number of provinces

front, state, state\_region

num\_queued\_constructions

Scope to the number of queued constructions target country has in total (example: num\_queued\_constructions >= 2)

country

num\_queued\_government\_constructions

Scope to the number of queued government constructions target country has in total (example: num\_queued\_government\_constructions >= 2)

country

num\_queued\_private\_constructions

Scope to the number of queued private constructions target country has in total (example: num\_queued\_private\_constructions >= 2)

country

num\_rivals

Scope to the number of rivals target country has in total (example: num\_rivals >= 2)

country

num\_ruling\_igs

Get the scoped country's current number of IGs in government

country

num\_shared\_rivals

Scope to the number of shared rivals scope country has with target country (example: num\_shared\_rivals:root >= 2)

country

num\_states

Get the scoped country's current number of states

country

num\_total\_battalions

Returns the total number of units on Front on the same side as Country, example: num\_total\_battalions(root)

front

num\_unincorporated\_states

Get the scoped country's current number of unincorporated states

country

num\_units

Get the number of active units for a commander or military formation  
Get the number of active units for military formations in the province that are allied to the specified country

character, military\_formation, province

num\_units\_in\_battle

Get the number of active units in battle for a commander or military formation  
Get the number of active units in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

num\_units\_not\_in\_battle

Get the number of active units not in battle for a commander or military formation  
Get the number of active units not in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

num\_units\_share

Share of units a commander has considering his command limit.

Example: if the character has 30 Command Limit, the fleet has 60 total Command Limit, and the formation has 50 total flotillas, then the character should get 25 flotillas for their battle)

character

num\_world\_market\_hub\_trade\_center\_levels

Get the total number of trade center levels associated with scope world market hub state

state

offense

Returns the unit's offense rating (including impact from their commander)

new\_combat\_unit

participants

Get the total number of participating countries in the scoped market

market

player\_owed\_obligation\_days\_left

Scope to number of days left on an obligation in diplomatic relations scope (example: obligation\_days\_left >= 2)

diplomatic\_relations

pop\_weight\_modifier\_scale

Get the Pop's weight modifier scale multiplier

pop

popularity

Get the scoped character's popularity, normally ranging between -100 and +100

character

population\_below\_expected\_sol

Scope to the current percentage of the state's population that are below their expected standard of living (0-1)

state

principal

Get the current debt principal of the country

country

prosperity

Scope to the amount of prosperity for scope company (example: prosperity >= 2)

company

rank\_value

Scope to rank value of a particular country rank (rank\_value:great\_power)

none

relations

Scope to relations between two countries (example: relations:root >= 2)

country

relations\_change\_rate

Scope to daily change in relations progress between two countries (example: relations\_change\_rate:root > 0)

country

relations\_threshold

Scope to relations threshold level for comparison purposes (relations\_threshold:warm)

none

remaining\_binding\_period

Get the current remaining binding period in days for the scoped treaty

treaty, treaty\_options

scope\_relations

Scope to relations value in diplomatic relations scope (example: scope\_relations >= 2)

diplomatic\_relations

scope\_tension

Scope to tension value in diplomatic relations scope (example: scope\_tension >= 2)

diplomatic\_relations

techs\_researched

Scope to the current amount of techs researched of a country

country

tension

Scope to tension between two countries (example: tension:root >= 2)

country

tension\_threshold

Scope to tension level threshold for comparison purposes (tension\_threshold:warm)

none

total\_export\_value

Scope to the total value of scope country's exports to the world market (example: total\_export\_value >= 1000)

country

total\_import\_value

Scope to the total value of scope country's imports from the world market (example: total\_import\_value >= 1000)

country

total\_trade\_value

Scope to the total value of scope country's trade with the world market (example: total\_trade\_value >= 1000)

country

training\_rate

Scope to the current maximum weekly training rate of a building

building

treaty\_exports

Get the current number of treaty (NOT world market) exports of the good to the market of the scoped MarketGoods

market\_goods

treaty\_imports

Get the current number of treaty (NOT world market) imports of the good to the market of the scoped MarketGoods

market\_goods

value

A numeric literal value eg: 1, 5.2, -6

none

weekly\_prosperity\_change

Scope to the weekly prosperity change for scope company (example: weekly\_prosperity\_change >= 2)

company

## Iterator triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=3 "Edit section: Iterator triggers") | [edit source](/index.php?title=Trigger&action=edit&section=3 "Edit section: Iterator triggers")\]

Iterators examine all relevant scopes and output one or more. By default, iterators examine only one of the relevant scopes, but can be forced to examine more with the `count` or `percent` parameters

List of iterator triggers

Trigger

Description

Example

Scopes

Targets

any\_active\_law

Iterate through all active laws in a country

any\_active\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_active\_party

Iterate through all active political parties in a country

any\_active\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_character

Iterate through all characters globally

any\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_exile\_pool

Iterate through characters in the exile pool

any\_character\_in\_exile\_pool = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_void

Iterate through characters in the void

any\_character\_in\_void = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_civil\_war

Iterate through all civil wars related to the scoped country

any\_civil\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

civil\_war

any\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

any\_cobelligerent\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

any\_cobelligerent\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_combat\_unit

Iterate through all combat units of input scope

any\_combat\_unit = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

any\_company

Iterate through all companies in a country

any\_company = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

company

any\_country

Iterate through all countries globally

any\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_decentralized\_country

Iterate through all countries that are decentralized

any\_decentralized\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

any\_diplomatic\_catalyst = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_catalyst

any\_diplomatic\_play

Iterate through all diplomatic plays globally

any\_diplomatic\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

diplomatic\_play

any\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

any\_diplomatically\_relevant\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_direct\_subject

Any country directly below current in hierarchy

any\_direct\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

any\_enemy\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

any\_enemy\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

harvest\_condition

any\_in\_global\_list

Iterate through all items in global list.

any\_in\_global\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_hierarchy

Any country in current hierarchy, including current

any\_in\_hierarchy = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_in\_list

Iterate through all items in list.

any\_in\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_local\_list

Iterate through all items in local list.

any\_in\_local\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

any\_influenced\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

interest\_group

any\_interest\_group

Iterate through all interest groups in a country

any\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_group

any\_law

Iterate through all laws in a country

any\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_lobby\_member

Iterate through all interest group members of a lobby

any\_lobby\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_lobby

interest\_group

any\_market

Iterate through all markets globally

any\_market = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

market

any\_market\_goods

Iterate through all active (market) goods in a market

any\_market\_goods = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market

market\_goods

any\_member

Iterate through all interest group members of a party

any\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

party

interest\_group

any\_military\_formation

Iterate through all military formations currently present at input scope

any\_military\_formation = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, hq

military\_formation

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

state

any\_overlord\_or\_above

Any country above current in hierarchy

any\_overlord\_or\_above = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_participant

Any of two participants of the diplomatic pact in a scope

any\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_pact

country

any\_political\_lobby

Iterate through all political lobbies in a country or interest group

any\_political\_lobby = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, interest\_group

political\_lobby

any\_political\_movement

Iterate through all political movements in a country

any\_political\_movement = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

political\_movement

any\_potential\_party

Iterate through all potential political parties in a country

any\_potential\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_power\_bloc

Iterate through all power blocs

any\_power\_bloc = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

power\_bloc

any\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

any\_power\_bloc\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

power\_bloc

country

any\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

any\_preferred\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

law

any\_primary\_culture

Primary cultures of the scoped country or country definition

any\_primary\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, country\_definition

culture

any\_province

Iterate through all Provinces in the scoped State

any\_province = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

province

state

any\_rival\_country

Any country that is being rivaled by the country in a scope

any\_rival\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_rivaling\_country

Any country that is rivaling the country in a scope

any\_rivaling\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_ally

Iterate through all allies to a: country

any\_scope\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_article

Iterate through articles of the scoped treaty

any\_scope\_article = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty

treaty\_article

any\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

any\_scope\_article\_option = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty, treaty\_options

treaty\_article\_options

any\_scope\_building

Iterate through all buildings in a: state, country

any\_scope\_building = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

building

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, strategic region)

any\_scope\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market, state\_region, strategic\_region

country

any\_scope\_culture

Iterate through all cultures in the scope

any\_scope\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

culture

any\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

any\_scope\_diplomatic\_pact = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_pact

any\_scope\_front

Iterate through all Fronts related to the scoped War

any\_scope\_front = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

front

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

any\_scope\_held\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_marker

any\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

any\_scope\_initiator\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_interest\_marker

Iterate through all interest markers in a: country, strategic region

any\_scope\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state\_region, strategic\_region

interest\_marker

any\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

any\_scope\_play\_involved = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, culture, interest\_group, state

pop

any\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

any\_scope\_regional\_hqs = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

company

building

any\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

any\_scope\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, state\_region, strategic\_region, theater

state

any\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

any\_scope\_target\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_theater

Iterate through all theaters in a: country

any\_scope\_theater = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

theater

any\_scope\_treaty

Iterate through in force treaties binding the scoped country

any\_scope\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

treaty

any\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

any\_scope\_violate\_sovereignty\_interested\_parties = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

any\_scope\_violate\_sovereignty\_wars = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_scope\_war

Iterate through all wars related to the scope

any\_scope\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

any\_sea\_node\_adjacent\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

state

any\_state

Iterate through all states globally

any\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state

any\_state\_region

Iterate through all state regions

any\_state\_region = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state\_region

any\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

any\_strategic\_objective = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

state

any\_subject\_of\_subject

Any country below direct subjects in hierarchy

any\_subject\_of\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_subject\_or\_below

Any country below current in hierarchy

any\_subject\_or\_below = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_supporting\_character

Iterate through all characters that support the scoped political movement

any\_supporting\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

character

any\_treaty

Iterate through treaties (both in force and drafts)

any\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

treaty

any\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

any\_valid\_mass\_migration\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

culture

any\_war\_participant

Iterate through all participants in a war

any\_war\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

country

## Flow triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=4 "Edit section: Flow triggers") | [edit source](/index.php?title=Trigger&action=edit&section=4 "Edit section: Flow triggers")\]

Flow triggers control how other triggers are used. This includes conditionals and loops as well as tooltips. They can always be used in any scope

List of flow triggers

Trigger

Description

Example

Scopes

Targets

all\_false

true if all contained triggers are false (equivalent to NOR)

none

and

true if all contained triggers are true

Returns false if at first false sub-trigger

none

any\_false

true if any contained trigger is false (equivalent to NAND)

none

calc\_true\_if

Returns true if the specified number of sub-triggers return true

calc\_true\_if = {
 amount = 2
 <trigger>
 <trigger>
 <trigger>
}

none

custom\_description

Wraps triggers that get a custom description instead of the auto-generated one

custom\_description = {
 text = <trigger\_localization\_key>
 subject = <optional subject scope> #defaults to current scope
 object = <optional object scope>
 value = <optional script value>
 ... triggers ...
}

none

custom\_tooltip

Replaces the tooltips for the enclosed triggers with a custom text

custom\_tooltip = {
 text = <text>
 subject = <scope> (optional)
 <trigger>
}

none

hidden\_trigger

Enclosed triggers are not shown in tooltips

hidden\_trigger = { <more triggers> }

none

nand

a negated AND trigger

Returns false if all contained triggers return true; returns true at first false sub-trigger

none

nor

a negated OR trigger

Returns true if all contained triggers return false; returns false at first true trigger

none

not

negates content of trigger

Returns true if all contained triggers return false

none

or

false if all contained triggers are false

Returns true at first true sub-trigger

none

switch

Switch on a trigger for the evaluation of another trigger with an optional fallback trigger.

switch = {
 trigger = simple\_assign\_trigger
 case\_1 = { <triggers> }
 case\_2 = { <triggers> }
 case\_n = { <triggers> }
 fallback = { <triggers> }

none

trigger\_else

Evaluates the display\_triggers if the triggers of preceding 'trigger\_if' or 'trigger\_else\_if' are not met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else = { <display\_triggers> }

none

trigger\_else\_if

Evaluates the display\_triggers if the triggers of the preceding \`trigger\_if\` or \`trigger\_else\_if\` are not met and its own trigger of the limit is met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

none

trigger\_if

Evaluates the display\_triggers if the triggers of the limit are met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

none

weighted\_calc\_true\_if

Returns true if the sum of weights of fulfilled sub-triggers amount to the specified sum

weighted\_calc\_true\_if = {
 amount = 10
 5 = { <trigger> }
 15 = { <trigger> }
 7 = { <trigger> }
}

none

## Log triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=5 "Edit section: Log triggers") | [edit source](/index.php?title=Trigger&action=edit&section=5 "Edit section: Log triggers")\]

Log triggers output a message for testing and debugging purposes. They can always be used in any scope.

List of log triggers

Trigger

Description

Example

Scopes

Targets

assert\_if

Conditionally cause an assert during run time

assert\_if = {
 limit = { <triggers> }
 text = <string> (optional)
}

none

assert\_read

Conditionally cause an assert during read time

assert\_read = yes / <string>

none

debug\_log

Log whether the parent trigger succeeded or failed

none

debug\_log\_details

Log whether the parent trigger succeeded or failed. Log which children succeeded or failed

none

## Variable triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=6 "Edit section: Variable triggers") | [edit source](/index.php?title=Trigger&action=edit&section=6 "Edit section: Variable triggers")\]

Variable triggers check a [variable](/Variable "Variable"). They can always be used in any scope, but may require a certain scope to read the correct variable.

List of variable triggers

Trigger

Description

Example

Scopes

Targets

add\_to\_temporary\_list

Saves a temporary target for use during the trigger execution

This is used to build lists in triggers.
If used within an any-trigger, placement within the trigger is quite important.
The game will iterate through every instance of the any-trigger
until it finds a single instance that fulfills the requirements, and then it will stop.
In order to add every instance of a scope that fulfills certain conditions,
use "count = all" while also placing this "effect" at the very end of the any-trigger
so that every condition is evaluated for every iteration.

none

any\_in\_global\_list

Iterate through all items in global list.

any\_in\_global\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_list

Iterate through all items in list.

any\_in\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_local\_list

Iterate through all items in local list.

any\_in\_local\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

global\_variable\_list\_size

Checks the size of a global variable list

global\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

has\_global\_variable

Checks whether the current scope has the specified global variable set

has\_global\_variable = name

none

has\_global\_variable\_list

Checks whether the current scope has the specified global variable list set

has\_global\_variable\_list = name

none

has\_local\_variable

Checks whether the current scope has the specified local variable set

has\_local\_variable = name

none

has\_local\_variable\_list

Checks whether the current scope has the specified local variable list set

has\_local\_variable\_list = name

none

has\_variable

Checks whether the current scope has the specified variable set

has\_variable = name

none

has\_variable\_list

Checks whether the current scope has the specified variable list set

has\_variable\_list = name

none

is\_in\_list

Checks if a target in in a list

none

is\_target\_in\_global\_variable\_list

Checks if a target is in a global variable list

is\_target\_in\_global\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

is\_target\_in\_local\_variable\_list

Checks if a target is in a local variable list

is\_target\_in\_local\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

is\_target\_in\_variable\_list

Checks if a target is in a variable list

is\_target\_in\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

list\_size

Checks the size of a list

list\_size = {
 name = <list name>
 value >= <value>
}

none

value

local\_variable\_list\_size

Checks the size of a local variable list

local\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

variable\_list\_size

Checks the size of a variable list

variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

## Triggers by scope\[[edit](/index.php?title=Trigger&veaction=edit&section=7 "Edit section: Triggers by scope") | [edit source](/index.php?title=Trigger&action=edit&section=7 "Edit section: Triggers by scope")\]

The following tables list triggers by their required scope. Some triggers are repeated as they can be used in multiple scopes.

### None/any scope\[[edit](/index.php?title=Trigger&veaction=edit&section=8 "Edit section: None/any scope") | [edit source](/index.php?title=Trigger&action=edit&section=8 "Edit section: None/any scope")\]

List of none triggers

Trigger

Description

Example

Targets

active\_lens

Checks if the specified lens is open

active\_lens = lens

active\_lens\_option

Checks if the specified lens option is activated

active\_lens\_option = lens\_option\_key

add\_to\_temporary\_list

Saves a temporary target for use during the trigger execution

This is used to build lists in triggers.
If used within an any-trigger, placement within the trigger is quite important.
The game will iterate through every instance of the any-trigger
until it finds a single instance that fulfills the requirements, and then it will stop.
In order to add every instance of a scope that fulfills certain conditions,
use "count = all" while also placing this "effect" at the very end of the any-trigger
so that every condition is evaluated for every iteration.

all\_false

true if all contained triggers are false (equivalent to NOR)

always

checks if the assigned yes/no value is true

always = yes # always succeeds
always = no # always fails
always = scope:a\_boolean\_value # evaluated at runtime

boolean

and

true if all contained triggers are true

Returns false if at first false sub-trigger

any\_character

Iterate through all characters globally

any\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_character\_in\_exile\_pool

Iterate through characters in the exile pool

any\_character\_in\_exile\_pool = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_character\_in\_void

Iterate through characters in the void

any\_character\_in\_void = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_country

Iterate through all countries globally

any\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_decentralized\_country

Iterate through all countries that are decentralized

any\_decentralized\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_diplomatic\_play

Iterate through all diplomatic plays globally

any\_diplomatic\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

any\_false

true if any contained trigger is false (equivalent to NAND)

any\_in\_global\_list

Iterate through all items in global list.

any\_in\_global\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

any\_in\_list

Iterate through all items in list.

any\_in\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

any\_in\_local\_list

Iterate through all items in local list.

any\_in\_local\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

any\_market

Iterate through all markets globally

any\_market = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market

any\_power\_bloc

Iterate through all power blocs

any\_power\_bloc = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

power\_bloc

any\_state

Iterate through all states globally

any\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_state\_region

Iterate through all state regions

any\_state\_region = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state\_region

any\_treaty

Iterate through treaties (both in force and drafts)

any\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty

assert\_if

Conditionally cause an assert during run time

assert\_if = {
 limit = { <triggers> }
 text = <string> (optional)
}

assert\_read

Conditionally cause an assert during read time

assert\_read = yes / <string>

calc\_true\_if

Returns true if the specified number of sub-triggers return true

calc\_true\_if = {
 amount = 2
 <trigger>
 <trigger>
 <trigger>
}

can\_create\_treaty

Checks if a treaty between countries can be created

can\_create\_treaty = {
 first\_country = <country>   # (default: none) must \_not\_ be specified if the created treaty is an amendment
 second\_country = <country>   # (default: none) must \_not\_ be specified if the created treaty is an amendment
 amends = <treaty>     # (default: none) if specified, the created treaty will be an amendment to the specified treaty

 is\_draft = yes | no     # (default: yes) must be yes if this treaty is an amendement to an existing treaty
 entered\_into\_force\_on = <date>  # (default: none) means "today" if not specified (for non-drafts). Must \_not\_ be specified for drafts. Cannot be in the future
 binding\_period = {    # (default: 0) must be a nonnegative number, fractional parts will be ignored (truncation)
  days = <nonnegative number>
  | weeks = <nonnegative number>
  | months = <nonnegative number>
  | years = <nonnegative number>
 }

 articles\_to\_create = {   # (default: empty) non-amendments must have at least one article to create
  {
   article = <key>    # (default: empty) the type of article (determined by script)
   source\_country = <country> # (default: none) directed articles \_must\_ specify this value, mutual articles \_must not\_ specify this value
   target\_country = <country> # (default: none) directed articles \_must\_ specify this value, mutual articles \_must not\_ specify this value

   inputs = {    # (default: empty) list of inputs values as required by the article
    {
     quantity = <positive number>
     | good = <good>
     | state = <state>
     | strategic\_region = <strategic region>
     | company = <company> # TODO: or company type?
     | building = <building> # TODO: or building type?
     | law = <law> # TODO: or law type?
     | country = <country>
    }

    { ... }     # one input per required input of the corresponding article type
   }
  }

  # some examples of valid articles
  { article = alliance }   # mutual, no inputs
  {
   article = resource\_transfer # directed
   source\_country = c:FRA  # because directed
   target\_country = c:GBR   # because directed

   inputs = {
    { quantity = 250 }  # because resource\_transfer has required\_inputs = { quantity good }
    { good = g:coal }  # because resource\_transfer has required\_inputs = { quantity good }
   }
  }
 }
}

can\_start\_tutorial\_lesson

Can the specified tutorial lesson be started?

can\_start\_tutorial\_lesson = reactive\_advice\_succession

current\_tooltip\_depth

Returns the number of tooltips currently open on screen

value

custom\_description

Wraps triggers that get a custom description instead of the auto-generated one

custom\_description = {
 text = <trigger\_localization\_key>
 subject = <optional subject scope> #defaults to current scope
 object = <optional object scope>
 value = <optional script value>
 ... triggers ...
}

custom\_tooltip

Replaces the tooltips for the enclosed triggers with a custom text

custom\_tooltip = {
 text = <text>
 subject = <scope> (optional)
 <trigger>
}

day\_value

Day value

value

daynight\_value

DayNight value

value

debug\_log

Log whether the parent trigger succeeded or failed

debug\_log\_details

Log whether the parent trigger succeeded or failed. Log which children succeeded or failed

exists

Checks whether the specified scope target exists (check for not being the null object)

exists = from.owner.var:cool\_var.mother

game\_date

Compare to current game date

game\_date = 1837.1.1

value

global\_population

Compares the global population

global\_population > 2000000000

value

global\_variable\_list\_size

Checks the size of a global variable list

global\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

has\_account\_item

Does the player have the item in the account

has\_account\_item = <key>

has\_cosmetic\_dlc

Does the client have this cosmetic DLC

has\_dlc\_feature

Does the host have DLC that enables this particular feature

has\_game\_rule

Is the given game rule setting enabled?

has\_game\_rule = faster\_conversion

has\_game\_started

True if game has started

has\_game\_started = bool

boolean

has\_gameplay\_dlc

Does the host have this gameplay DLC

has\_global\_variable

Checks whether the current scope has the specified global variable set

has\_global\_variable = name

has\_global\_variable\_list

Checks whether the current scope has the specified global variable list set

has\_global\_variable\_list = name

has\_local\_variable

Checks whether the current scope has the specified local variable set

has\_local\_variable = name

has\_local\_variable\_list

Checks whether the current scope has the specified local variable list set

has\_local\_variable\_list = name

has\_map\_interaction

Checks if the map interaction type is active

has\_map\_interaction = key

has\_map\_interaction\_diplomatic\_action

Checks if our current map interaction is a specific diplomatic action

has\_map\_interaction\_diplomatic\_action = <key>

has\_reached\_end\_date

True if the end date (NDefines::NGame::END\_DATE) has been reached

has\_reached\_end\_date = yes

boolean

has\_unification\_candidate

Check if there is at least one unification candidate for country tag

has\_country\_unification\_candidate = GER

has\_variable

Checks whether the current scope has the specified variable set

has\_variable = name

has\_variable\_list

Checks whether the current scope has the specified variable list set

has\_variable\_list = name

hidden\_trigger

Enclosed triggers are not shown in tooltips

hidden\_trigger = { <more triggers> }

is\_building\_type\_expanded

Checks if the CProductionMethodsPanelEntry for a particular CBuildingType is expanded

is\_building\_type\_expanded = <scope/key>

is\_game\_paused

Checks if the game is paused

is\_game\_paused = yes/no

boolean

is\_gamestate\_tutorial\_active

Is the gamestate tutorial active? See save\_progress\_in\_gamestate in tutorial\_lesson\_chains documentation.

boolean

is\_in\_list

Checks if a target in in a list

is\_lens\_open

Checks if a certain lens is open, specified as a lens key. Specify an optional named tab to check if this tab is open.

is\_lens\_open = {
 lens = lens\_key
 tab\_name = tab\_key
}

is\_objective\_completed

Is the objective completed for the country in scope?

is\_objective\_completed = yes

boolean

is\_panel\_open

Checks if a certain infopanel is open, specified as an event target (target) or as a string (panel\_name).

Specify an optional named tab (tab\_name) to check if this tab is open.

is\_panel\_open = {
 target = <scope> OR panel\_name = panel\_name
 tab\_name = tab\_name
}

is\_popup\_open

Checks if the specified popup panel is open

is\_popup\_open = popup

is\_rightclick\_menu\_open

Checks if the specified rightclick menu is open

is\_rightclick\_menu\_open = <menu\_key>

is\_set

Checks whether the specified scope target has been set (includes being the null object)

is\_set = from.owner.var:cool\_var.mother

is\_target\_in\_global\_variable\_list

Checks if a target is in a global variable list

is\_target\_in\_global\_variable\_list = {
 name = <variable name>
 target = <event target>
}

is\_target\_in\_local\_variable\_list

Checks if a target is in a local variable list

is\_target\_in\_local\_variable\_list = {
 name = <variable name>
 target = <event target>
}

is\_target\_in\_variable\_list

Checks if a target is in a variable list

is\_target\_in\_variable\_list = {
 name = <variable name>
 target = <event target>
}

is\_template\_used

Checks if character template has already been used

is\_template\_used = template\_key

is\_theme\_selected

Does the player have the theme selected in settings

is\_theme\_selected = <key>

is\_tutorial\_active

Is the tutorial active?

boolean

is\_tutorial\_lesson\_active

Is this the current tutorial lesson?

is\_tutorial\_lesson\_active = reactive\_advice\_succession

is\_tutorial\_lesson\_chain\_completed

Has the tutorial lesson chain with the specified key been finished?

is\_tutorial\_lesson\_completed

has the tutorial lesson with the specified name been finished?

is\_tutorial\_lesson\_step\_completed

Has the tutorial lesson step been finished?

is\_tutorial\_lesson\_step\_completed = lesson\_key:step\_key

list\_size

Checks the size of a list

list\_size = {
 name = <list name>
 value >= <value>
}

value

local\_variable\_list\_size

Checks the size of a local variable list

local\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

month

Compare to current game date month (Jan: 0, Dec: 11)

month > 10

value

nand

a negated AND trigger

Returns false if all contained triggers return true; returns true at first false sub-trigger

night\_value

Night value

value

nor

a negated OR trigger

Returns true if all contained triggers return false; returns false at first true trigger

not

negates content of trigger

Returns true if all contained triggers return false

or

false if all contained triggers are false

Returns true at first true sub-trigger

real\_month

Compare to current real date month (Jan: 1, Dec: 12)

real\_month > 10

value

save\_temporary\_scope\_as

Saves current scope as a temporary target for use during the trigger execution.

Cannot overwrite already saved scopes

save\_temporary\_scope\_as = <string>

save\_temporary\_scope\_value\_as

Saves a numerical or bool value as an arbitrarily-named temporary target to be referenced later in the same trigger.

Cannot overwrite already saved scope values

save\_temporary\_scope\_value\_as = {
 name = <string>
 value = <script\_value>
}

should\_show\_nudity

can nudity be shown?

should\_show\_nudity = yes/no

boolean

switch

Switch on a trigger for the evaluation of another trigger with an optional fallback trigger.

switch = {
 trigger = simple\_assign\_trigger
 case\_1 = { <triggers> }
 case\_2 = { <triggers> }
 case\_n = { <triggers> }
 fallback = { <triggers> }

trigger\_else

Evaluates the display\_triggers if the triggers of preceding 'trigger\_if' or 'trigger\_else\_if' are not met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else = { <display\_triggers> }

trigger\_else\_if

Evaluates the display\_triggers if the triggers of the preceding \`trigger\_if\` or \`trigger\_else\_if\` are not met and its own trigger of the limit is met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

trigger\_if

Evaluates the display\_triggers if the triggers of the limit are met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

variable\_list\_size

Checks the size of a variable list

variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

weighted\_calc\_true\_if

Returns true if the sum of weights of fulfilled sub-triggers amount to the specified sum

weighted\_calc\_true\_if = {
 amount = 10
 5 = { <trigger> }
 15 = { <trigger> }
 7 = { <trigger> }
}

year

Compares the current year of the game

year > 1850

value

### Building scope\[[edit](/index.php?title=Trigger&veaction=edit&section=9 "Edit section: Building scope") | [edit source](/index.php?title=Trigger&action=edit&section=9 "Edit section: Building scope")\]

List of building triggers

Trigger

Description

Example

Targets

any\_combat\_unit

Iterate through all combat units of input scope

any\_combat\_unit = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

new\_combat\_unit

building\_has\_goods\_shortage

Check if building has a shortage of any of its inputs

building\_has\_goods\_shortage = yes/no

boolean

can\_queue\_building\_levels

Checks if the building's owner could queue the provided number of additional levels without hitting a level or resource potential cap

can\_queue\_building\_levels = number

cash\_reserves\_available

Evaluates a production building's available cash reserves

cash\_reserves\_available > 25000

value

cash\_reserves\_ratio

Evaluates a production building's available cash reserve ratio compared to its maximum

Returns 1 if the building has no maximum cash reserves

cash\_reserves\_ratio > 0.2

value

country\_ownership\_fraction

Compares the fraction of this building's levels that are owned by a country

 country\_ownership\_fraction > 0.5

value

earnings

Compare a building's current annual earnings per employee

earnings >= 10

value

fraction\_of\_levels\_owned\_by\_country

Compares the fraction of total levels of a building a country or investors in that country owns

fraction\_of\_levels\_owned\_by\_country = {
 target = c:GBR
 value > 0.5
}

value

has\_active\_production\_method

Checks if a scoped building has the specified production method active

has\_active\_production\_method = key

has\_deployed\_units

Check if a building supports any units which have been deployed outside of home HQ

has\_deployed\_units = yes/no

boolean

has\_employee\_slots\_filled

Checks whether the amount of employees of a certain poptype are above or below a given percentage of the total amount the building can currently hire. I.e. if a building is at 80% (given as 0.8) of its current hiring capacity for Shopkeepers, for example.

has\_employee\_slots\_filled = {
 pop\_type = <pop type>
 percent = <0-1 value>
}

has\_failed\_hires

Checks if a building failed to hire someone last week

has\_failed\_hires = yes/no

boolean

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

is\_buildable

Check if a building is buildable = yes (default)

is\_buildable = yes/no

boolean

is\_building\_group

True if scope is a building of given group

building\_group = building

is\_building\_type

True if scope is a building of given type

is\_building\_type = building

is\_government\_funded

Check if a building is is\_government\_funded

is\_government\_funded = yes/no

boolean

is\_subsidized

Check if a building is being subsidized

is\_subsidized = yes/no

boolean

is\_subsistence\_building

Check if a building is a subsistence building

is\_subsistence\_building = yes/no

boolean

is\_under\_construction

Checks if building is under construction

is\_under\_construction = bool

boolean

levels\_owned\_by\_country

Compares how many levels a country or investors in that country own in a building

 levels\_owned\_by\_country = {
 target = c:GBR
 value >= 1
}

value

occupancy

Evaluates a building's current occupancy

occupancy < 0.25

value

private\_ownership\_fraction

Compares the fraction of this building's levels that are privately owned

 private\_ownership\_fraction > 0.5

value

self\_ownership\_fraction

Compares the fraction of this building's levels that are owned by the building itself

 self\_ownership\_fraction > 0.5

value

weekly\_profit

Checks whether the profits the building has made this week are above or below a given value

weekly\_profit >= 20.0

value

### Character scope\[[edit](/index.php?title=Trigger&veaction=edit&section=10 "Edit section: Character scope") | [edit source](/index.php?title=Trigger&action=edit&section=10 "Edit section: Character scope")\]

List of character triggers

Trigger

Description

Example

Targets

age

Compares the character age

age > 20

value

can\_agitate

Check if the scope character can agitate the target country

can\_agitate = <scope/c:TAG>

country

character\_acceptance

Compares the acceptance of the scoped character in the target country against an acceptance value

character\_acceptance = {
 target = c:FRA
 value > 50
}

value

character\_supports\_political\_movement

Checks whether the scoped character supports a political movement

character\_supports\_political\_movement = yes/no

boolean

commander\_is\_available

Check if a commander is not busy

commander\_is\_available = bool

boolean

commander\_rank

Compares the commanders rank

commander\_rank > 2

value

could\_support\_political\_movement

Check if a character could potentially support a political movement

could\_support\_political\_movement = <scope>

experience\_level

Compares the character experience level

experience\_level > 1

value

has\_commander\_order

Checks whether the scoped character is following the given order

has\_commander\_order = order type key/order type scope

has\_culture

Checks characters culture

has\_culture = culture scope or character scope

has\_ideology

Check if scoped character or interest group has ideology

has\_military\_formation

Checks if character has a Military Formation

has\_military\_formation = bool

boolean

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

has\_religion

Checks characters religion

has\_religion = religion/character scope

has\_role

Checks if character has the specified role

has\_role = role

has\_template

Checks if character was made from the specified template

has\_template = template\_key

has\_trait

Checks if character has specific trait

has\_trait = trait

is\_advancing\_on\_front

Checks if a commander is advancing on a front

is\_advancing\_on\_front = front

front

is\_attacker\_in\_battle

Checks if a Commander is attacker in a battle

is\_attacker\_in\_battle = bool

boolean

is\_busy

Check if character is busy

is\_busy = bool

boolean

is\_character\_alive

Checks if the scoped character is alive

is\_character\_alive = bool

boolean

is\_defender\_in\_battle

Checks if a Commander is defender in a battle

is\_defender\_in\_battle = bool

boolean

is\_female

Check if character is female

is\_female = bool

boolean

is\_heir

Checks whether the scoped character is an heir

is\_heir = yes / no

boolean

is\_historical

Check if character is historical

is\_historical = yes/no

boolean

is\_immortal

checks if the scoped character is immortal

scope:abbath = { is\_immortal = yes }

boolean

is\_in\_battle

Checks if a Commander is engaged in battle

is\_in\_battle = bool

boolean

is\_in\_exile\_pool

Checks whether the scoped character is in the exile pool

is\_in\_exile\_pool = yes/no

boolean

is\_in\_void

Check if character is in the void

is\_in\_void = bool

boolean

is\_interest\_group\_type

Checks if Interest Group is of a certain type

Can also be used on characters directly

is\_interest\_group\_type = x

is\_monarch

Checks if character is a monarch of a country with hereditary succession

is\_monarch = bool

boolean

is\_on\_front

Checks if a character or military formation has been assigned to a Front and has arrived there

is\_on\_front = yes/no

boolean

is\_ruler

Checks if character is a ruler/head of state of a country

is\_ruler = bool

boolean

law\_enactment\_stance

Compares the stance of the scoped character, movement or interest group has about enactment of the specified law compared to the current active law in the same group

law\_stance

Compares the base stance of the scoped character, movement or interest group has about the specified law, ignoring the current active law in the same group

trait\_value

Compares the character's total trait value

trait\_value < experience\_level

value

was\_exiled

Checks whether the scoped character was exiled

was\_exiled = yes/no

boolean

years\_of\_service

Compares the commander's years of service

years\_of\_service > 1

value

### Civil war scope\[[edit](/index.php?title=Trigger&veaction=edit&section=11 "Edit section: Civil war scope") | [edit source](/index.php?title=Trigger&action=edit&section=11 "Edit section: Civil war scope")\]

List of civil war triggers

Trigger

Description

Example

Targets

civil\_war\_progress

Compare progress of civil war

civil\_war\_progress > 0.50

value

is\_civil\_war\_type

Check if the scoped civil war is of a specific type

is\_civil\_war\_type = revolution / secession

### Company scope\[[edit](/index.php?title=Trigger&veaction=edit&section=12 "Edit section: Company scope") | [edit source](/index.php?title=Trigger&action=edit&section=12 "Edit section: Company scope")\]

List of company triggers

Trigger

Description

Example

Targets

any\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

any\_scope\_regional\_hqs = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

building

can\_potentially\_produce\_prestige\_goods

Checks if scope company can potentially produce target prestige good type

can\_potentially\_produce\_prestige\_good = pg:prestige\_good\_generic\_clothes

company\_employed\_levels

Check the number of employed building levels of the scoped company

company\_employed\_levels > 1.5

value

company\_global\_productivity\_comparison

Check the average global productivity of buildings matching this company's buildings, scaled by owned levels for each type

company\_global\_productivity\_comparison > 1.0

value

company\_has\_building\_type\_monopoly

Check if the scoped company has a monopoly of the specified building type

company\_has\_building\_type\_monopoly = bt:building\_food\_industry

building\_type

company\_has\_monopoly

Check if the scoped company has any monopoly

 company\_has\_monopoly = yes

boolean

company\_is\_prosperous

Check if a company is considered to be prosperous

boolean

company\_owned\_levels

Check the number of owned building levels of the scoped company

company\_owned\_levels > 1.5

value

company\_productivity

Check the productivity of the scoped company

company\_productivity > 1.0

value

company\_prosperity

Check the prosperity of a company

company\_prosperity > 0

value

is\_company\_type

Checks if scoped company is of the specified type

is\_company\_type = company\_type:company\_rheinmetall

company\_type

is\_producing\_prestige\_goods

Checks if scope company is producing prstige goods

is\_producing\_prestige\_goods = yes

boolean

### Country scope\[[edit](/index.php?title=Trigger&veaction=edit&section=13 "Edit section: Country scope") | [edit source](/index.php?title=Trigger&action=edit&section=13 "Edit section: Country scope")\]

List of country triggers

Trigger

Description

Example

Targets

additional\_war\_exhaustion

Compares the additional war exhaustion the scoped country has accumulated from scripted events in the target diplomatic play

additional\_war\_exhaustion = {
 target = scope:war.diplomatic\_play
 value > 1.0
}

value

aggressive\_diplomatic\_plays\_permitted

True if country is independent or permitted to start their own Diplomatic Plays

aggressive\_diplomatic\_plays\_permitted = bool

boolean

any\_active\_law

Iterate through all active laws in a country

any\_active\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

law

any\_active\_party

Iterate through all active political parties in a country

any\_active\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

party

any\_civil\_war

Iterate through all civil wars related to the scoped country

any\_civil\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

civil\_war

any\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

any\_cobelligerent\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

any\_cobelligerent\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_company

Iterate through all companies in a country

any\_company = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

company

any\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

any\_diplomatic\_catalyst = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_catalyst

any\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

any\_diplomatically\_relevant\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_direct\_subject

Any country directly below current in hierarchy

any\_direct\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

any\_enemy\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

any\_enemy\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

harvest\_condition

any\_in\_hierarchy

Any country in current hierarchy, including current

any\_in\_hierarchy = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_interest\_group

Iterate through all interest groups in a country

any\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

any\_law

Iterate through all laws in a country

any\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

law

any\_military\_formation

Iterate through all military formations currently present at input scope

any\_military\_formation = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

military\_formation

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_overlord\_or\_above

Any country above current in hierarchy

any\_overlord\_or\_above = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_political\_lobby

Iterate through all political lobbies in a country or interest group

any\_political\_lobby = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_lobby

any\_political\_movement

Iterate through all political movements in a country

any\_political\_movement = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

any\_potential\_party

Iterate through all potential political parties in a country

any\_potential\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

party

any\_primary\_culture

Primary cultures of the scoped country or country definition

any\_primary\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

culture

any\_rival\_country

Any country that is being rivaled by the country in a scope

any\_rival\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_rivaling\_country

Any country that is rivaling the country in a scope

any\_rivaling\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_ally

Iterate through all allies to a: country

any\_scope\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_building

Iterate through all buildings in a: state, country

any\_scope\_building = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

building

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_culture

Iterate through all cultures in the scope

any\_scope\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

culture

any\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

any\_scope\_diplomatic\_pact = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_pact

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

any\_scope\_held\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_marker

any\_scope\_interest\_marker

Iterate through all interest markers in a: country, strategic region

any\_scope\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_marker

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

pop

any\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

any\_scope\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_scope\_theater

Iterate through all theaters in a: country

any\_scope\_theater = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

theater

any\_scope\_treaty

Iterate through in force treaties binding the scoped country

any\_scope\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty

any\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

any\_scope\_violate\_sovereignty\_interested\_parties = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

any\_scope\_violate\_sovereignty\_wars = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

any\_scope\_war

Iterate through all wars related to the scope

any\_scope\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

any\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

any\_strategic\_objective = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_subject\_of\_subject

Any country below direct subjects in hierarchy

any\_subject\_of\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_subject\_or\_below

Any country below current in hierarchy

any\_subject\_or\_below = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

any\_valid\_mass\_migration\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

culture

approaching\_bureaucracy\_shortage

Check if Institutions in the country will incur a Bureaucracy shortage eventually

boolean

arable\_land\_country

Compare arable land in \*all\* states

arable\_land\_country > 10

value

army\_mobilization\_option\_fraction

Checks that a countries army has a certain percentage of units with a specific monbilization option

scope:country = {
 army\_mobilization\_option\_fraction = {
 target = mobilization\_option:key
 value >= 0.2
 }
}

value

army\_power\_projection

Compare to a country's total army power projection

 scope:example\_country = { army\_power\_projection < root.army\_power\_projection }

value

authority

Compares the available authority of the scoped country

value

authority\_usage

Compares the consumed authority of the scoped country

value

average\_country\_infrastructure

Check average infrastructure in all states owned by scope country

average\_country\_infrastructure = 3

value

average\_incorporated\_country\_infrastructure

Check average infrastructure in incorporated states owned by the scope country

average\_incorporated\_country\_infrastructure = 3

value

average\_sol\_for\_culture

Compares the average standard of living for the target culture in the country

average\_sol\_for\_culture = {
 target = cu:polish
 value > 10
}

value

average\_sol\_for\_primary\_cultures

Compare average standard of living for primary cultures

value

average\_sol\_for\_religion

Compares the average standard of living for the target religion in the country

average\_sol\_for\_religion = {
 target = rel:catholic
 value > 10
}

value

average\_sol\_for\_slaves

Compare average standard of living for enslaved pops

value

bureaucracy

Compares the available bureaucracy of the scoped country

value

bureaucracy\_usage

Compares the consumed bureaucracy of the scoped country

value

can\_add\_wargoal\_against

Checks if scope country can add any wargoal against target country in an active diplomatic play

can\_add\_wargoal\_against = <scope>

country

can\_afford\_diplomatic\_action

Checks if the country in scope can afford the Influence for the specified diplomatic action (pact or ongoing)

can\_afford\_diplomatic\_action = {
 target = <country scope>
 type = <diplomatic action key>
}

can\_break\_diplomatic\_pact

Checks if there is a diplomatic pact of the specified type with target country that can be broken by scope country

can\_break\_diplomatic\_pact = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

can\_create\_diplomatic\_pact

Checks if a diplomatic pact is valid to create with another country

can\_create\_diplomatic\_pact = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

can\_decrease\_autonomy

Check if a subject country is able to become a less autonomous subject type

can\_increase\_autonomy = yes

boolean

can\_establish\_company

Check if the country can establish a new company

boolean

can\_form\_nation

Check if the target country is able to potentially form a nation

can\_form\_nation = <tag>

can\_have\_as\_subject

Checks if a country can have another country as a particular type of subject

can\_have\_as\_subject = {
 who = <country scope>
 type = <subject type key>
}

can\_have\_subjects

Check if the country is able to have subjects of its own

boolean

can\_increase\_autonomy

Check if a subject country is able to become a more autonomous subject type

can\_increase\_autonomy = yes

boolean

can\_leave\_power\_bloc

Checks if the country in scope can leave its current Power Bloc (returns false if country is not in a Power Bloc)

can\_leave\_power\_bloc = yes

boolean

can\_lobbies\_target

Checks if target country is a valid target for lobbies in the scoped country

can\_lobbies\_target = <target country scope>

country

can\_own\_autonomy\_be\_decreased

Check if scoped country can have it's autonomy decreased

can\_own\_autonomy\_be\_decreased = yes/no

boolean

can\_research

True if a country can research an technology

can\_research = technology

can\_send\_diplomatic\_action

Checks if a diplomatic action is valid to send by scope country to target country

can\_send\_diplomatic\_action = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

can\_take\_on\_scaled\_debt

Checks if scoped country can take on a certain amount of scaled debt from another country

can\_take\_on\_scaled\_debt = {
 who = <country scope>
 value = <0-1 value>
}

can\_trigger\_event

Check if country can trigger the specified event

can\_trigger\_event = key

check\_area

Compares areas of object to another object

construction\_queue\_duration

Compares the maximum of all the very roughly approximated weeks remaining to finish the constructions in any queue

construction\_queue\_duration < 52

value

construction\_queue\_government\_duration

Compares the very roughly approximated weeks remaining to finish the constructions in the government queue

construction\_queue\_government\_duration < 52

value

construction\_queue\_num\_queued\_government\_levels

Compares the number of government constructed building levels in the construction queue

value

construction\_queue\_num\_queued\_levels

Compares the number of building levels in the construction queue

value

construction\_queue\_num\_queued\_private\_levels

Compares the number of privately constructed building levels in the construction queue

value

construction\_queue\_private\_duration

Compares the very roughly approximated weeks remaining to finish the constructions in the private queue

construction\_queue\_private\_duration < 52

value

country\_army\_unit\_type\_fraction

Checks that a country has a certain percentage of a specific army unit type

scope:example\_formation = {
 country\_military\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

value

country\_average\_cultural\_acceptance

Compares the average acceptance of a culture that is present in a country against a value

country\_average\_cultural\_acceptance = {
 target = cu:french
 value >= 50
}

value

country\_average\_religious\_acceptance

Compares the average acceptance of a religion that is present in a country against a value

country\_average\_religious\_acceptance = {
 target = rel:catholic
 value >= 50
}

value

country\_can\_have\_mass\_migration\_to

Checks if the scoped country can have mass migration to target country

can\_have\_mass\_migration\_to = c:country

country

country\_fervor\_primary\_culture

Compares with the hightest fervor of a country's primary cultures

country\_fervor\_primary\_culture > 0.2

value

country\_has\_building\_group\_levels

Checks the sum of building levels for a building group in a country

 country\_has\_building\_group\_levels = {
 type = bg\_heavy\_industry
 value >= 100
}

value

country\_has\_building\_levels

Checks the sum of building levels for a country

country\_has\_building\_levels >= 10

value

country\_has\_building\_type\_levels

Checks the sum of building levels for a building type in a country

 country\_has\_building\_type\_levels = {
 target = bt:building\_barracks
 value >= 100
}

value

country\_has\_building\_type\_monopoly

Check if the scoped country has a monopoly of the specified building type

country\_has\_building\_type\_monopoly = bt:building\_food\_industry

building\_type

country\_has\_country\_monopoly

Check if the scoped country has any country monopolies

boolean

country\_has\_primary\_culture

Checks if a culture is one of the primary cultures in the country

country\_has\_primary\_culture = <culture>

culture

country\_has\_state\_religion

Checks if a religion is the state religion in the country

country\_has\_accepted\_religion = <religion>

religion

country\_innovation

Checks the amount of weekly innovation a country produces

country\_innovation <= 200

value

country\_navy\_unit\_type\_fraction

Checks that a country has a certain percentage of a specific navy unit type

scope:example\_formation = {
 country\_navy\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

value

country\_or\_subject\_owns\_entire\_state\_region

Checks whether the scoped country or any of its subjects owns the entire specified state region

country\_or\_subject\_owns\_entire\_state\_region = key

country\_rank

Compares a Country's Power Ranking

country\_rank < rank\_value:major\_power

value

country\_tier

Compare tier of country tag

country\_tier = x

country\_turmoil

Compares the country's population weighted turmoil

country\_turmoil > 0.2

value

cultural\_acceptance\_base

Compares the acceptance from shared cultural traits of a culture in the scoped country against an acceptence value

cultural\_acceptance\_base = {
 target = cu:french
 value > 50
}

value

culture\_percent\_country

Checks that a country's population has a certain percentage of a specific culture

scope:example\_country = {
 culture\_percent\_country = {
  target = cu:dixie
  value >= 0.2
 }
}

value

discriminates\_religion

Checks if the scoped country discriminates the given religion (key)

discriminates\_religion = <religion key/scope>

religion

economic\_dependence

Compares the degree of dependence the country in scope has to the target country. **Note: This is an expensive trigger**

economic\_dependence = {
 target = scope:other\_country
 value > 2.2
}

value

empty\_agitator\_slots

Checks number of empty agitator slots in a country

empty\_agitator\_slots >= 1

value

enacting\_any\_law

Checks if the country is enacting any law.

enacting\_any\_law = bool

boolean

enactment\_chance

Compares the current enactment chance in scope country, including values from enactment modifiers

value

enactment\_chance\_for\_law

Compares the enactment chance in scope country for given law, including values from enactment modifiers

enactment\_chance\_for\_law = {
 target = scope:my\_lawtype
 value > 0.2
}

value

enactment\_chance\_for\_law\_without\_enactment\_modifier

Compares the enactment chance in scope country for given law, excluding values from enactment modifiers

enactment\_chance\_for\_law\_without\_enactment\_modifier = {
 target = scope:my\_lawtype
 value > 0.2
}

value

enactment\_chance\_without\_enactment\_modifier

Compares the current enactment chance in scope country, excluding values from enactment modifiers

value

enactment\_phase

Compares the current law enactment phase in scope country.

value

enactment\_setback\_count

Compares the current enactment setback count in scope country.

value

enemy\_contested\_wargoals

Determines the fraction of wargoals that enemies in the war are currently contesting

enemy\_contested\_wargoals = {
 target = scope:war
 value > 0.4
}

value

enemy\_occupation

Determines the (weighted) enemy occupation score of a country

enemy\_occupation > 0.2

value

expanding\_institution

Checks if the institution is expanding

expanding\_institution = <scope/key>

gdp\_ownership\_ratio

Compares the ratio of GDP the specified country owns in the scoped country

gdp\_ownership\_ratio = {
 target = scope:owner\_country
 value > 0.1
}

value

gdp\_per\_capita\_ranking

Compares a Country's GDP per Capita Ranking (position)

gdp\_per\_capita\_ranking > 42

value

global\_country\_ranking

Compares a Country's Power Ranking (position)

global\_country\_ranking > 42

value

gold\_reserves

Does the country have the required gold reserves

gold\_reserves > 500

value

gold\_reserves\_limit

Compares the country's gold reserves limit

gold\_reserves > gold\_reserves\_limit

value

goods\_production\_rank

Compare to a country's production leaderboard rank of a good

"goods\_production\_rank(g:luxury\_clothes)" < 10

value

government\_legitimacy

Compare Legitimacy

value

government\_transfer\_of\_power

Checks country's government's transfer of power

government\_transfer\_of\_power = key

value

government\_wage\_level

Compares the government wage level of scoped country. `very_low, low, medium, high, very_high`; returns same number as government\_wage\_level\_value trigger

government\_wage\_level = <level\_term>

value

government\_wage\_level\_value

Compares the government wage level value of scoped country. `0, 1, 2, 3, 4`

government\_wage\_level\_value = <level\_num>

value

has\_active\_peace\_deal

True if the country is in a war where there is a proposed peace deal

has\_active\_peace\_deal = bool

boolean

has\_any\_law\_commitment

Checks if a country has a commitment to enact any law

has\_any\_law\_commitment = yes

boolean

has\_any\_secessionists\_broken\_out

Check if the country has secessionists broken out

boolean

has\_any\_secessionists\_growing

Check if the country has any secessionists growing

boolean

has\_any\_subventions\_on

Check if the scoped country has any level of subventions on a goods

scope:country = {
 has\_any\_subventions\_on = g:grain
}

goods

has\_any\_tariffs\_on

Check if the scoped country has any level of tariffs on a goods

scope:country = {
 has\_any\_tariffs\_on = g:grain
}

goods

has\_attitude

Checks if scoped country has a particular attitude towards another country

has\_attitude = {
 who = <country scope>
 attitude = <attitude key>
}

has\_building

True if a state/market/state region/country has a building type

has\_building = building

has\_civil\_war\_from\_movement\_type

Checks if a country is having a civil war started by a particular movement type

has\_civil\_war\_from\_movement\_type = law\_type:law\_autocracy

political\_movement\_type

has\_claim

Checks if country in scope has a claim on state/state region/country

has\_claim = <scope>

has\_company

Checks if a company of the specified type exists in scope country

has\_company = company\_type:company\_rheinmetall

company\_type

has\_completed\_subgoal

Checks if the scoped country has completed a certain subgoal

has\_completed\_subgoal = subgoal\_key

has\_consumption\_tax

Checks if the country is taxing the target good.

c:FRA = { has\_consumption\_tax = g:grain }

goods

has\_convoys\_being\_sunk

Check if the country has convoys being sunk through convoy raiding

boolean

has\_decreasing\_interests

Checks if the country has decreasing interest levels

boolean

has\_diplomatic\_pact

Checks if two countries have an active diplomatic pact of type

has\_diplomatic\_pact = {
 who = <country scope>
 type = <diplomatic action key>
 is\_initiator = yes/no
}
is\_initiator is optional parameter that checks to see if scope country is the original initiator/target of the pact

has\_diplomatic\_relevance

Checks if target country is diplomatically relevant for scope country

has\_diplomatic\_relevance = <scope>

country

has\_diplomats\_expelled

Checks if country in scope has recently expelled diplomats of event target

has\_diplomats\_expelled = country

country

has\_famine

Check if state or country has famine

has\_famine = yes/no

boolean

has\_free\_government\_reform

Check if the country has a free (of radicals) government reform

has\_free\_government\_reform = <yes/no>

boolean

has\_global\_highest\_gdp

Checks if the scoped country has the highest GDP

has\_global\_highest\_gdp = yes/no

boolean

has\_global\_highest\_innovation

Checks if the scoped country has the highest weekly innovation

has\_global\_highest\_innovation = yes/no

boolean

has\_government\_clout

Does the country's government have the necessary total Clout

value

has\_government\_type

Is the country's government type as specified

has\_government\_type = key

has\_healthy\_economy

Check if the country has a healthy economy

boolean

has\_inactive\_journal\_entry

Check if the country has at least one inactive journal entry of the specified type

has\_inactive\_journal\_entry = key

has\_institution

Checks if scope country has a particular type of institution

has\_institution = <scope/key>

has\_insurrectionary\_interest\_groups

Check if the country has Interest Groups that are insurrectionary

boolean

has\_interest\_marker\_in\_region

True if scope country has an interest marker in target region

has\_interest\_marker\_in\_region = region scope/tag

has\_journal\_entry

Check if the country has at least one active journal entry of the specified type

has\_journal\_entry = key

has\_law

Checks if a country has a certain law active

law\_type

has\_law\_commitment

Checks if a country has a commitment to enact a certain law

law\_type

has\_law\_imposition\_rights

Checks if the scoped country has the necessary permits to demand another country enacts a certain law

has\_law\_imposition\_rights = {
 target\_country = scope:target\_country
 type = law\_type:law\_serfdom
}

has\_law\_or\_variant

Checks if a country has a certain law or one its variant laws active

law\_type

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

has\_objective

Checks if the scoped country has a certain objective type

has\_objective = objective\_type\_key

has\_overlapping\_interests

Checks if country in scope has an overlapping interest marker with any of target country's interests

has\_overlapping\_interests = <scope>

country

has\_port

Check if state has at least one port

has\_port = yes/no

boolean

has\_possible\_decisions

Check if a country has any possible Decisions

has\_possible\_decisions = yes

boolean

has\_potential\_to\_form\_country

Check if the target country could ever be able to form a nation

has\_potential\_to\_form\_country = <tag>

has\_power\_struggle

Checks if scope Power Bloc has a power struggle

has\_power\_struggle = yes/no

boolean

has\_researchable\_technology

Check if the country has any researchable technology left.

boolean

has\_revolution

Check if the country has revolutionary uprising

boolean

has\_ruling\_interest\_group

Does the country's government include the named IG

has\_ruling\_interest\_group\_count

Does the country's government consist of the specified number of IGs

value

has\_secret\_goal

Checks if scoped country has a particular secret goal towards another country

has\_secret\_goal = {
 who = <country scope>
 secret\_goal = <secret\_goal key>
}

has\_social\_hierarchy

Checks if the scoped country has adopted a specific social hierarchy

has\_social\_hierarchy = <key>

has\_state\_in\_state\_region

Check if country has a state in the state region

has\_state\_in\_state\_region = key

has\_strategic\_adjacency

Checks if country in scope has a strategic adjacency (direct/coastal/wargoal adjacency) to target state/country

has\_strategic\_adjacency = <scope>

has\_strategic\_land\_adjacency

Checks if country in scope has a strategic adjacency (direct land border or wargoal adjacency only) to target state/country

has\_strategic\_adjacency = <scope>

has\_strategy

Checks if country in scope has a particular AI strategy

has\_strategy = <key>

has\_subject\_relation\_with

Checks if country in scope is subject or overlord of event target

has\_subject\_relation\_with = country

country

has\_sufficient\_construction\_capacity\_for\_investment

Check if country has enough construction capacity to be spending all of its incoming investment pool funds.

boolean

has\_technology\_progress

Does the country have the required progress for an technology

has\_technology\_progress = {
 technology = <technology key>
 progress = <0-1 value>
}

value

has\_technology\_researched

True if a country has researched an technology

has\_technology\_researched = <scope/key>

has\_treaty\_port\_in\_market

Checks if the scoped country has a treaty port in target market

c:POR = { has\_treaty\_port\_in\_market = c:CHI.market }

market

has\_truce\_with

Check if a country has a truce with a different target country

has\_truce\_with = c:GBR

country

has\_war\_with

Checks if country in scope is at war with event target

has\_war\_with = country

country

has\_wasted\_construction

Check if country is wasting any of its produced construction.

boolean

highest\_secession\_progress

Compares the highest secession progress of any secession movement in a given country

highest\_secession\_progress > 0.7

value

in\_default

Check if the country is currently in default

boolean

in\_election\_campaign

Check if the country is in election campaign period

in\_election\_campaign = <yes/no>

boolean

influence

Compares the available influence of the scoped country

value

influence\_usage

Compares the consumed influence of the scoped country

value

institution\_investment\_level

Compares the level of investment in an institution

institution\_investment\_level = {
 institution = <key>
 value = <comparator value>
}

value

investment\_pool

Does the country have this amount of money saved in its investment pool

investment\_pool > 1000

value

investment\_pool\_gross\_income

Does the country have this amount of gross income (income before expenses) for its investment pool

investment\_pool\_gross\_income > 1000

value

investment\_pool\_net\_income

Does the country have this amount of net income (income after expenses) for its investment pool

investment\_pool\_net\_income > 1000

value

is\_adjacent\_to\_country

Checks if country in scope is adjacent to a target country

is\_adjacent\_to\_country = <scope>

country

is\_adjacent\_to\_state

Checks if country in scope is adjacent to a target state

is\_adjacent\_to\_state = <scope>

state

is\_ai

True if country scope is controlled by an AI

is\_ai = bool

boolean

is\_at\_war

Check if the country is at war

boolean

is\_banning\_goods

Check if a country is banning a good

is\_banning\_goods = <goods key/scope>

is\_considered\_adjacent\_due\_to\_wargoals

Checks if the scoped country is considered adjacent to the target country by virtue of the scoped country having wargoals that are adjacent to the target country in a committed diplomatic play

is\_considered\_adjacent\_due\_to\_wargoals = <target country scope>

country

is\_construction\_paused

Check if construction in a state is paused.

boolean

is\_country\_alive

Checks if the scoped country is alive, i.e. if it has at least one state on the map and can be interacted with

is\_country\_alive = yes/no

boolean

is\_country\_type

Checks the countrys type

is\_country\_type = country type

is\_diplomatic\_play\_ally\_of

Checks if country in scope is in a diplomatic play together with event target

is\_diplomatic\_play\_ally\_of = country

country

is\_diplomatic\_play\_committed\_participant

True if country is a committed participant of any diplomatic play

is\_diplomatic\_play\_participant = bool

boolean

is\_diplomatic\_play\_enemy\_of

Checks if country in scope is in a diplomatic play against event target

is\_diplomatic\_play\_enemy\_of = country

country

is\_diplomatic\_play\_initiator

True if country is the initiator of any diplomatic play

is\_diplomatic\_play\_initiator = bool

boolean

is\_diplomatic\_play\_involved\_with

Checks if country in scope is involved in the same diplomatic play as event target

is\_diplomatic\_play\_involved\_with = country

country

is\_diplomatic\_play\_participant\_with

Checks if country in scope is a committed participant in the same diplomatic play as event target

is\_diplomatic\_play\_participant\_with = country

country

is\_diplomatic\_play\_target

True if country is the target of any diplomatic play

is\_diplomatic\_play\_target = bool

boolean

is\_diplomatic\_play\_undecided\_participant

True if country is a undecided participant of any diplomatic play

is\_diplomatic\_play\_participant = bool

boolean

is\_direct\_subject\_of

Checks if country in scope is a direct subject (not subject-of-subject) of event target

is\_direct\_subject\_of = country

country

is\_enacting\_law

Checks if the scoped country is enacting a specific law type.

is\_enacting\_law = law\_type:law\_conscription

law\_type

is\_expanding\_institution

Are you expanding an institution

is\_expanding\_institution = <yes>/<no>

boolean

is\_forced\_to\_join\_plays

Checks if the scoped country is forced to join the target country's Diplomatic Plays

is\_forced\_to\_join\_plays = c:country

country

is\_home\_country\_for

Checks if a country is the home country for target country

country

is\_immune\_to\_revolutions

Checks if the country has been set to be immune to revolutions via set\_immune\_to\_revolutions

Warning: This does not check if the country is naturally immune to revolutions due to for example being a revolutionary country itself, only for the effects of set\_immune\_to\_revolutions.

is\_immune\_to\_revolutions = yes/no

boolean

is\_in\_customs\_union

Check if the country is part of a customs union

boolean

is\_in\_customs\_union\_with

Check if the country is in a customs union with scoped country

is\_in\_customs\_union\_with = c:FRA

country

is\_in\_power\_bloc

Checks if the country is in a Power Bloc

is\_in\_power\_bloc = yes/no

boolean

is\_in\_same\_power\_bloc

Checks if the scoped country is in the same power bloc as the target scoped country

is\_in\_same\_power\_bloc = c:GBR

country

is\_in\_war\_together

Checks if country in scope is in war on the same side as event target

is\_in\_war\_together = country

country

is\_indirect\_subject\_of

Checks if country in scope is an indirect subject (subject-of-subject) of event target

is\_indirect\_subject\_of = country

country

is\_insurrectionary

Check if country, movement or IG is insurrectionary

boolean

is\_junior\_in\_customs\_union

True if country is a junior country in a customs custom

is\_junior\_in\_customs\_union = bool

boolean

is\_local\_player

True if country scope is a player

is\_local\_player = bool

boolean

is\_losing\_power\_rank

Check if the country is in the process of dropping in power ranking

boolean

is\_market\_reachable\_for\_trade

Check if the scope country can reach the specified market scope for purposes of trade

is\_market\_reachable\_for\_trade = scope:my\_market\_scope

market

is\_mass\_migration\_origin

Checks if the scoped country is the origin of mass migration

is\_mass\_migration\_origin = yes

boolean

is\_mass\_migration\_origin\_of\_culture

Checks if the scoped country origin of mass migration of specific culture

is\_mass\_migration\_origin\_of\_culture = cu:example\_culture

culture

is\_owed\_obligation\_by

Checks if the scoped country is owed a obligation by the target country

is\_owed\_obligation\_by = c:FRA

country

is\_player

True if country scope is a player

is\_player = bool

boolean

is\_power\_bloc\_leader

Checks if the country is a leader of a Power Bloc

is\_power\_bloc\_leader = yes/no

boolean

is\_researching\_technology

Check if the country is actively researching a tech

is\_researching\_technology = <scope/key/any>

is\_researching\_technology\_category

Check if the country is actively researching a tech category

is\_researching\_technology\_category = <key/any>

is\_revolutionary

Check if the country, movement or interest group is revolutionary

boolean

is\_secessionist

Check if the country, movement or interest group is secessionist

boolean

is\_subject

True if country is a subject

is\_subject = bool

boolean

is\_subject\_of

Checks if country in scope is subject (or subject-of-subject) of event target

is\_subject\_of = country

country

is\_subject\_type

Checks the country's subject type

is\_subject\_type = subject type

is\_supporting\_unification\_candidate

Check if scope country is supporting a unification candidate for a specific country formation

is\_supporting\_unification\_candidate = {
 who = c:PRU
 country\_formation = GER
}

is\_taxing\_goods

Check if a country is taxing a good

has\_embargo = <goods key/scope>

is\_unification\_candidate

Check if scope country is a unification candidate for country tag

is\_unification\_candidate = GER

is\_violating\_sovereignty\_of

Check if the scoped country is violating the sovereignty of a target country

is\_violating\_sovereignty\_of = c:GBR

country

isolated\_states

Compare number of Isolated States

value

leading\_producer\_of

Checks if country is producing the most of a certain good

leading\_producer\_of = g:luxury\_clothes

goods

leads\_customs\_union

Check if any other country is part of this country's customs union

leads\_customs\_union = bool

boolean

liberty\_desire

Compare trigger for the Liberty Desire value in a scoped country.

liberty\_desire > 50

value

liberty\_desire\_weekly\_progress

Compare trigger for the weekly Liberty Desire progress value in a scoped country.

liberty\_desire\_weekly\_progress > 0

value

liberty\_desire\_weekly\_progress\_from\_support\_independence

Compare trigger for the weekly Liberty Desire progress value from the scoped country having their independence supported.

liberty\_desire\_weekly\_progress\_from\_support\_independence > 0

value

literacy\_rate

Checks if a pop, state or country has a certain amount of literacy

literacy = value

value

loyalist\_fraction

Compares loyalist fraction in pops in state or country, all parameters except value are optional

loyalist\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

value

max\_law\_enactment\_setbacks

Compares to the max number of law enactment setbacks a country can suffer before the law fails

value

max\_num\_companies

The limit of how many companies the scoped country is allowed to have

num\_num\_companies > 0

value

max\_num\_declared\_interests

Compares the maximum number of declared interests of scoped country

max\_num\_declared\_interests = <value>

value

military\_wage\_level

Compares the military wage level of scoped country. `very_low, low, medium, high, very_high`; returns same number as military\_wage\_level\_value trigger

military\_wage\_level = <level\_term>

value

military\_wage\_level\_value

Compares the military wage level value of scoped country. `0, 1, 2, 3, 4`

military\_wage\_level\_value = <level\_num>

value

nationalization\_cost

Compares the total cost of nationalizing all buildings in scope country owned by the government or pops of target country

nationalization\_cost = {
 target = scope:other\_country
 value > 100000
}

value

naval\_power\_projection

Compare to a country's total navy power projection

scope:example\_country = { naval\_power\_projection < root.naval\_power\_projection }

value

neighbors\_any\_power\_bloc

Checks if the scoped country neighbors any power bloc. Note: the scoped country is not considered to be neighboring the power bloc it belongs to (if any)

neighbors\_any\_power\_bloc = yes/no

boolean

neighbors\_member\_of\_same\_power\_bloc

Checks if the scoped country neighbors any other country belonging to the same power bloc

neighbors\_member\_of\_same\_power\_bloc = yes/no

boolean

neighbors\_power\_bloc

Checks if the scoped country neighbors the target power bloc. Note: the scoped country is not considered to be neighboring the power bloc it belongs to (if any)

neighbors\_power\_bloc = c:PRU.power\_bloc

power\_bloc

num\_companies

How many companies the scoped country has

num\_companies > 0

value

num\_declared\_interests

Compares the number of declared interests of scoped country

num\_declared\_interests = <value>

value

num\_diplomatic\_pacts

Compare to a country's number of diplomatic pacts of the specified type

num\_diplomatic\_pacts = {
 type = <key>
 value < 10
}

value

num\_investments\_of\_type

Checks the number of buildings levels of a given type the scoped country has outside their own country.

 num\_investments\_of\_type = {
 type = building\_oil\_rig
 value > 5
}

value

num\_political\_lobbies

Compare to number of political lobbies in scoped country or interest group

num\_political\_lobbies > 0

value

num\_subjects

Compares the number of subjects of scoped country

num\_subjects = <value>

value

num\_taxed\_goods

Compares the number of consumption taxed goods of scoped country

num\_taxed\_goods = <value>

value

number\_of\_claims

The number of claims on foreign states the country has

number\_of\_claims > 0

value

number\_of\_possible\_decisions

The number of possible decisions a Country can take

number\_of\_possible\_decisions > 0

value

overlord\_can\_decrease\_subject\_autonomy

Check if an overlord can decrease a subject autonomy

overlord\_can\_decrease\_subject\_autonomy = bool

boolean

owes\_obligation\_to

Checks if country in scope owes a obligation to event target

owes\_obligation\_to = country

country

owns\_entire\_state\_region

Check if country owns entire region

owns\_entire\_state\_region = key

owns\_treaty\_port\_in

Does country own the treaty port in assigned state region

owns\_treaty\_port\_in = key

play\_participant\_has\_war\_goal\_of\_type\_against

Checks if scope country holds a wargoal of a specific type targeting a specific country in any diplomatic play

play\_participant\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

play\_side\_has\_war\_goal\_of\_type\_against

Checks if any country on the same side as scope country in any diplomatic play holds a wargoal of a specific type targeting a specific country

play\_side\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

politically\_involved\_ratio

Compares the percentage of politically involved population in a country

politically\_involved\_ratio > 0.25

value

pop\_type\_percent\_country

Checks whether the scoped country has <percent> of its population belonging to the specified pop type

scope:example\_country = {
 pop\_type\_percent\_country = {
  pop\_type = <pop\_type>
  percent <compare operator (<,>,= and so on)> <percent>
 }
}

value

potential\_diplomatic\_play\_power\_ratio

Check the expected power ratio between scope country side and target country side in a potential diplomatic play

Attempts to predict which sides involved participants will take (if any) so doesn't just check 'starting strengths'
potential\_diplomatic\_play\_power\_ratio = {
 type = dp\_open\_market
 target = c:BEL
 state = c:BEL.capital
 value > 1.0
}
state and type are optional, will default to dp\_open\_market on target capital

value

power\_bloc\_share\_gdp

Compare the share of GDP of the country in scope against all its Power Bloc current members, returns -1 if not in a Power Bloc

power\_bloc\_share\_gdp > 0.2

value

power\_bloc\_share\_gdp\_with

Compare the share of GDP of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_gdp\_with(scope:country) > 0.2"

value

power\_bloc\_share\_gdp\_without

Compare the share of GDP of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_gdp\_without(scope:country) > 0.2"

value

power\_bloc\_share\_power\_projection

Compare the share of Power Projection of the country in scope against all its Power Bloc members, returns -1 if not in a Power Bloc

power\_bloc\_share\_power\_projection > 0.2

value

power\_bloc\_share\_power\_projection\_with

Compare the share of Power Projection of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_power\_projection\_with(scope:country) > 0.2"

value

power\_bloc\_share\_power\_projection\_without

Compare the share of Power Projection of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_power\_projection\_without(scope:country) > 0.2"

value

power\_bloc\_share\_prestige

Compare the share of Prestige of the country in scope against all its Power Bloc members, returns -1 if not in a Power Bloc

power\_bloc\_share\_prestige > 0.2

value

power\_bloc\_share\_prestige\_with

Compare the share of Prestige of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_prestige\_with(scope:country) > 0.2"

value

power\_bloc\_share\_prestige\_without

Compare the share of Prestige of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_prestige\_without(scope:country) > 0.2"

value

prestige

Compare prestige

value

primary\_cultures\_percent\_country

Checks that a country's population has a certain percentage of the country's primary cultures

scope:example\_country = {
 primary\_cultures\_percent\_country >= 0.2
}

value

produced\_authority

Compares the produced authority of the scoped country

value

produced\_bureaucracy

Compares the produced bureaucracy of the scoped country

value

produced\_influence

Compares the produced influence of the scoped country

value

radical\_fraction

Compares radical fraction in pops in state or country, all parameters except value are optional

radical\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

value

radical\_population\_fraction

Compares the fraction of the population that are radicals in a given country

radical\_population\_fraction > 0.2

value

relative\_authority

Compares the relative production to usage of bureaucracy for the scoped country

value

relative\_bureaucracy

Compares relative production to usage of bureaucracy for the scoped country

value

relative\_influence

Compares relative production to usage of influence for the scoped country

value

religion\_percent\_country

Checks that a country's population has a certain percentage of a specific religion

scope:example\_country = {
 religion\_percent\_country = {
  target = rel:catholic
  value <= 0.8
 }
}

value

ruler\_can\_have\_command

Checks if the country's government type allows its ruler to have command

ruler\_can\_have\_command = yes/no

boolean

scaled\_debt

Compare value to a country's debt relative to debt ceiling

scaled\_debt = value

value

scaled\_gold\_reserves

Compare value to a country's gold reserves relative to reserves limit

scaled\_gold\_reserves = value

value

should\_set\_wargoal

Check if the country is lacking a primary wargoal in any diplomatic play it is part of

boolean

shrinking\_institution

Checks if the institution is shrinking

expanding\_institution = <scope/key>

size\_weighted\_lost\_battles\_fraction

Determines the fraction of battles the target country has lost in the target war, weighted by manpower size of all battles in the war

size\_weighted\_lost\_battles\_fraction = {
 target = scope:war
 value > 0.3
}
add = "size\_weighted\_lost\_battles\_fraction(scope:war)"

value

sol\_ranking

Compares a Country's Standard of Living Ranking (position)

sol\_ranking > 42

value

subject\_can\_increase\_autonomy

Check if a subject can increase its autonomy

subject\_can\_increase\_autonomy = bool

boolean

supply\_network\_strength

Compares the country's supply network strength (can exceed 1)

supply\_network\_strength > 1.1

value

taking\_loans

Check if the country is currently running a weekly deficit and taking loans to compensate

boolean

tax\_level

Compares the overall tax level of scoped country. `very_low, low, medium, high, very_high`; returns same number as tax\_level\_value trigger

tax\_level = <level\_term>

value

tax\_level\_value

Compares the overall tax level integer value of scoped country. `0, 1, 2, 3, 4`

income\_tax\_level\_value = <level\_num>

value

tenure\_in\_current\_power\_bloc\_days

Compare the number of days the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_days > 720

value

tenure\_in\_current\_power\_bloc\_months

Compare the number of (whole) months the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_months > 20

value

tenure\_in\_current\_power\_bloc\_weeks

Compare the number of (whole) weeks the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_weeks > 12

value

tenure\_in\_current\_power\_bloc\_years

Compare the number of (whole) years the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_years > 5

value

total\_population

Compares the total population of a given country

total\_population > 100000

value

total\_population\_including\_subjects

Compares the total population in the scope country and its subjects

total\_population\_including\_subjects > 40000000

value

total\_population\_including\_subjects\_share

Compares the total population in the scope country and its subjects' share of the global population

total\_population\_including\_subjects\_share > 0.4

value

total\_population\_share

Compares the total population of a given country's share of the global population

total\_population\_share > 0.02

value

transfer\_money\_gross\_income

Does the country have this amount of gross income (income before expenses) from money transfer treaties

transfer\_money\_gross\_income > 1000

value

transfer\_money\_net\_income

Does the country have this amount of net income (income after expenses) from money transfer treaties

transfer\_money\_net\_income > 1000

value

war\_participant\_has\_war\_goal\_of\_type\_against

Checks if scope country holds a wargoal of a specific type targeting a specific country in any war

war\_participant\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

war\_side\_has\_war\_goal\_of\_type\_against

Checks if any country on the same side as scope country in any war holds a wargoal of a specific type targeting a specific country

war\_side\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

was\_formed\_from

Check if a formed country previously had a specific definition

was\_formed\_from = <tag>

weekly\_net\_fixed\_income

Does the country have this amount of weekly income after fixed expenses

weekly\_net\_fixed\_income > 1000

value

would\_accept\_diplomatic\_action

Checks if a country would accept a diplomatic action proposed by another country

would\_accept\_diplomatic\_action = {
 actor = <country scope>
 type = <action type key>
 first\_state = <state scope, optional>
 second\_state = <state scope, optional>
 modify\_acceptance = <script value, optional>
}
if set, modify\_acceptance will be added to their acceptance score, to allow for checking if they would accept with an obligation, etc

### Culture scope\[[edit](/index.php?title=Trigger&veaction=edit&section=14 "Edit section: Culture scope") | [edit source](/index.php?title=Trigger&action=edit&section=14 "Edit section: Culture scope")\]

List of culture triggers

Trigger

Description

Example

Targets

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

pop

culture\_can\_have\_mass\_migration\_to\_country

Checks if the scoped culture can have mass migration to target country

can\_have\_mass\_migration\_to\_country = c:country

country

culture\_current\_fervor

Compares the current fervor for a given culture

culture\_current\_fervor > 0.2

value

culture\_has\_community\_in\_state

Checks if the scoped culture has a cultural community in a state.

culture\_has\_community\_in\_state = <state>

culture\_has\_national\_awakening

Checks if the scoped culture has an active national awakening

culture\_has\_national\_awakening = yes / no

boolean

culture\_national\_awakening\_occurred

Checks if the scoped culture has ever had a national awakening started

culture\_national\_awakening\_occurred = yes / no

boolean

culture\_secession\_progress

Checks the culture's progress percentage towards secession in a country. 0 if no secession movement is active for the culture.

culture\_secession\_progress = {
 target = scope:example\_country
 value > 0.5
}

value

culture\_target\_fervor

Compares the target fervor for a given culture

culture\_target\_fervor > 0.2

value

has\_cultural\_obsession

Checks if a culture has a certain goods as obsession

has\_cultural\_obsession = <goods key>

has\_culture\_graphics

Checks if a culture has a certain culture\_graphics

has\_culture\_graphics = <culture graphics key>

has\_discrimination\_trait

Checks if scoped culture or religion has the given discrimination trait

has\_trait = trait

has\_homeland

Checks if scoped culture has a homeland in target state or state region

has\_homeland = state/state region

is\_primary\_culture\_of

Checks if culture is any of a country's primary cultures

is\_primary\_culture\_of = <country>

country

shares\_heritage\_trait\_group\_with\_any\_primary\_culture

Checks if culture shares any trait group marked as 'heritage' with any of a country's primary cultures

shares\_heritage\_trait\_group\_with\_any\_primary\_culture = <country>

country

shares\_heritage\_trait\_group\_with\_culture

Checks if culture shares any trait group marked as 'heritage' with another culture

shares\_heritage\_trait\_group\_with\_culture = <culture>

culture

shares\_heritage\_trait\_group\_with\_state\_religion

Checks if the religion shares any heritage trait group with a country's state religion

shares\_heritage\_trait\_group\_with\_state\_religion = <country>

country

shares\_heritage\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'heritage' with any of a country's primary cultures

shares\_heritage\_trait\_with\_any\_primary\_culture = <country>

country

shares\_heritage\_trait\_with\_culture

Checks if culture shares any trait marked as 'heritage' with another culture

shares\_heritage\_trait\_with\_culture = <culture>

culture

shares\_language\_trait\_group\_with\_any\_primary\_culture

Checks if culture shares any trait group marked as 'language' with any of a country's primary cultures

shares\_language\_trait\_group\_with\_any\_primary\_culture = <country>

country

shares\_language\_trait\_group\_with\_culture

Checks if culture shares any trait group marked as 'language' with another culture

shares\_language\_trait\_group\_with\_culture = <culture>

culture

shares\_language\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'language' with any of a country's primary cultures

shares\_language\_trait\_with\_any\_primary\_culture = <country>

country

shares\_language\_trait\_with\_culture

Checks if culture shares any trait marked as 'language' with another culture

shares\_language\_trait\_with\_culture = <culture>

culture

shares\_tradition\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'tradition' with any of a country's primary cultures

shares\_language\_trait\_with\_any\_primary\_culture = <country>

country

shares\_tradition\_trait\_with\_culture

Checks if culture shares any trait marked as 'tradition' with another culture

shares\_language\_trait\_with\_culture = <culture>

culture

### Diplomatic play scope\[[edit](/index.php?title=Trigger&veaction=edit&section=15 "Edit section: Diplomatic play scope") | [edit source](/index.php?title=Trigger&action=edit&section=15 "Edit section: Diplomatic play scope")\]

List of diplomatic play triggers

Trigger

Description

Example

Targets

any\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

any\_scope\_initiator\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

any\_scope\_play\_involved = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

any\_scope\_target\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

escalation

Checks whether escalation has passed a certain threshold

escalation > -58

value

has\_play\_goal

Checks if diplomatic play has a certain war goal type

has\_play\_goal = return\_state

initiator\_is

Checks who the initiator of a diplomatic play is

initiator\_is = country

country

is\_diplomatic\_play\_type

Checks diplomatic play is of a certain type

is\_diplomatic\_play\_type = play type

is\_war

True if the diplomatic play has escalated into war

is\_war = bool

boolean

target\_is

Checks who the target of a diplomatic play is

target\_is = country

country

### Interest group scope\[[edit](/index.php?title=Trigger&veaction=edit&section=16 "Edit section: Interest group scope") | [edit source](/index.php?title=Trigger&action=edit&section=16 "Edit section: Interest group scope")\]

List of interest group triggers

Trigger

Description

Example

Targets

any\_political\_lobby

Iterate through all political lobbies in a country or interest group

any\_political\_lobby = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_lobby

any\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

any\_preferred\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

law

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

pop

has\_ideology

Check if scoped character or interest group has ideology

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

has\_party

True if IG scope has a party

has\_party = bool

boolean

ig\_approval

Compare to scoped interest group approval

Usages: ig\_approval > 2, ig\_approval < happy

value

ig\_clout

Compare to scoped interest group's clout

ig\_clout >= 0.2

value

ig\_government\_power\_share

Compare to scoped interest group's political strength divided by total government political strength

ig\_government\_power\_share >= 0.5

value

interest\_group\_population

Compares population number in an interest group

interest\_group\_population > 1000

value

interest\_group\_population\_percentage

Compares percentage of population in an interest group

interest\_group\_population\_percentage > 0.2

value

interest\_group\_supports\_political\_movement

Checks whether the scoped interest group supports a political movement

interest\_group\_supports\_political\_movement = yes/no

boolean

is\_in\_government

True if IG scope is in the government

is\_in\_government = bool

boolean

is\_insurrectionary

Check if country, movement or IG is insurrectionary

boolean

is\_interest\_group\_type

Checks if Interest Group is of a certain type

Can also be used on characters directly

is\_interest\_group\_type = x

is\_marginal

True if IG scope is marginal

is\_marginal = bool

boolean

is\_member\_of\_any\_lobby

Checks if interest group is member of any lobby

is\_member\_of\_any\_lobby = yes

boolean

is\_member\_of\_lobby

Checks if interest group is member of a certain lobby type

is\_member\_of\_lobby = lobby\_type:pro\_country\_lobby

political\_lobby\_type

is\_member\_of\_party

Checks if Interest Group is a member of target party

is\_member\_of\_party = <party>

party

is\_powerful

True if IG scope is influential

is\_powerful = bool

boolean

is\_revolutionary

Check if the country, movement or interest group is revolutionary

boolean

is\_same\_interest\_group\_type

Checks if Interest Group is of the same IG type as target

is\_interest\_group\_type = scope:neighbor\_leading\_ig

interest\_group

is\_secessionist

Check if the country, movement or interest group is secessionist

boolean

is\_strongest\_ig\_in\_government

Checks whether the scoped interest group has the most clout out of all interest groups in government

is\_strongest\_ig\_in\_government = yes/no

boolean

law\_enactment\_stance

Compares the stance of the scoped character, movement or interest group has about enactment of the specified law compared to the current active law in the same group

law\_stance

Compares the base stance of the scoped character, movement or interest group has about the specified law, ignoring the current active law in the same group

most\_powerful\_strata

Compares an interest groups most powerful strata

num\_political\_lobbies

Compare to number of political lobbies in scoped country or interest group

num\_political\_lobbies > 0

value

prefers\_law

Checks if the scoped interest group prefers the specified law to the comparison law

### Institution scope\[[edit](/index.php?title=Trigger&veaction=edit&section=17 "Edit section: Institution scope") | [edit source](/index.php?title=Trigger&action=edit&section=17 "Edit section: Institution scope")\]

List of institution triggers

Trigger

Description

Example

Targets

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

### Journal entry scope\[[edit](/index.php?title=Trigger&veaction=edit&section=18 "Edit section: Journal entry scope") | [edit source](/index.php?title=Trigger&action=edit&section=18 "Edit section: Journal entry scope")\]

List of journal entry triggers

Trigger

Description

Example

Targets

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

is\_goal\_complete

Check if the journal entry's goal has been met

is\_goal\_complete = yes/no

boolean

is\_progressing

Check if the journal entry is progressing

is\_progressing = yes/no

boolean

journal\_entry\_age

Return the age of the journal entry (since activation) in days

journal\_entry\_age > 720

value

scripted\_bar\_progress

Determines the progress of a scripted progress bar

scripted\_bar\_progress = {
 name = progress\_bar\_tag
 value > 0.4
}

value

### Market scope\[[edit](/index.php?title=Trigger&veaction=edit&section=19 "Edit section: Market scope") | [edit source](/index.php?title=Trigger&action=edit&section=19 "Edit section: Market scope")\]

List of market triggers

Trigger

Description

Example

Targets

any\_market\_goods

Iterate through all active (market) goods in a market

any\_market\_goods = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market\_goods

any\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, strategic region)

any\_scope\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

check\_area

Compares areas of object to another object

has\_active\_building

True if a state has an active building type

has\_active\_building = building

has\_building

True if a state/market/state region/country has a building type

has\_building = building

has\_port

Check if state has at least one port

has\_port = yes/no

boolean

market\_consumption\_share

Compare fraction of scope market consumption coming from target country

market\_consumption\_share = {
 target = <country scope>
 goods = key (optional)
 value > 0.5 }

value

market\_exports

Compare total units of exports from scope to target market (if set) or world market in general (if no market specified)

market\_exports = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

value

market\_exports\_reliance

Compare fraction of buy orders in scope market that are due to exports to target market (if set) or world market in general (if no market specified)

market\_exports\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

value

market\_has\_goods\_shortage

Check if market has a shortage on any of its building inputs

market\_has\_goods\_shortage = yes/no

boolean

market\_imports

Compare total units of imports from target to scope market

market\_imports = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

value

market\_imports\_reliance

Compare fraction of buy orders in scope market that are due to imports from target market (if set) or world market in general (if no market specified)

market\_imports\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

value

market\_number\_goods\_shortages

Check how many shortages a market has on any of its building inputs

market\_number\_goods\_shortages >= 2

value

market\_number\_goods\_shortages\_with

Check how many shortages a market has on any of its building inputs, plus the ones from the target country

"market\_number\_goods\_shortages\_with(scope:target)" >= 2

value

market\_number\_goods\_shortages\_without

Check how many shortages a market has on any of its building inputs, subtracting the ones from the target country

"market\_number\_goods\_shortages\_without(scope:target)" >= 2

value

market\_production\_share

Compare fraction of scope market production coming from target country

market\_production\_share = {
 target = <country scope>
 goods = key (optional)
 value > 0.5
}

value

market\_trade

Compare total units of goods traded between scope market and target market (if set) or world market in general (if no market specified)

market\_trade = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

value

market\_trade\_reliance

Compare fraction of buy and sell orders in scope market that are due to trade with target market (if set) or world market in general (if no market specified)

market\_trade\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

value

### Military formation scope\[[edit](/index.php?title=Trigger&veaction=edit&section=20 "Edit section: Military formation scope") | [edit source](/index.php?title=Trigger&action=edit&section=20 "Edit section: Military formation scope")\]

List of military formation triggers

Trigger

Description

Example

Targets

any\_combat\_unit

Iterate through all combat units of input scope

any\_combat\_unit = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

new\_combat\_unit

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

formation\_army\_unit\_type\_fraction

Checks that a formation has a certain percentage of a specific army unit type

scope:example\_formation = {
 country\_military\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

value

formation\_navy\_unit\_type\_fraction

Checks that a formation has a certain percentage of a specific navy unit type

scope:example\_formation = {
 formation\_navy\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

value

has\_high\_attrition

Checks if a Military Formation's attrition risk is higher than the base value for their type

has\_high\_attrition = bool

boolean

has\_mobilization\_option

Checks that a formation has a specific mobilization option

has\_mobilization\_option = mobilization\_option:key

mobilization\_option

is\_army

Checks if a military formation is Army

is\_army = bool

boolean

is\_fleet

Checks if a military formation is Fleet

is\_fleet = bool

boolean

is\_fully\_mobilized

Checks if the Military Formation is fully mobilized

is\_fully\_mobilized = bool

boolean

is\_mobilized

Checks if the military formation is mobilized

is\_mobilized = bool

boolean

is\_on\_front

Checks if a character or military formation has been assigned to a Front and has arrived there

is\_on\_front = yes/no

boolean

organization

Compares the Organization of the Military Formation in scope

organization <= define:NMilitary|MILITARY\_FORMATION\_ORGANIZATION\_MIN

value

organization\_target

Compares the target Organization of the Military Formation in scope

organization\_target < define:NMilitary|MILITARY\_FORMATION\_ORGANIZATION\_MAX

value

### New combat unit scope\[[edit](/index.php?title=Trigger&veaction=edit&section=21 "Edit section: New combat unit scope") | [edit source](/index.php?title=Trigger&action=edit&section=21 "Edit section: New combat unit scope")\]

List of new combat unit triggers

Trigger

Description

Example

Targets

has\_unit\_type

Checks if a Combat Unit is of the specified type

has\_unit\_type = unit\_type:key

combat\_unit\_type

unit\_formation\_has\_commander

Checks if the formation of the scoped combat unit has the provided character as one of its commanders

unit\_formation\_has\_commander = <character>

character

### Party scope\[[edit](/index.php?title=Trigger&veaction=edit&section=22 "Edit section: Party scope") | [edit source](/index.php?title=Trigger&action=edit&section=22 "Edit section: Party scope")\]

List of party triggers

Trigger

Description

Example

Targets

any\_member

Iterate through all interest group members of a party

any\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

election\_momentum

Compare election momentum of the scoped party against a value

election\_momentum = 0.75

value

has\_party\_member

Checks if the target interest group is a member of scope party

has\_party\_member = <ig>

interest\_group

is\_party

Checks if the target party is same as scoped party. Will only work on exact same party object, meaning you can't compare across countries.

is\_party = <party>

party

is\_party\_type

Checks if the scoped party's type is the specified one

is\_party\_type = party\_type\_database\_key

is\_same\_party\_type

Checks if Party is of the same party type as target

is\_same\_party\_type\_as = scope:neighboring\_party

party

### Political lobby scope\[[edit](/index.php?title=Trigger&veaction=edit&section=23 "Edit section: Political lobby scope") | [edit source](/index.php?title=Trigger&action=edit&section=23 "Edit section: Political lobby scope")\]

List of political lobby triggers

Trigger

Description

Example

Targets

any\_lobby\_member

Iterate through all interest group members of a lobby

any\_lobby\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

appeasement

Compares the appeasement of a political lobby.

value

is\_political\_lobby\_type

Checks political lobby is of a certain lobby type

is\_political\_lobby\_type = political lobby type

lobby\_clout

Compare to total clout of scoped lobby's interest groups

lobby\_clout >= 0.2

value

lobby\_formation\_reason

Check scope diplomatic catalyst or political lobby scope for lobby formation reason

lobby\_formation\_reason = diplomacy

### Political movement scope\[[edit](/index.php?title=Trigger&veaction=edit&section=24 "Edit section: Political movement scope") | [edit source](/index.php?title=Trigger&action=edit&section=24 "Edit section: Political movement scope")\]

List of political movement triggers

Trigger

Description

Example

Targets

any\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

any\_influenced\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

any\_supporting\_character

Iterate through all characters that support the scoped political movement

any\_supporting\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

character

has\_character\_ideology

Check if scoped political movement has character ideology

has\_core\_ideology

Check if scoped political movement has core ideology

has\_ideology

Check if scoped character or interest group has ideology

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

is\_being\_bolstered

Check if scoped movement is being bolstered

is\_being\_bolstered = <yes>/<no>

boolean

is\_being\_suppressed

Check if scoped movement is being suppressed

is\_being\_suppressed = <yes>/<no>

boolean

is\_insurrectionary

Check if country, movement or IG is insurrectionary

boolean

is\_political\_movement\_type

Check if a political movement is a particular type

is\_political\_movement\_type = movement\_to\_enact

is\_revolutionary

Check if the country, movement or interest group is revolutionary

boolean

is\_secessionist

Check if the country, movement or interest group is secessionist

boolean

law\_enactment\_stance

Compares the stance of the scoped character, movement or interest group has about enactment of the specified law compared to the current active law in the same group

law\_stance

Compares the base stance of the scoped character, movement or interest group has about the specified law, ignoring the current active law in the same group

movement\_can\_cause\_obstinance

Check if the political movement can generate obstinance in states

movement\_can\_cause\_obstinance = yes

boolean

movement\_is\_causing\_obstinance

Check if the political movement is causing obstinance in any state

movement\_is\_causing\_obstinance = no

boolean

movement\_pressure

Compares the pressure of the scoped movement on the target IG

"movement\_pressure( ig:ig\_intelligentsia )" > 0.1

value

political\_movement\_identity\_support

Compare pop with correct culture/religion identity support of political movement

political\_movement\_identity\_support > 0.5

value

political\_movement\_military\_support

Compare military support of political movement

political\_movement\_military\_support > 0.5

value

political\_movement\_popular\_support

Compare popular support of political movement

political\_movement\_popular\_support > 0.5

value

political\_movement\_radicalism

Compare radicalism of political movement

political\_movement\_radicalism > 50

value

political\_movement\_support

Compare support of political movement

political\_movement\_support > 0.5

value

political\_movement\_wealth\_support

Compare wealth support of political movement

political\_movement\_wealth\_support > 0.5

value

### Pop scope\[[edit](/index.php?title=Trigger&veaction=edit&section=25 "Edit section: Pop scope") | [edit source](/index.php?title=Trigger&action=edit&section=25 "Edit section: Pop scope")\]

List of pop triggers

Trigger

Description

Example

Targets

dependents

Compares the dependents size of the scoped pop

dependents >= 2000

value

food\_security

Checks if a pop has a certain amount of food security

food\_security = value

value

has\_ongoing\_assimilation

Checks if the scoped pop has ongoing cultural assimilation

has\_ongoing\_assimilation = yes

boolean

has\_ongoing\_conversion

Checks if the scoped pop has ongoing religious conversion

has\_ongoing\_conversion = yes

boolean

has\_pop\_culture

Checks if pop has specific culture

has\_pop\_culture = culture

has\_pop\_religion

Checks if pop has specific religion

has\_pop\_religion = religion

has\_social\_class

Checks if the scoped pop belongs to a specific social class

has\_social\_class = <key>

has\_state\_religion

Check if the Pop has the state religion

boolean

is\_employed

Check if the pop is employed

boolean

is\_in\_mild\_starvation

Check if the pop is in mild starvation

is\_in\_mild\_starvation = yes

boolean

is\_in\_severe\_starvation

Check if the pop is in severe starvation

is\_in\_severe\_starvation = yes

boolean

is\_in\_starvation

Check if the pop is in starvation (mild or severe)

is\_in\_starvation = yes

boolean

is\_pop\_type

Checks if pop is of specified type

is\_pop\_type = poptype

literacy\_rate

Checks if a pop, state or country has a certain amount of literacy

literacy = value

value

pop\_acceptance

Compares the scoped pop's acceptance against an acceptance value

pop\_acceptance > 50

value

pop\_employment\_building

Checks if pop is working in a specific building type

pop\_employment\_building = building

pop\_employment\_building\_group

Checks if pop is working in a specific building type

pop\_employment\_building = building

pop\_has\_primary\_culture

Checks if pop's culture is primary

pop\_has\_primary\_culture = bool

boolean

pop\_loyalist\_fraction

Compares the number of Radicals in a pop to its total size

pop\_loyalty\_fraction < 0.5

value

pop\_neutral\_fraction

Compares the number of Neutrals (not Radical, not Loyalist) in a pop to its total size

pop\_neutral\_fraction > 0.95

value

pop\_radical\_fraction

Compares the number of Radicals in a pop to its total size

pop\_radical\_fraction > 0.25

value

quality\_of\_life

Compares the quality of life of the given pop

quality\_of\_life > 10

value

standard\_of\_living

Compares the standard of living of a given pop

standard\_of\_living > 10

value

strata

Checks the strata of the scoped pop

strata >= upper/middle/lower

value

total\_size

Compares the total size of the scoped pop

total\_size >= 3000

value

wealth

Checks if a pop has a certain amount of wealth

wealth = value

value

workforce

Compares the workforce size of the scoped pop

workforce >= 1000

value

### Power bloc scope\[[edit](/index.php?title=Trigger&veaction=edit&section=26 "Edit section: Power bloc scope") | [edit source](/index.php?title=Trigger&action=edit&section=26 "Edit section: Power bloc scope")\]

List of power bloc triggers

Trigger

Description

Example

Targets

any\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

any\_power\_bloc\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

can\_invite\_any\_country

Checks whether is possible to invite any country to the scoped Power Bloc.

can\_invite\_any\_country = yes/no

boolean

current\_cohesion\_number

Compare the current Cohesion as a numeric value

current\_cohesion\_number > 50

value

current\_cohesion\_percentage

Compare the current Cohesion as a percentage of the maximum of a Power Bloc in scope

current\_cohesion\_percentage > 0.5

value

free\_principle\_slots

Compare a power bloc's free principle slot count

power\_bloc = {
  free\_principle\_slots >= 2
}

value

has\_identity

Checks if the scoped power bloc has certain central identity

has\_identity = identity:key

power\_bloc\_identity

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

has\_principle

Checks if the scoped power bloc has certain principle

has\_principle = principle:key

power\_bloc\_principle

has\_principle\_group

Checks if the scoped power bloc has a principle in the specified group

has\_principle\_group = principle\_group:key

power\_bloc\_principle\_group

leverage\_advantage

Checks that the leverage advantage between power bloc and target country meets condition

power\_bloc = {
 leverage\_advantage = {
  target = c:BEL
  value > 0
 }
}

value

num\_power\_bloc\_members

Compare the number of members in a power bloc against a value

num\_power\_bloc\_members >= 2

value

num\_power\_bloc\_states

Compare the number of states in a power bloc against a value

num\_power\_bloc\_states >= 2

value

power\_bloc\_rank

Compare a power bloc's rank

power\_bloc = {
  power\_bloc\_rank <= 2
}

value

power\_bloc\_total\_leading\_goods\_producers

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

power\_bloc\_total\_leading\_goods\_producers > 15

value

power\_bloc\_total\_leading\_goods\_producers\_with

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking, plus one additional country not currently in the power bloc. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

"power\_bloc\_total\_leading\_goods\_producers\_with(scope:target) > 15"

value

power\_bloc\_total\_leading\_goods\_producers\_without

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking, minus one country that is currently in the power bloc. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

"power\_bloc\_total\_leading\_goods\_producers\_without(scope:target) > 15"

value

power\_bloc\_worst\_economic\_dependence

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

value

power\_bloc\_worst\_economic\_dependence\_with

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

value

power\_bloc\_worst\_economic\_dependence\_without

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

value

power\_bloc\_worst\_infamy

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

value

power\_bloc\_worst\_infamy\_with

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

value

power\_bloc\_worst\_infamy\_without

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

value

power\_bloc\_worst\_leader\_relations

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

value

power\_bloc\_worst\_leader\_relations\_with

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

value

power\_bloc\_worst\_leader\_relations\_without

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

value

power\_bloc\_worst\_leader\_religion\_population\_fraction

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

value

power\_bloc\_worst\_leader\_religion\_population\_fraction\_with

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

value

power\_bloc\_worst\_leader\_religion\_population\_fraction\_without

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

value

power\_bloc\_worst\_liberty\_desire

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

value

power\_bloc\_worst\_liberty\_desire\_with

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

value

power\_bloc\_worst\_liberty\_desire\_without

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type\_with

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type\_without

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

value

predicted\_cohesion\_percentage\_with

Predicts the cohesion of a Power Bloc (as a fraction of 1) if it included target country as a member

predicted\_cohesion\_percentage\_with, = {
 target = scope:other\_country
 value > 0.9
}

value

target\_cohesion\_number

Compare the target Cohesion as a numeric value

target\_cohesion\_number > 50

value

target\_cohesion\_percentage

Compare the target Cohesion as a percentage of the maximum of a Power Bloc in scope

target\_cohesion\_percentage > 0.5

value

total\_used\_principle\_levels

Compare a power bloc's total number of active principle levels

power\_bloc = {
 total\_used\_principle\_levels >= 10
}

value

used\_principle\_slots

Compare a power bloc's used principle slot count

power\_bloc = {
  used\_principle\_slots >= 2
}

value

### State scope\[[edit](/index.php?title=Trigger&veaction=edit&section=27 "Edit section: State scope") | [edit source](/index.php?title=Trigger&action=edit&section=27 "Edit section: State scope")\]

List of state triggers

Trigger

Description

Example

Targets

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

harvest\_condition

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_scope\_building

Iterate through all buildings in a: state, country

any\_scope\_building = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

building

any\_scope\_culture

Iterate through all cultures in the scope

any\_scope\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

culture

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

pop

any\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

any\_sea\_node\_adjacent\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

arable\_land

Check arable land in state

arable\_land > 10

value

available\_jobs

Checks the state's number of available jobs in non-subsistence buildings

available\_jobs > 10000

value

blockade\_level

Compare to state's blockade level

blockade\_level > 0.1

value

can\_activate\_production\_method

Checks if the building of a particular type in scoped state is able to active the specified production method

can\_activate\_production\_method = {
 building\_type = <key>
 production\_method = <key>
}

can\_construct\_building

Checks if 1 level of <building\_type> can be constructed in a scoped state

can\_construct\_building = <building\_type>

check\_area

Compares areas of object to another object

cultural\_acceptance\_delta

Compare the current local delta (modification) to a culture's acceptance in the scoped state

cultural\_acceptance\_delta = {
 target = cu:romanian
 value < -10
}

value

culture\_percent\_state

Checks that a state's population has a certain percentage of a specific culture

scope:example\_state = {
 culture\_percent\_state = {
  target = cu:dixie
  value >= 0.2
 }
}

value

devastation

Compares the devastation of a given state

devastation > 5

value

free\_arable\_land

Check free arable land in state

free\_arable\_land > 0

value

has\_active\_building

True if a state has an active building type

has\_active\_building = building

has\_assimilating\_pops

Check if a state has any pops currently in the process of assimilating.

boolean

has\_building

True if a state/market/state region/country has a building type

has\_building = building

has\_claim\_by

Checks if a state is claimed by a country

any\_state = {
 owner = root
 has\_claim\_by = c:MEX
}

country

has\_converting\_pops

Check if a state has any pops currently in the process of converting.

boolean

has\_cultural\_community

Checks if a culture has a cultural community in the scoped state.

has\_cultural\_community = <culture>

has\_decree

Checks if scope state has a particular type of decree

has\_decree = <key>

has\_famine

Check if state or country has famine

has\_famine = yes/no

boolean

has\_mobilizing\_unit

Checks if any Building in a scoped State maintains any Combat Units that are currently mobilizing

has\_mobilizing\_unit = bool

boolean

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

has\_port

Check if state has at least one port

has\_port = yes/no

boolean

has\_potential\_resource

Checks if the specificed building group is allowed in the scoped state. Used to check if a state can potentially produce a resource

has\_potential\_resource = bg\_rubber\_plantations

building\_group

has\_state\_trait

Checks if scoped state has a certain trait

has\_state\_trait = key

ig\_state\_pol\_strength\_share

True if IG in scope has scripted political strength in state

ig\_state\_pol\_strength\_share = {
 target = scope:target\_ig
 value = 0.3
}

value

incorporation\_progress

Check incorporation progress in state

incorporation\_progress > 0.25

value

infrastructure

Compares the infrastructure value of a given state

infrastructure > 10

value

infrastructure\_delta

Compares the infrastructure balance of a given state

infrastructure\_delta > 0

value

infrastructure\_usage

Compares the infrastructure usage value of a given state

infrastructure\_usage > 10

value

is\_blockaded\_by

Checks if a state is blockaded by a country

country

is\_capital

Check if state is the capital of the owner = bool

boolean

is\_coastal

Check if state borders a (non-impassable) sea region

is\_coastal = yes/no

boolean

is\_homeland\_of\_country\_cultures

Checks if state is homeland of any of the target country's primary cultures

is\_homeland\_of\_country\_cultures = <country>

country

is\_in\_revolt

Check if a state has any chance to split off into a revolutionary or seceding country

is\_in\_revolt = yes/no.

boolean

is\_incorporated

Check if state is incorporated = bool

boolean

is\_isolated\_from\_market

Check if a state is isolated from its market

boolean

is\_largest\_state\_in\_region

Check if state is the largest in the state region = bool

boolean

is\_mass\_migration\_target

Mass migration target is state.

any\_state = { owner = ROOT is\_mass\_migration\_target = yes }

boolean

is\_mass\_migration\_target\_for\_culture

Scoped state is a mass migration target for the specified culture.

is\_mass\_migration\_target\_for\_culture = scope:my\_culture

culture

is\_potential\_treaty\_port

Checks if the scoped state has the potential to become a treaty port for target country

is\_potential\_treaty\_port = c:FRA

country

is\_production\_method\_active

Checks if the building of a particular type in scoped state has the specified production method active

is\_production\_method\_active = {
 building\_type = <key>
 production\_method = <key>
}

is\_sea\_adjacent

Check if state borders a sea region (regular or impassable)

is\_sea\_adjacent = yes/no

boolean

is\_slave\_state

Check if a state employs or has the potential to employ slaves.

boolean

is\_split\_state

Checks if the scoped state is a split state.

scope:example\_state = { is\_split\_state = yes }

boolean

is\_strategic\_objective

Checks if the scoped State is a Strategic Objective of a Country

is\_strategic\_objective = <country>

country

is\_target\_of\_wargoal

Checks if state is target of any wargoal in wars involving a specific country

has\_war\_goal = <country>

country

is\_treaty\_port

Checks if the scoped state is a treaty port

is\_treaty\_port = yes/no

boolean

is\_under\_colonization

Check if state is under colonization

is\_under\_colonization = yes/no

boolean

is\_world\_market\_hub

Check if state is a world market hub

is\_world\_market\_hub = yes

boolean

literacy\_rate

Checks if a pop, state or country has a certain amount of literacy

literacy = value

value

loyalist\_fraction

Compares loyalist fraction in pops in state or country, all parameters except value are optional

loyalist\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

value

loyalty

Compares the loyalty in a given state, i.e. the fraction of Loyalists

loyalty > 0.2

value

market\_access

Checks the market access of the scoped state

market\_access > 0.8

value

most\_prominent\_revolting\_interest\_group

Checks if the most prominent revolting interest group in the scoped state has the given interest group type. Evaluates false if the scoped state is not in revolt.

most\_prominent\_revolting\_interest\_group = ig\_landowners

num\_cultural\_communities

Check cultural communities in state

num\_cultural\_communities > 5

value

obstinance

Compares state obstinance

obstinance > 0.5

value

pollution\_generation

Compare total pollution generation across all buildings in the state

value

pop\_type\_percent\_state

Checks that a state's population has a certain percentage of a specific pop type

scope:example\_state = {
 pop\_type\_percent\_state = {
  target = pop\_type:laborer
  value <= 0.8
 }
}

value

population\_by\_culture

Compare the current population of a target culture in the scoped state

population\_by\_culture = {
 target = cu:romanian
 value > 2500
}

value

primary\_cultures\_percent\_state

Checks that a state's population has a certain percentage of the country's primary cultures

scope:example\_state = {
 primary\_cultures\_percent\_state >= 0.2
}

value

radical\_fraction

Compares radical fraction in pops in state or country, all parameters except value are optional

radical\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

value

relative\_infrastructure

Compares the infrastructure to infrastructure usage of a state

relative\_infrastructure > 1.1

value

religion\_percent\_state

Checks that a state's population has a certain percentage of a specific religion

scope:example\_state = {
 religion\_percent\_state = {
  target = rel:catholic
  value <= 0.8
 }
}

value

state\_cultural\_acceptance

Checks how accepted a pop of the target culture is in the scoped state

 state\_cultural\_acceptance = {
 target = cu:french
 value >= 50

value

state\_exports

Compare total units of exports from scope to target state (if set) or world state in general (if no state specified)

state\_exports = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

value

state\_has\_building\_group\_levels

Checks the sum of building levels for a building group in a state

 state\_has\_building\_group\_levels = {
 type = bg\_heavy\_industry
 value >= 100
}

value

state\_has\_building\_levels

Checks the sum of building levels for a state

state\_has\_building\_levels >= 10

value

state\_has\_building\_type\_levels

Checks the sum of building levels for a building type in a state

 state\_has\_building\_type\_levels = {
 target = bt:building\_barracks
 value >= 100
}

value

state\_has\_goods\_shortage

Check if state has a shortage on any of its building inputs

state\_has\_goods\_shortage = yes/no

boolean

state\_has\_national\_awakening

Checks if the scoped state is in a state region with an active national awakening map marker

state\_has\_national\_awakening = yes/no

boolean

state\_imports

Compare total units of imports from target to scope state

state\_imports = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

value

state\_is\_eligible\_as\_mass\_migration\_target

Check if state can receive a mass migration at all.

any\_state = { state\_is\_eligible\_as\_mass\_migration\_target = yes }

boolean

state\_population

Checks the total population of the scoped state

state\_population <= 250000

value

state\_religious\_acceptance

Checks how accepted a pop of the given religion is in the the scoped state

 state\_religious\_acceptance = {
 target = rel:catholic
 value >= 50

value

state\_trade

Compare total units of goods traded between scope state and target state (if set) or world state in general (if no state specified)

state\_trade = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

value

state\_unemployment\_rate

Checks the unemployment rate (percentage) in the scoped state

state\_unemployment\_rate > 0.1

value

tax\_capacity

Checks the taxation capacity of the scoped state

tax\_capacity > 50

value

tax\_capacity\_usage

Checks the taxation capacity usage of the scoped state

tax\_capacity\_usage > 50

value

total\_urbanization

Compares the total urbanization of a given state

total\_urbanization > 5

value

turmoil

Compares the turmoil in a given state, i.e. the fraction of Radicals

turmoil > 0.2

value

world\_market\_access

Checks the world market access of the scoped state

world\_market\_access > 0.8

value

### State goods scope\[[edit](/index.php?title=Trigger&veaction=edit&section=28 "Edit section: State goods scope") | [edit source](/index.php?title=Trigger&action=edit&section=28 "Edit section: State goods scope")\]

List of state goods triggers

Trigger

Description

Example

Targets

export\_advantage

Compare to state goods export advantage

export\_advantage >= 100

value

export\_tariff\_level

Checks if state goods has a particular export tariff level set

export\_tariff\_level = max\_tariffs

has\_potential\_supply

Check if the market goods or state goods has a potential supply, either though local production or theoretical import

boolean

import\_advantage

Compare to state goods import advantage

import\_advantage >= 100

value

import\_tariff\_level

Checks if state goods has a particular import tariff level set

import\_tariff\_level = max\_tariffs

relative\_export\_advantage

Compare to state goods relative export advantage

relative\_export\_advantage > 0.1

value

relative\_import\_advantage

Compare to state goodsrelative import advantage

relative\_import\_advantage > 0.1

value

state\_goods\_cheaper

Checks if state goods is at least the specified percentage cheaper than base price

state\_goods\_cheaper > <value>

value

state\_goods\_consumption

Checks if state goods has the specified number of total consumption

state\_goods\_consumption < <value>

value

state\_goods\_delta

Checks if state has the specified goods delta (production - consumption)

state\_goods\_delta < <value>

value

state\_goods\_has\_local\_goods\_shortage

Check if state goods has a shortage in a state, but NOT in the whole market

state\_goods\_has\_local\_goods\_shortage = yes/no

boolean

state\_goods\_pricier

Checks if state goods is at least the specified percentage more expensive than base price

state\_goods\_pricier > <value>

value

state\_goods\_production

Checks if state goods has the specified number of total production

state\_goods\_production < <value>

value

### State region scope\[[edit](/index.php?title=Trigger&veaction=edit&section=29 "Edit section: State region scope") | [edit source](/index.php?title=Trigger&action=edit&section=29 "Edit section: State region scope")\]

List of state region triggers

Trigger

Description

Example

Targets

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

harvest\_condition

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

any\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, strategic region)

any\_scope\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

any\_scope\_interest\_marker

Iterate through all interest markers in a: country, strategic region

any\_scope\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_marker

any\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

any\_scope\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

check\_area

Compares areas of object to another object

contains\_capital\_of

Checks if scoped state region contains the capital of target tag

contains\_capital\_of = country scope/tag

has\_building

True if a state/market/state region/country has a building type

has\_building = building

has\_harvest\_condition

Check if the scoped state region has a harvest condition of type

has\_harvest\_condition = drought

infrastructure

Compares the infrastructure value of a given state

infrastructure > 10

value

infrastructure\_delta

Compares the infrastructure balance of a given state

infrastructure\_delta > 0

value

infrastructure\_usage

Compares the infrastructure usage value of a given state

infrastructure\_usage > 10

value

is\_homeland

Checks if scoped state region is a homeland of target culture

is\_homeland = culture scope/name

is\_state\_region\_land

Check if the state region is on land

boolean

pollution\_amount

Compare state region pollution

value

remaining\_undepleted

Check remaining amount of resource, like gold mines in a state

remaining\_undepleted = {
 type = bg\_gold\_mining
 amount > 1
}

value

### Treaty scope\[[edit](/index.php?title=Trigger&veaction=edit&section=30 "Edit section: Treaty scope") | [edit source](/index.php?title=Trigger&action=edit&section=30 "Edit section: Treaty scope")\]

List of treaty triggers

Trigger

Description

Example

Targets

any\_scope\_article

Iterate through articles of the scoped treaty

any\_scope\_article = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty\_article

any\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

any\_scope\_article\_option = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty\_article\_options

binds

Checks if the scoped treaty or treaty\_options binds the given country

scope:some\_treaty = {
 if = {
  limit = {
   binds = c:FRA
   binds = c:BEL
  }
 }
}

country

is\_enforced

Checks if the scoped treaty is enforced on either party

scope:some\_treaty = {
 if = {
  limit = { is\_enforced = yes }
 }
}

boolean

is\_equal\_exchange

Checks if the scoped treaty has the same articles on both sides

scope:some\_treaty = {
 if = {
  limit = { is\_equal\_exchange = yes }
 }
}

boolean

is\_fulfilled\_by

Check if the scoped treaty is fulfilled by the given country

scope:treaty = {
 is\_fulfilled\_by = c:FRA
}

country

is\_renegotiation

Checks if the scoped treaty is amending an existing treaty

scope:some\_treaty = {
 if = {
  limit = { is\_renegotiation = yes }
 }
}

boolean

### War scope\[[edit](/index.php?title=Trigger&veaction=edit&section=31 "Edit section: War scope") | [edit source](/index.php?title=Trigger&action=edit&section=31 "Edit section: War scope")\]

List of war triggers

Trigger

Description

Example

Targets

any\_scope\_front

Iterate through all Fronts related to the scoped War

any\_scope\_front = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

front

any\_war\_participant

Iterate through all participants in a war

any\_war\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

has\_war\_exhaustion

Checks the war exhaustion of the target country in the scoped war

has\_war\_exhaustion = {
 target = c:GBR
 value > 50
}

value

has\_war\_goal

Checks if war has a certain war goal type

has\_war\_goal = return\_state

has\_war\_support

Checks the war support of the target country in the scoped war

has\_war\_support = {
 target = c:GBR
 value > 50
}

value

is\_holder\_of\_wargoal\_in\_war

Checks if the specified country is the holder of any wargoal in the scoped war

is\_holder\_of\_wargoal\_in\_war = <country>

country

is\_war\_participant

Check if the target country is participant in a war

country

is\_warleader

Check if country is warleader in war

country

num\_casualties

Checks the number of total casualties in the scoped war

num\_casualties >= 5000

value

num\_country\_casualties

Checks the number of casualties for the target country in the scoped war

num\_country\_casualties = {
 target = country
 value < 5000
}

value

num\_country\_dead

Checks the number of dead for the target country in the scoped war

num\_country\_dead = {
 target = country
 value < 5000
}

value

num\_country\_wounded

Checks the number of wounded for the target country in the scoped war

num\_country\_wounded = {
 target = country
 value < 5000
}

value

num\_dead

Checks the number of total dead in the scoped war

num\_dead >= 5000

value

num\_wounded

Checks the number of total wounded in the scoped war

num\_wounded >= 5000

value

war\_exhaustion\_from\_acceptance\_of\_dead

Determines the war exhaustion a country gets from their degree of cultural acceptance of manpower killed in the war, regardless of what side they were on

war\_exhaustion\_from\_acceptance\_of\_dead = {
 target = root
 value >= 0.4
}

value

war\_has\_active\_peace\_deal

True if the war has a proposed peace deal

war\_has\_active\_peace\_deal = bool

boolean

## All triggers\[[edit](/index.php?title=Trigger&veaction=edit&section=32 "Edit section: All triggers") | [edit source](/index.php?title=Trigger&action=edit&section=32 "Edit section: All triggers")\]

Use this table to search for all triggers.

List of triggers

Trigger

Description

Example

Scopes

Targets

active\_lens

Checks if the specified lens is open

active\_lens = lens

none

active\_lens\_option

Checks if the specified lens option is activated

active\_lens\_option = lens\_option\_key

none

add\_to\_temporary\_list

Saves a temporary target for use during the trigger execution

This is used to build lists in triggers.
If used within an any-trigger, placement within the trigger is quite important.
The game will iterate through every instance of the any-trigger
until it finds a single instance that fulfills the requirements, and then it will stop.
In order to add every instance of a scope that fulfills certain conditions,
use "count = all" while also placing this "effect" at the very end of the any-trigger
so that every condition is evaluated for every iteration.

none

additional\_war\_exhaustion

Compares the additional war exhaustion the scoped country has accumulated from scripted events in the target diplomatic play

additional\_war\_exhaustion = {
 target = scope:war.diplomatic\_play
 value > 1.0
}

country

value

age

Compares the character age

age > 20

character

value

aggressive\_diplomatic\_plays\_permitted

True if country is independent or permitted to start their own Diplomatic Plays

aggressive\_diplomatic\_plays\_permitted = bool

country

boolean

all\_false

true if all contained triggers are false (equivalent to NOR)

none

always

checks if the assigned yes/no value is true

always = yes # always succeeds
always = no # always fails
always = scope:a\_boolean\_value # evaluated at runtime

none

boolean

and

true if all contained triggers are true

Returns false if at first false sub-trigger

none

any\_active\_law

Iterate through all active laws in a country

any\_active\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_active\_party

Iterate through all active political parties in a country

any\_active\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_character

Iterate through all characters globally

any\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_exile\_pool

Iterate through characters in the exile pool

any\_character\_in\_exile\_pool = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_void

Iterate through characters in the void

any\_character\_in\_void = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_civil\_war

Iterate through all civil wars related to the scoped country

any\_civil\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

civil\_war

any\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

any\_cobelligerent\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

any\_cobelligerent\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_combat\_unit

Iterate through all combat units of input scope

any\_combat\_unit = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

any\_company

Iterate through all companies in a country

any\_company = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

company

any\_country

Iterate through all countries globally

any\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_decentralized\_country

Iterate through all countries that are decentralized

any\_decentralized\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

any\_diplomatic\_catalyst = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_catalyst

any\_diplomatic\_play

Iterate through all diplomatic plays globally

any\_diplomatic\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

diplomatic\_play

any\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

any\_diplomatically\_relevant\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_direct\_subject

Any country directly below current in hierarchy

any\_direct\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

any\_enemy\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

any\_enemy\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_false

true if any contained trigger is false (equivalent to NAND)

none

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

harvest\_condition

any\_in\_global\_list

Iterate through all items in global list.

any\_in\_global\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_hierarchy

Any country in current hierarchy, including current

any\_in\_hierarchy = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_in\_list

Iterate through all items in list.

any\_in\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_local\_list

Iterate through all items in local list.

any\_in\_local\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

any\_influenced\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

interest\_group

any\_interest\_group

Iterate through all interest groups in a country

any\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_group

any\_law

Iterate through all laws in a country

any\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_lobby\_member

Iterate through all interest group members of a lobby

any\_lobby\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_lobby

interest\_group

any\_market

Iterate through all markets globally

any\_market = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

market

any\_market\_goods

Iterate through all active (market) goods in a market

any\_market\_goods = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market

market\_goods

any\_member

Iterate through all interest group members of a party

any\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

party

interest\_group

any\_military\_formation

Iterate through all military formations currently present at input scope

any\_military\_formation = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, hq

military\_formation

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

state

any\_overlord\_or\_above

Any country above current in hierarchy

any\_overlord\_or\_above = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_participant

Any of two participants of the diplomatic pact in a scope

any\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_pact

country

any\_political\_lobby

Iterate through all political lobbies in a country or interest group

any\_political\_lobby = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, interest\_group

political\_lobby

any\_political\_movement

Iterate through all political movements in a country

any\_political\_movement = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

political\_movement

any\_potential\_party

Iterate through all potential political parties in a country

any\_potential\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_power\_bloc

Iterate through all power blocs

any\_power\_bloc = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

power\_bloc

any\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

any\_power\_bloc\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

power\_bloc

country

any\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

any\_preferred\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

law

any\_primary\_culture

Primary cultures of the scoped country or country definition

any\_primary\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, country\_definition

culture

any\_province

Iterate through all Provinces in the scoped State

any\_province = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

province

state

any\_rival\_country

Any country that is being rivaled by the country in a scope

any\_rival\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_rivaling\_country

Any country that is rivaling the country in a scope

any\_rivaling\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_ally

Iterate through all allies to a: country

any\_scope\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_article

Iterate through articles of the scoped treaty

any\_scope\_article = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty

treaty\_article

any\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

any\_scope\_article\_option = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty, treaty\_options

treaty\_article\_options

any\_scope\_building

Iterate through all buildings in a: state, country

any\_scope\_building = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

building

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, strategic region)

any\_scope\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market, state\_region, strategic\_region

country

any\_scope\_culture

Iterate through all cultures in the scope

any\_scope\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

culture

any\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

any\_scope\_diplomatic\_pact = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_pact

any\_scope\_front

Iterate through all Fronts related to the scoped War

any\_scope\_front = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

front

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

any\_scope\_held\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_marker

any\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

any\_scope\_initiator\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_interest\_marker

Iterate through all interest markers in a: country, strategic region

any\_scope\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state\_region, strategic\_region

interest\_marker

any\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

any\_scope\_play\_involved = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, culture, interest\_group, state

pop

any\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

any\_scope\_regional\_hqs = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

company

building

any\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

any\_scope\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, state\_region, strategic\_region, theater

state

any\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

any\_scope\_target\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_theater

Iterate through all theaters in a: country

any\_scope\_theater = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

theater

any\_scope\_treaty

Iterate through in force treaties binding the scoped country

any\_scope\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

treaty

any\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

any\_scope\_violate\_sovereignty\_interested\_parties = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

any\_scope\_violate\_sovereignty\_wars = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_scope\_war

Iterate through all wars related to the scope

any\_scope\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

any\_sea\_node\_adjacent\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

state

any\_state

Iterate through all states globally

any\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state

any\_state\_region

Iterate through all state regions

any\_state\_region = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state\_region

any\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

any\_strategic\_objective = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

state

any\_subject\_of\_subject

Any country below direct subjects in hierarchy

any\_subject\_of\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_subject\_or\_below

Any country below current in hierarchy

any\_subject\_or\_below = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_supporting\_character

Iterate through all characters that support the scoped political movement

any\_supporting\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

character

any\_treaty

Iterate through treaties (both in force and drafts)

any\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

treaty

any\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

any\_valid\_mass\_migration\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

culture

any\_war\_participant

Iterate through all participants in a war

any\_war\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

country

appeasement

Compares the appeasement of a political lobby.

political\_lobby

value

approaching\_bureaucracy\_shortage

Check if Institutions in the country will incur a Bureaucracy shortage eventually

country

boolean

arable\_land

Check arable land in state

arable\_land > 10

state

value

arable\_land\_country

Compare arable land in \*all\* states

arable\_land\_country > 10

country

value

army\_mobilization\_option\_fraction

Checks that a countries army has a certain percentage of units with a specific monbilization option

scope:country = {
 army\_mobilization\_option\_fraction = {
 target = mobilization\_option:key
 value >= 0.2
 }
}

country

value

army\_power\_projection

Compare to a country's total army power projection

 scope:example\_country = { army\_power\_projection < root.army\_power\_projection }

country

value

assert\_if

Conditionally cause an assert during run time

assert\_if = {
 limit = { <triggers> }
 text = <string> (optional)
}

none

assert\_read

Conditionally cause an assert during read time

assert\_read = yes / <string>

none

authority

Compares the available authority of the scoped country

country

value

authority\_usage

Compares the consumed authority of the scoped country

country

value

available\_jobs

Checks the state's number of available jobs in non-subsistence buildings

available\_jobs > 10000

state

value

average\_country\_infrastructure

Check average infrastructure in all states owned by scope country

average\_country\_infrastructure = 3

country

value

average\_incorporated\_country\_infrastructure

Check average infrastructure in incorporated states owned by the scope country

average\_incorporated\_country\_infrastructure = 3

country

value

average\_sol\_for\_culture

Compares the average standard of living for the target culture in the country

average\_sol\_for\_culture = {
 target = cu:polish
 value > 10
}

country

value

average\_sol\_for\_primary\_cultures

Compare average standard of living for primary cultures

country

value

average\_sol\_for\_religion

Compares the average standard of living for the target religion in the country

average\_sol\_for\_religion = {
 target = rel:catholic
 value > 10
}

country

value

average\_sol\_for\_slaves

Compare average standard of living for enslaved pops

country

value

binds

Checks if the scoped treaty or treaty\_options binds the given country

scope:some\_treaty = {
 if = {
  limit = {
   binds = c:FRA
   binds = c:BEL
  }
 }
}

treaty, treaty\_options

country

blockade\_level

Compare to state's blockade level

blockade\_level > 0.1

state

value

building\_has\_goods\_shortage

Check if building has a shortage of any of its inputs

building\_has\_goods\_shortage = yes/no

building

boolean

bureaucracy

Compares the available bureaucracy of the scoped country

country

value

bureaucracy\_usage

Compares the consumed bureaucracy of the scoped country

country

value

calc\_true\_if

Returns true if the specified number of sub-triggers return true

calc\_true\_if = {
 amount = 2
 <trigger>
 <trigger>
 <trigger>
}

none

can\_activate\_production\_method

Checks if the building of a particular type in scoped state is able to active the specified production method

can\_activate\_production\_method = {
 building\_type = <key>
 production\_method = <key>
}

state

can\_add\_wargoal\_against

Checks if scope country can add any wargoal against target country in an active diplomatic play

can\_add\_wargoal\_against = <scope>

country

country

can\_afford\_diplomatic\_action

Checks if the country in scope can afford the Influence for the specified diplomatic action (pact or ongoing)

can\_afford\_diplomatic\_action = {
 target = <country scope>
 type = <diplomatic action key>
}

country

can\_agitate

Check if the scope character can agitate the target country

can\_agitate = <scope/c:TAG>

character

country

can\_be\_enacted

Checks if a law could be enacted by its country, considering its current situation

can\_be\_enacted = yes

law

boolean

can\_break\_diplomatic\_pact

Checks if there is a diplomatic pact of the specified type with target country that can be broken by scope country

can\_break\_diplomatic\_pact = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

country

can\_construct\_building

Checks if 1 level of <building\_type> can be constructed in a scoped state

can\_construct\_building = <building\_type>

state

can\_create\_diplomatic\_pact

Checks if a diplomatic pact is valid to create with another country

can\_create\_diplomatic\_pact = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

country

can\_create\_treaty

Checks if a treaty between countries can be created

can\_create\_treaty = {
 first\_country = <country>   # (default: none) must \_not\_ be specified if the created treaty is an amendment
 second\_country = <country>   # (default: none) must \_not\_ be specified if the created treaty is an amendment
 amends = <treaty>     # (default: none) if specified, the created treaty will be an amendment to the specified treaty

 is\_draft = yes | no     # (default: yes) must be yes if this treaty is an amendement to an existing treaty
 entered\_into\_force\_on = <date>  # (default: none) means "today" if not specified (for non-drafts). Must \_not\_ be specified for drafts. Cannot be in the future
 binding\_period = {    # (default: 0) must be a nonnegative number, fractional parts will be ignored (truncation)
  days = <nonnegative number>
  | weeks = <nonnegative number>
  | months = <nonnegative number>
  | years = <nonnegative number>
 }

 articles\_to\_create = {   # (default: empty) non-amendments must have at least one article to create
  {
   article = <key>    # (default: empty) the type of article (determined by script)
   source\_country = <country> # (default: none) directed articles \_must\_ specify this value, mutual articles \_must not\_ specify this value
   target\_country = <country> # (default: none) directed articles \_must\_ specify this value, mutual articles \_must not\_ specify this value

   inputs = {    # (default: empty) list of inputs values as required by the article
    {
     quantity = <positive number>
     | good = <good>
     | state = <state>
     | strategic\_region = <strategic region>
     | company = <company> # TODO: or company type?
     | building = <building> # TODO: or building type?
     | law = <law> # TODO: or law type?
     | country = <country>
    }

    { ... }     # one input per required input of the corresponding article type
   }
  }

  # some examples of valid articles
  { article = alliance }   # mutual, no inputs
  {
   article = resource\_transfer # directed
   source\_country = c:FRA  # because directed
   target\_country = c:GBR   # because directed

   inputs = {
    { quantity = 250 }  # because resource\_transfer has required\_inputs = { quantity good }
    { good = g:coal }  # because resource\_transfer has required\_inputs = { quantity good }
   }
  }
 }
}

none

can\_decrease\_autonomy

Check if a subject country is able to become a less autonomous subject type

can\_increase\_autonomy = yes

country

boolean

can\_establish\_company

Check if the country can establish a new company

country

boolean

can\_form\_nation

Check if the target country is able to potentially form a nation

can\_form\_nation = <tag>

country

can\_have\_as\_subject

Checks if a country can have another country as a particular type of subject

can\_have\_as\_subject = {
 who = <country scope>
 type = <subject type key>
}

country

can\_have\_declared\_interest\_here

Check if the target country scope fulfills the rules for conditions for having a declared interest in the strategic region in scope. Does not check for availability of declared interests.

can\_have\_declared\_interest\_here = c:SWE

strategic\_region

country

can\_have\_subjects

Check if the country is able to have subjects of its own

country

boolean

can\_increase\_autonomy

Check if a subject country is able to become a more autonomous subject type

can\_increase\_autonomy = yes

country

boolean

can\_invite\_any\_country

Checks whether is possible to invite any country to the scoped Power Bloc.

can\_invite\_any\_country = yes/no

power\_bloc

boolean

can\_leave\_power\_bloc

Checks if the country in scope can leave its current Power Bloc (returns false if country is not in a Power Bloc)

can\_leave\_power\_bloc = yes

country

boolean

can\_lobbies\_target

Checks if target country is a valid target for lobbies in the scoped country

can\_lobbies\_target = <target country scope>

country

country

can\_own\_autonomy\_be\_decreased

Check if scoped country can have it's autonomy decreased

can\_own\_autonomy\_be\_decreased = yes/no

country

boolean

can\_potentially\_produce\_prestige\_goods

Checks if scope company can potentially produce target prestige good type

can\_potentially\_produce\_prestige\_good = pg:prestige\_good\_generic\_clothes

company

can\_queue\_building\_levels

Checks if the building's owner could queue the provided number of additional levels without hitting a level or resource potential cap

can\_queue\_building\_levels = number

building

can\_research

True if a country can research an technology

can\_research = technology

country

can\_send\_diplomatic\_action

Checks if a diplomatic action is valid to send by scope country to target country

can\_send\_diplomatic\_action = {
  target = <country scope>
  type = <action type key>
  first\_state = <state scope, optional>
  second\_state = <state scope, optional>
}

country

can\_start\_tutorial\_lesson

Can the specified tutorial lesson be started?

can\_start\_tutorial\_lesson = reactive\_advice\_succession

none

can\_take\_on\_scaled\_debt

Checks if scoped country can take on a certain amount of scaled debt from another country

can\_take\_on\_scaled\_debt = {
 who = <country scope>
 value = <0-1 value>
}

country

can\_trigger\_event

Check if country can trigger the specified event

can\_trigger\_event = key

country

cash\_reserves\_available

Evaluates a production building's available cash reserves

cash\_reserves\_available > 25000

building

value

cash\_reserves\_ratio

Evaluates a production building's available cash reserve ratio compared to its maximum

Returns 1 if the building has no maximum cash reserves

cash\_reserves\_ratio > 0.2

building

value

character\_acceptance

Compares the acceptance of the scoped character in the target country against an acceptance value

character\_acceptance = {
 target = c:FRA
 value > 50
}

character

value

character\_supports\_political\_movement

Checks whether the scoped character supports a political movement

character\_supports\_political\_movement = yes/no

character

boolean

check\_area

Compares areas of object to another object

country, market, province, state, state\_region, strategic\_region, theater

civil\_war\_progress

Compare progress of civil war

civil\_war\_progress > 0.50

civil\_war

value

commander\_is\_available

Check if a commander is not busy

commander\_is\_available = bool

character

boolean

commander\_rank

Compares the commanders rank

commander\_rank > 2

character

value

company\_employed\_levels

Check the number of employed building levels of the scoped company

company\_employed\_levels > 1.5

company

value

company\_global\_productivity\_comparison

Check the average global productivity of buildings matching this company's buildings, scaled by owned levels for each type

company\_global\_productivity\_comparison > 1.0

company

value

company\_has\_building\_type\_monopoly

Check if the scoped company has a monopoly of the specified building type

company\_has\_building\_type\_monopoly = bt:building\_food\_industry

company

building\_type

company\_has\_monopoly

Check if the scoped company has any monopoly

 company\_has\_monopoly = yes

company

boolean

company\_is\_prosperous

Check if a company is considered to be prosperous

company

boolean

company\_owned\_levels

Check the number of owned building levels of the scoped company

company\_owned\_levels > 1.5

company

value

company\_productivity

Check the productivity of the scoped company

company\_productivity > 1.0

company

value

company\_prosperity

Check the prosperity of a company

company\_prosperity > 0

company

value

construction\_queue\_duration

Compares the maximum of all the very roughly approximated weeks remaining to finish the constructions in any queue

construction\_queue\_duration < 52

country

value

construction\_queue\_government\_duration

Compares the very roughly approximated weeks remaining to finish the constructions in the government queue

construction\_queue\_government\_duration < 52

country

value

construction\_queue\_num\_queued\_government\_levels

Compares the number of government constructed building levels in the construction queue

country

value

construction\_queue\_num\_queued\_levels

Compares the number of building levels in the construction queue

country

value

construction\_queue\_num\_queued\_private\_levels

Compares the number of privately constructed building levels in the construction queue

country

value

construction\_queue\_private\_duration

Compares the very roughly approximated weeks remaining to finish the constructions in the private queue

construction\_queue\_private\_duration < 52

country

value

contains\_capital\_of

Checks if scoped state region contains the capital of target tag

contains\_capital\_of = country scope/tag

state\_region

could\_support\_political\_movement

Check if a character could potentially support a political movement

could\_support\_political\_movement = <scope>

character

country\_army\_unit\_type\_fraction

Checks that a country has a certain percentage of a specific army unit type

scope:example\_formation = {
 country\_military\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

country

value

country\_average\_cultural\_acceptance

Compares the average acceptance of a culture that is present in a country against a value

country\_average\_cultural\_acceptance = {
 target = cu:french
 value >= 50
}

country

value

country\_average\_religious\_acceptance

Compares the average acceptance of a religion that is present in a country against a value

country\_average\_religious\_acceptance = {
 target = rel:catholic
 value >= 50
}

country

value

country\_can\_have\_mass\_migration\_to

Checks if the scoped country can have mass migration to target country

can\_have\_mass\_migration\_to = c:country

country

country

country\_definition\_has\_culture

Checks if a culture is one of the cultures of the country definition

country\_definition\_has\_culture = <culture>

country\_definition

culture

country\_fervor\_primary\_culture

Compares with the hightest fervor of a country's primary cultures

country\_fervor\_primary\_culture > 0.2

country

value

country\_has\_building\_group\_levels

Checks the sum of building levels for a building group in a country

 country\_has\_building\_group\_levels = {
 type = bg\_heavy\_industry
 value >= 100
}

country

value

country\_has\_building\_levels

Checks the sum of building levels for a country

country\_has\_building\_levels >= 10

country

value

country\_has\_building\_type\_levels

Checks the sum of building levels for a building type in a country

 country\_has\_building\_type\_levels = {
 target = bt:building\_barracks
 value >= 100
}

country

value

country\_has\_building\_type\_monopoly

Check if the scoped country has a monopoly of the specified building type

country\_has\_building\_type\_monopoly = bt:building\_food\_industry

country

building\_type

country\_has\_country\_monopoly

Check if the scoped country has any country monopolies

country

boolean

country\_has\_local\_shortage

Whether the scoped market goods are in shortage in the target country

country\_has\_local\_shortage = scope:example\_country

market\_goods

country

country\_has\_primary\_culture

Checks if a culture is one of the primary cultures in the country

country\_has\_primary\_culture = <culture>

country

culture

country\_has\_state\_religion

Checks if a religion is the state religion in the country

country\_has\_accepted\_religion = <religion>

country

religion

country\_innovation

Checks the amount of weekly innovation a country produces

country\_innovation <= 200

country

value

country\_navy\_unit\_type\_fraction

Checks that a country has a certain percentage of a specific navy unit type

scope:example\_formation = {
 country\_navy\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

country

value

country\_or\_subject\_owns\_entire\_state\_region

Checks whether the scoped country or any of its subjects owns the entire specified state region

country\_or\_subject\_owns\_entire\_state\_region = key

country

country\_ownership\_fraction

Compares the fraction of this building's levels that are owned by a country

 country\_ownership\_fraction > 0.5

building

value

country\_rank

Compares a Country's Power Ranking

country\_rank < rank\_value:major\_power

country

value

country\_tier

Compare tier of country tag

country\_tier = x

country

country\_turmoil

Compares the country's population weighted turmoil

country\_turmoil > 0.2

country

value

cultural\_acceptance\_base

Compares the acceptance from shared cultural traits of a culture in the scoped country against an acceptence value

cultural\_acceptance\_base = {
 target = cu:french
 value > 50
}

country

value

cultural\_acceptance\_delta

Compare the current local delta (modification) to a culture's acceptance in the scoped state

cultural\_acceptance\_delta = {
 target = cu:romanian
 value < -10
}

state

value

culture\_can\_have\_mass\_migration\_to\_country

Checks if the scoped culture can have mass migration to target country

can\_have\_mass\_migration\_to\_country = c:country

culture

country

culture\_current\_fervor

Compares the current fervor for a given culture

culture\_current\_fervor > 0.2

culture

value

culture\_has\_community\_in\_state

Checks if the scoped culture has a cultural community in a state.

culture\_has\_community\_in\_state = <state>

culture

culture\_has\_national\_awakening

Checks if the scoped culture has an active national awakening

culture\_has\_national\_awakening = yes / no

culture

boolean

culture\_national\_awakening\_occurred

Checks if the scoped culture has ever had a national awakening started

culture\_national\_awakening\_occurred = yes / no

culture

boolean

culture\_percent\_country

Checks that a country's population has a certain percentage of a specific culture

scope:example\_country = {
 culture\_percent\_country = {
  target = cu:dixie
  value >= 0.2
 }
}

country

value

culture\_percent\_state

Checks that a state's population has a certain percentage of a specific culture

scope:example\_state = {
 culture\_percent\_state = {
  target = cu:dixie
  value >= 0.2
 }
}

state

value

culture\_secession\_progress

Checks the culture's progress percentage towards secession in a country. 0 if no secession movement is active for the culture.

culture\_secession\_progress = {
 target = scope:example\_country
 value > 0.5
}

culture

value

culture\_target\_fervor

Compares the target fervor for a given culture

culture\_target\_fervor > 0.2

culture

value

current\_cohesion\_number

Compare the current Cohesion as a numeric value

current\_cohesion\_number > 50

power\_bloc

value

current\_cohesion\_percentage

Compare the current Cohesion as a percentage of the maximum of a Power Bloc in scope

current\_cohesion\_percentage > 0.5

power\_bloc

value

current\_manpower

Compares the current manpower of a battle side

manpower > 10000

battle\_side

value

current\_tooltip\_depth

Returns the number of tooltips currently open on screen

none

value

custom\_description

Wraps triggers that get a custom description instead of the auto-generated one

custom\_description = {
 text = <trigger\_localization\_key>
 subject = <optional subject scope> #defaults to current scope
 object = <optional object scope>
 value = <optional script value>
 ... triggers ...
}

none

custom\_tooltip

Replaces the tooltips for the enclosed triggers with a custom text

custom\_tooltip = {
 text = <text>
 subject = <scope> (optional)
 <trigger>
}

none

day\_value

Day value

none

value

daynight\_value

DayNight value

none

value

debug\_log

Log whether the parent trigger succeeded or failed

none

debug\_log\_details

Log whether the parent trigger succeeded or failed. Log which children succeeded or failed

none

dependents

Compares the dependents size of the scoped pop

dependents >= 2000

pop

value

devastation

Compares the devastation of a given state

devastation > 5

state

value

discriminates\_religion

Checks if the scoped country discriminates the given religion (key)

discriminates\_religion = <religion key/scope>

country

religion

earnings

Compare a building's current annual earnings per employee

earnings >= 10

building

value

economic\_dependence

Compares the degree of dependence the country in scope has to the target country. **Note: This is an expensive trigger**

economic\_dependence = {
 target = scope:other\_country
 value > 2.2
}

country

value

election\_momentum

Compare election momentum of the scoped party against a value

election\_momentum = 0.75

party

value

empty\_agitator\_slots

Checks number of empty agitator slots in a country

empty\_agitator\_slots >= 1

country

value

enacting\_any\_law

Checks if the country is enacting any law.

enacting\_any\_law = bool

country

boolean

enactment\_chance

Compares the current enactment chance in scope country, including values from enactment modifiers

country

value

enactment\_chance\_for\_law

Compares the enactment chance in scope country for given law, including values from enactment modifiers

enactment\_chance\_for\_law = {
 target = scope:my\_lawtype
 value > 0.2
}

country

value

enactment\_chance\_for\_law\_without\_enactment\_modifier

Compares the enactment chance in scope country for given law, excluding values from enactment modifiers

enactment\_chance\_for\_law\_without\_enactment\_modifier = {
 target = scope:my\_lawtype
 value > 0.2
}

country

value

enactment\_chance\_without\_enactment\_modifier

Compares the current enactment chance in scope country, excluding values from enactment modifiers

country

value

enactment\_phase

Compares the current law enactment phase in scope country.

country

value

enactment\_setback\_count

Compares the current enactment setback count in scope country.

country

value

enemy\_contested\_wargoals

Determines the fraction of wargoals that enemies in the war are currently contesting

enemy\_contested\_wargoals = {
 target = scope:war
 value > 0.4
}

country

value

enemy\_occupation

Determines the (weighted) enemy occupation score of a country

enemy\_occupation > 0.2

country

value

escalation

Checks whether escalation has passed a certain threshold

escalation > -58

diplomatic\_play

value

exists

Checks whether the specified scope target exists (check for not being the null object)

exists = from.owner.var:cool\_var.mother

none

expanding\_institution

Checks if the institution is expanding

expanding\_institution = <scope/key>

country

experience\_level

Compares the character experience level

experience\_level > 1

character

value

export\_advantage

Compare to state goods export advantage

export\_advantage >= 100

state\_goods

value

export\_tariff\_level

Checks if state goods has a particular export tariff level set

export\_tariff\_level = max\_tariffs

state\_goods

food\_security

Checks if a pop has a certain amount of food security

food\_security = value

pop

value

formation\_army\_unit\_type\_fraction

Checks that a formation has a certain percentage of a specific army unit type

scope:example\_formation = {
 country\_military\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

military\_formation

value

formation\_navy\_unit\_type\_fraction

Checks that a formation has a certain percentage of a specific navy unit type

scope:example\_formation = {
 formation\_navy\_unit\_type\_fraction = {
  target = unit\_type:key
  value >= 0.2
 }
}

military\_formation

value

fraction\_of\_levels\_owned\_by\_country

Compares the fraction of total levels of a building a country or investors in that country owns

fraction\_of\_levels\_owned\_by\_country = {
 target = c:GBR
 value > 0.5
}

building

value

free\_arable\_land

Check free arable land in state

free\_arable\_land > 0

state

value

free\_principle\_slots

Compare a power bloc's free principle slot count

power\_bloc = {
  free\_principle\_slots >= 2
}

power\_bloc

value

game\_date

Compare to current game date

game\_date = 1837.1.1

none

value

gdp\_ownership\_ratio

Compares the ratio of GDP the specified country owns in the scoped country

gdp\_ownership\_ratio = {
 target = scope:owner\_country
 value > 0.1
}

country

value

gdp\_per\_capita\_ranking

Compares a Country's GDP per Capita Ranking (position)

gdp\_per\_capita\_ranking > 42

country

value

global\_country\_ranking

Compares a Country's Power Ranking (position)

global\_country\_ranking > 42

country

value

global\_population

Compares the global population

global\_population > 2000000000

none

value

global\_variable\_list\_size

Checks the size of a global variable list

global\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

gold\_reserves

Does the country have the required gold reserves

gold\_reserves > 500

country

value

gold\_reserves\_limit

Compares the country's gold reserves limit

gold\_reserves > gold\_reserves\_limit

country

value

goods\_production\_rank

Compare to a country's production leaderboard rank of a good

"goods\_production\_rank(g:luxury\_clothes)" < 10

country

value

government\_legitimacy

Compare Legitimacy

country

value

government\_transfer\_of\_power

Checks country's government's transfer of power

government\_transfer\_of\_power = key

country

value

government\_wage\_level

Compares the government wage level of scoped country. `very_low, low, medium, high, very_high`; returns same number as government\_wage\_level\_value trigger

government\_wage\_level = <level\_term>

country

value

government\_wage\_level\_value

Compares the government wage level value of scoped country. `0, 1, 2, 3, 4`

government\_wage\_level\_value = <level\_num>

country

value

harvest\_condition\_intensity

With a harvest condition scope, compare the intensity of the harvest condition in a given country (since the state region can span multiple countries)

harvest\_condition\_intensity = {
 target = scope:my\_country\_scope
 value > 0.5
}

harvest\_condition

value

has\_account\_item

Does the player have the item in the account

has\_account\_item = <key>

none

has\_active\_building

True if a state has an active building type

has\_active\_building = building

market, state

has\_active\_peace\_deal

True if the country is in a war where there is a proposed peace deal

has\_active\_peace\_deal = bool

country

boolean

has\_active\_production\_method

Checks if a scoped building has the specified production method active

has\_active\_production\_method = key

building

has\_any\_law\_commitment

Checks if a country has a commitment to enact any law

has\_any\_law\_commitment = yes

country

boolean

has\_any\_secessionists\_broken\_out

Check if the country has secessionists broken out

country

boolean

has\_any\_secessionists\_growing

Check if the country has any secessionists growing

country

boolean

has\_any\_subventions\_on

Check if the scoped country has any level of subventions on a goods

scope:country = {
 has\_any\_subventions\_on = g:grain
}

country

goods

has\_any\_tariffs\_on

Check if the scoped country has any level of tariffs on a goods

scope:country = {
 has\_any\_tariffs\_on = g:grain
}

country

goods

has\_assimilating\_pops

Check if a state has any pops currently in the process of assimilating.

state

boolean

has\_attitude

Checks if scoped country has a particular attitude towards another country

has\_attitude = {
 who = <country scope>
 attitude = <attitude key>
}

country

has\_battle\_condition

True if the battle side currently has a condition with the given key

has\_battle\_condition = battle\_condition\_key

battle\_side

has\_building

True if a state/market/state region/country has a building type

has\_building = building

country, market, state, state\_region

has\_character\_ideology

Check if scoped political movement has character ideology

political\_movement

has\_civil\_war\_from\_movement\_type

Checks if a country is having a civil war started by a particular movement type

has\_civil\_war\_from\_movement\_type = law\_type:law\_autocracy

country

political\_movement\_type

has\_claim

Checks if country in scope has a claim on state/state region/country

has\_claim = <scope>

country

has\_claim\_by

Checks if a state is claimed by a country

any\_state = {
 owner = root
 has\_claim\_by = c:MEX
}

state

country

has\_commander\_order

Checks whether the scoped character is following the given order

has\_commander\_order = order type key/order type scope

character

has\_company

Checks if a company of the specified type exists in scope country

has\_company = company\_type:company\_rheinmetall

country

company\_type

has\_completed\_subgoal

Checks if the scoped country has completed a certain subgoal

has\_completed\_subgoal = subgoal\_key

country

has\_consumption\_tax

Checks if the country is taxing the target good.

c:FRA = { has\_consumption\_tax = g:grain }

country

goods

has\_converting\_pops

Check if a state has any pops currently in the process of converting.

state

boolean

has\_convoys\_being\_sunk

Check if the country has convoys being sunk through convoy raiding

country

boolean

has\_core\_ideology

Check if scoped political movement has core ideology

political\_movement

has\_cosmetic\_dlc

Does the client have this cosmetic DLC

none

has\_cultural\_community

Checks if a culture has a cultural community in the scoped state.

has\_cultural\_community = <culture>

state

has\_cultural\_obsession

Checks if a culture has a certain goods as obsession

has\_cultural\_obsession = <goods key>

culture

has\_culture

Checks characters culture

has\_culture = culture scope or character scope

character

has\_culture\_graphics

Checks if a culture has a certain culture\_graphics

has\_culture\_graphics = <culture graphics key>

culture

has\_decreasing\_interests

Checks if the country has decreasing interest levels

country

boolean

has\_decree

Checks if scope state has a particular type of decree

has\_decree = <key>

state

has\_deployed\_units

Check if a building supports any units which have been deployed outside of home HQ

has\_deployed\_units = yes/no

building

boolean

has\_diplomatic\_pact

Checks if two countries have an active diplomatic pact of type

has\_diplomatic\_pact = {
 who = <country scope>
 type = <diplomatic action key>
 is\_initiator = yes/no
}
is\_initiator is optional parameter that checks to see if scope country is the original initiator/target of the pact

country

has\_diplomatic\_play

Check if strategic region has a diplomatic play or not

strategic\_region

boolean

has\_diplomatic\_relevance

Checks if target country is diplomatically relevant for scope country

has\_diplomatic\_relevance = <scope>

country

country

has\_diplomats\_expelled

Checks if country in scope has recently expelled diplomats of event target

has\_diplomats\_expelled = country

country

country

has\_discrimination\_trait

Checks if scoped culture or religion has the given discrimination trait

has\_trait = trait

culture, religion

has\_dlc\_feature

Does the host have DLC that enables this particular feature

none

has\_employee\_slots\_filled

Checks whether the amount of employees of a certain poptype are above or below a given percentage of the total amount the building can currently hire. I.e. if a building is at 80% (given as 0.8) of its current hiring capacity for Shopkeepers, for example.

has\_employee\_slots\_filled = {
 pop\_type = <pop type>
 percent = <0-1 value>
}

building

has\_failed\_hires

Checks if a building failed to hire someone last week

has\_failed\_hires = yes/no

building

boolean

has\_famine

Check if state or country has famine

has\_famine = yes/no

country, state

boolean

has\_free\_government\_reform

Check if the country has a free (of radicals) government reform

has\_free\_government\_reform = <yes/no>

country

boolean

has\_game\_rule

Is the given game rule setting enabled?

has\_game\_rule = faster\_conversion

none

has\_game\_started

True if game has started

has\_game\_started = bool

none

boolean

has\_gameplay\_dlc

Does the host have this gameplay DLC

none

has\_global\_highest\_gdp

Checks if the scoped country has the highest GDP

has\_global\_highest\_gdp = yes/no

country

boolean

has\_global\_highest\_innovation

Checks if the scoped country has the highest weekly innovation

has\_global\_highest\_innovation = yes/no

country

boolean

has\_global\_variable

Checks whether the current scope has the specified global variable set

has\_global\_variable = name

none

has\_global\_variable\_list

Checks whether the current scope has the specified global variable list set

has\_global\_variable\_list = name

none

has\_government\_clout

Does the country's government have the necessary total Clout

country

value

has\_government\_type

Is the country's government type as specified

has\_government\_type = key

country

has\_harvest\_condition

Check if the scoped state region has a harvest condition of type

has\_harvest\_condition = drought

state\_region

has\_healthy\_economy

Check if the country has a healthy economy

country

boolean

has\_high\_attrition

Checks if a Military Formation's attrition risk is higher than the base value for their type

has\_high\_attrition = bool

military\_formation

boolean

has\_homeland

Checks if scoped culture has a homeland in target state or state region

has\_homeland = state/state region

culture

has\_identity

Checks if the scoped power bloc has certain central identity

has\_identity = identity:key

power\_bloc

power\_bloc\_identity

has\_ideology

Check if scoped character or interest group has ideology

character, interest\_group, political\_movement

has\_inactive\_journal\_entry

Check if the country has at least one inactive journal entry of the specified type

has\_inactive\_journal\_entry = key

country

has\_institution

Checks if scope country has a particular type of institution

has\_institution = <scope/key>

country

has\_insurrectionary\_interest\_groups

Check if the country has Interest Groups that are insurrectionary

country

boolean

has\_interest\_marker\_in\_region

True if scope country has an interest marker in target region

has\_interest\_marker\_in\_region = region scope/tag

country

has\_journal\_entry

Check if the country has at least one active journal entry of the specified type

has\_journal\_entry = key

country

has\_label

Check if the scope object has the specified label

has\_label = label\_key

province

has\_law

Checks if a country has a certain law active

country

law\_type

has\_law\_commitment

Checks if a country has a commitment to enact a certain law

country

law\_type

has\_law\_imposition\_rights

Checks if the scoped country has the necessary permits to demand another country enacts a certain law

has\_law\_imposition\_rights = {
 target\_country = scope:target\_country
 type = law\_type:law\_serfdom
}

country

has\_law\_or\_variant

Checks if a country has a certain law or one its variant laws active

country

law\_type

has\_local\_variable

Checks whether the current scope has the specified local variable set

has\_local\_variable = name

none

has\_local\_variable\_list

Checks whether the current scope has the specified local variable list set

has\_local\_variable\_list = name

none

has\_map\_interaction

Checks if the map interaction type is active

has\_map\_interaction = key

none

has\_map\_interaction\_diplomatic\_action

Checks if our current map interaction is a specific diplomatic action

has\_map\_interaction\_diplomatic\_action = <key>

none

has\_military\_formation

Checks if character has a Military Formation

has\_military\_formation = bool

character

boolean

has\_mobilization\_option

Checks that a formation has a specific mobilization option

has\_mobilization\_option = mobilization\_option:key

military\_formation

mobilization\_option

has\_mobilizing\_unit

Checks if any Building in a scoped State maintains any Combat Units that are currently mobilizing

has\_mobilizing\_unit = bool

state

boolean

has\_modifier

Check if a supported scope has a certain timed modifier

has\_modifier = <key>

building, character, country, institution, interest\_group, journal\_entry, political\_movement, power\_bloc, state

has\_objective

Checks if the scoped country has a certain objective type

has\_objective = objective\_type\_key

country

has\_ongoing\_assimilation

Checks if the scoped pop has ongoing cultural assimilation

has\_ongoing\_assimilation = yes

pop

boolean

has\_ongoing\_conversion

Checks if the scoped pop has ongoing religious conversion

has\_ongoing\_conversion = yes

pop

boolean

has\_overlapping\_interests

Checks if country in scope has an overlapping interest marker with any of target country's interests

has\_overlapping\_interests = <scope>

country

country

has\_party

True if IG scope has a party

has\_party = bool

interest\_group

boolean

has\_party\_member

Checks if the target interest group is a member of scope party

has\_party\_member = <ig>

party

interest\_group

has\_play\_goal

Checks if diplomatic play has a certain war goal type

has\_play\_goal = return\_state

diplomatic\_play

has\_pop\_culture

Checks if pop has specific culture

has\_pop\_culture = culture

pop

has\_pop\_religion

Checks if pop has specific religion

has\_pop\_religion = religion

pop

has\_port

Check if state has at least one port

has\_port = yes/no

country, market, state

boolean

has\_possible\_decisions

Check if a country has any possible Decisions

has\_possible\_decisions = yes

country

boolean

has\_potential\_resource

Checks if the specificed building group is allowed in the scoped state. Used to check if a state can potentially produce a resource

has\_potential\_resource = bg\_rubber\_plantations

state

building\_group

has\_potential\_supply

Check if the market goods or state goods has a potential supply, either though local production or theoretical import

market\_goods, state\_goods

boolean

has\_potential\_to\_form\_country

Check if the target country could ever be able to form a nation

has\_potential\_to\_form\_country = <tag>

country

has\_power\_struggle

Checks if scope Power Bloc has a power struggle

has\_power\_struggle = yes/no

country

boolean

has\_principle

Checks if the scoped power bloc has certain principle

has\_principle = principle:key

power\_bloc

power\_bloc\_principle

has\_principle\_group

Checks if the scoped power bloc has a principle in the specified group

has\_principle\_group = principle\_group:key

power\_bloc

power\_bloc\_principle\_group

has\_reached\_end\_date

True if the end date (NDefines::NGame::END\_DATE) has been reached

has\_reached\_end\_date = yes

none

boolean

has\_religion

Checks characters religion

has\_religion = religion/character scope

character

has\_religious\_taboo

Checks if a religion has a certain goods as taboo

has\_religious\_taboo = <goods key>

religion

has\_researchable\_technology

Check if the country has any researchable technology left.

country

boolean

has\_revolution

Check if the country has revolutionary uprising

country

boolean

has\_role

Checks if character has the specified role

has\_role = role

character

has\_ruling\_interest\_group

Does the country's government include the named IG

country

has\_ruling\_interest\_group\_count

Does the country's government consist of the specified number of IGs

country

value

has\_secret\_goal

Checks if scoped country has a particular secret goal towards another country

has\_secret\_goal = {
 who = <country scope>
 secret\_goal = <secret\_goal key>
}

country

has\_social\_class

Checks if the scoped pop belongs to a specific social class

has\_social\_class = <key>

pop

has\_social\_hierarchy

Checks if the scoped country has adopted a specific social hierarchy

has\_social\_hierarchy = <key>

country

has\_state\_in\_state\_region

Check if country has a state in the state region

has\_state\_in\_state\_region = key

country

has\_state\_religion

Check if the Pop has the state religion

pop

boolean

has\_state\_trait

Checks if scoped state has a certain trait

has\_state\_trait = key

state

has\_strategic\_adjacency

Checks if country in scope has a strategic adjacency (direct/coastal/wargoal adjacency) to target state/country

has\_strategic\_adjacency = <scope>

country

has\_strategic\_land\_adjacency

Checks if country in scope has a strategic adjacency (direct land border or wargoal adjacency only) to target state/country

has\_strategic\_adjacency = <scope>

country

has\_strategy

Checks if country in scope has a particular AI strategy

has\_strategy = <key>

country

has\_subject\_relation\_with

Checks if country in scope is subject or overlord of event target

has\_subject\_relation\_with = country

country

country

has\_sufficient\_construction\_capacity\_for\_investment

Check if country has enough construction capacity to be spending all of its incoming investment pool funds.

country

boolean

has\_technology\_progress

Does the country have the required progress for an technology

has\_technology\_progress = {
 technology = <technology key>
 progress = <0-1 value>
}

country

value

has\_technology\_researched

True if a country has researched an technology

has\_technology\_researched = <scope/key>

country

has\_template

Checks if character was made from the specified template

has\_template = template\_key

character

has\_terrain

Check if the province has the specified terrain type

has\_terrain = terrain\_key

province

has\_trait

Checks if character has specific trait

has\_trait = trait

character

has\_treaty\_port\_in\_market

Checks if the scoped country has a treaty port in target market

c:POR = { has\_treaty\_port\_in\_market = c:CHI.market }

country

market

has\_truce\_with

Check if a country has a truce with a different target country

has\_truce\_with = c:GBR

country

country

has\_type

Checks if the scoped object has the type identified by the given string

scope:some\_object = {
 has\_type = defensive\_pact
}

treaty\_article, treaty\_article\_options

has\_unification\_candidate

Check if there is at least one unification candidate for country tag

has\_country\_unification\_candidate = GER

none

has\_unit\_type

Checks if a Combat Unit is of the specified type

has\_unit\_type = unit\_type:key

new\_combat\_unit

combat\_unit\_type

has\_variable

Checks whether the current scope has the specified variable set

has\_variable = name

none

has\_variable\_list

Checks whether the current scope has the specified variable list set

has\_variable\_list = name

none

has\_war\_exhaustion

Checks the war exhaustion of the target country in the scoped war

has\_war\_exhaustion = {
 target = c:GBR
 value > 50
}

war

value

has\_war\_goal

Checks if war has a certain war goal type

has\_war\_goal = return\_state

war

has\_war\_support

Checks the war support of the target country in the scoped war

has\_war\_support = {
 target = c:GBR
 value > 50
}

war

value

has\_war\_with

Checks if country in scope is at war with event target

has\_war\_with = country

country

country

has\_wasted\_construction

Check if country is wasting any of its produced construction.

country

boolean

hidden\_trigger

Enclosed triggers are not shown in tooltips

hidden\_trigger = { <more triggers> }

none

highest\_secession\_progress

Compares the highest secession progress of any secession movement in a given country

highest\_secession\_progress > 0.7

country

value

ig\_approval

Compare to scoped interest group approval

Usages: ig\_approval > 2, ig\_approval < happy

interest\_group

value

ig\_clout

Compare to scoped interest group's clout

ig\_clout >= 0.2

interest\_group

value

ig\_government\_power\_share

Compare to scoped interest group's political strength divided by total government political strength

ig\_government\_power\_share >= 0.5

interest\_group

value

ig\_state\_pol\_strength\_share

True if IG in scope has scripted political strength in state

ig\_state\_pol\_strength\_share = {
 target = scope:target\_ig
 value = 0.3
}

state

value

import\_advantage

Compare to state goods import advantage

import\_advantage >= 100

state\_goods

value

import\_tariff\_level

Checks if state goods has a particular import tariff level set

import\_tariff\_level = max\_tariffs

state\_goods

in\_default

Check if the country is currently in default

country

boolean

in\_election\_campaign

Check if the country is in election campaign period

in\_election\_campaign = <yes/no>

country

boolean

income\_transfer

Compares the base income transfer of the diplomatic pact

income\_transfer > 0.05

diplomatic\_pact

value

incorporation\_progress

Check incorporation progress in state

incorporation\_progress > 0.25

state

value

influence

Compares the available influence of the scoped country

country

value

influence\_usage

Compares the consumed influence of the scoped country

country

value

infrastructure

Compares the infrastructure value of a given state

infrastructure > 10

state, state\_region

value

infrastructure\_delta

Compares the infrastructure balance of a given state

infrastructure\_delta > 0

state, state\_region

value

infrastructure\_usage

Compares the infrastructure usage value of a given state

infrastructure\_usage > 10

state, state\_region

value

initiator\_is

Checks who the initiator of a diplomatic play is

initiator\_is = country

diplomatic\_play

country

institution\_investment\_level

Compares the level of investment in an institution

institution\_investment\_level = {
 institution = <key>
 value = <comparator value>
}

country

value

interest\_group\_population

Compares population number in an interest group

interest\_group\_population > 1000

interest\_group

value

interest\_group\_population\_percentage

Compares percentage of population in an interest group

interest\_group\_population\_percentage > 0.2

interest\_group

value

interest\_group\_supports\_political\_movement

Checks whether the scoped interest group supports a political movement

interest\_group\_supports\_political\_movement = yes/no

interest\_group

boolean

investment\_pool

Does the country have this amount of money saved in its investment pool

investment\_pool > 1000

country

value

investment\_pool\_gross\_income

Does the country have this amount of gross income (income before expenses) for its investment pool

investment\_pool\_gross\_income > 1000

country

value

investment\_pool\_net\_income

Does the country have this amount of net income (income after expenses) for its investment pool

investment\_pool\_net\_income > 1000

country

value

is\_adjacent\_to\_country

Checks if country in scope is adjacent to a target country

is\_adjacent\_to\_country = <scope>

country

country

is\_adjacent\_to\_state

Checks if country in scope is adjacent to a target state

is\_adjacent\_to\_state = <scope>

country

state

is\_advancing\_on\_front

Checks if a commander is advancing on a front

is\_advancing\_on\_front = front

character

front

is\_ai

True if country scope is controlled by an AI

is\_ai = bool

country

boolean

is\_army

Checks if a military formation is Army

is\_army = bool

military\_formation

boolean

is\_at\_war

Check if the country is at war

country

boolean

is\_attacker\_in\_battle

Checks if a Commander is attacker in a battle

is\_attacker\_in\_battle = bool

character

boolean

is\_banning\_goods

Check if a country is banning a good

is\_banning\_goods = <goods key/scope>

country

is\_being\_bolstered

Check if scoped movement is being bolstered

is\_being\_bolstered = <yes>/<no>

political\_movement

boolean

is\_being\_suppressed

Check if scoped movement is being suppressed

is\_being\_suppressed = <yes>/<no>

political\_movement

boolean

is\_blockaded\_by

Checks if a state is blockaded by a country

state

country

is\_buildable

Check if a building is buildable = yes (default)

is\_buildable = yes/no

building

boolean

is\_building\_group

True if scope is a building of given group

building\_group = building

building

is\_building\_type

True if scope is a building of given type

is\_building\_type = building

building

is\_building\_type\_expanded

Checks if the CProductionMethodsPanelEntry for a particular CBuildingType is expanded

is\_building\_type\_expanded = <scope/key>

none

is\_busy

Check if character is busy

is\_busy = bool

character

boolean

is\_capital

Check if state is the capital of the owner = bool

state

boolean

is\_character\_alive

Checks if the scoped character is alive

is\_character\_alive = bool

character

boolean

is\_civil\_war\_type

Check if the scoped civil war is of a specific type

is\_civil\_war\_type = revolution / secession

civil\_war

is\_coastal

Check if state borders a (non-impassable) sea region

is\_coastal = yes/no

state

boolean

is\_company\_type

Checks if scoped company is of the specified type

is\_company\_type = company\_type:company\_rheinmetall

company

company\_type

is\_considered\_adjacent\_due\_to\_wargoals

Checks if the scoped country is considered adjacent to the target country by virtue of the scoped country having wargoals that are adjacent to the target country in a committed diplomatic play

is\_considered\_adjacent\_due\_to\_wargoals = <target country scope>

country

country

is\_construction\_paused

Check if construction in a state is paused.

country

boolean

is\_consumed\_by\_government\_buildings

Check if the market goods is instrumental in running the bureaucratic machine

market\_goods

boolean

is\_consumed\_by\_military\_buildings

Check if the goods is instrumental in running the war machine

market\_goods

boolean

is\_country\_alive

Checks if the scoped country is alive, i.e. if it has at least one state on the map and can be interacted with

is\_country\_alive = yes/no

country

boolean

is\_country\_type

Checks the countrys type

is\_country\_type = country type

country

is\_defender\_in\_battle

Checks if a Commander is defender in a battle

is\_defender\_in\_battle = bool

character

boolean

is\_diplomatic\_action\_type

Checks diplomatic pact is of a certain action type

is\_diplomatic\_action\_type = diplomatic action type

diplomatic\_pact

is\_diplomatic\_catalyst\_type

Checks diplomatic catalyst is of a certain catalyst type

is\_diplomatic\_catalyst\_type = diplomatic catalyst type

diplomatic\_catalyst

is\_diplomatic\_pact\_in\_danger

Checks if diplomatic pact is in danger of breaking

is\_diplomatic\_pact\_in\_danger = yes

diplomatic\_pact

boolean

is\_diplomatic\_play\_ally\_of

Checks if country in scope is in a diplomatic play together with event target

is\_diplomatic\_play\_ally\_of = country

country

country

is\_diplomatic\_play\_committed\_participant

True if country is a committed participant of any diplomatic play

is\_diplomatic\_play\_participant = bool

country

boolean

is\_diplomatic\_play\_enemy\_of

Checks if country in scope is in a diplomatic play against event target

is\_diplomatic\_play\_enemy\_of = country

country

country

is\_diplomatic\_play\_initiator

True if country is the initiator of any diplomatic play

is\_diplomatic\_play\_initiator = bool

country

boolean

is\_diplomatic\_play\_involved\_with

Checks if country in scope is involved in the same diplomatic play as event target

is\_diplomatic\_play\_involved\_with = country

country

country

is\_diplomatic\_play\_participant\_with

Checks if country in scope is a committed participant in the same diplomatic play as event target

is\_diplomatic\_play\_participant\_with = country

country

country

is\_diplomatic\_play\_target

True if country is the target of any diplomatic play

is\_diplomatic\_play\_target = bool

country

boolean

is\_diplomatic\_play\_type

Checks diplomatic play is of a certain type

is\_diplomatic\_play\_type = play type

diplomatic\_play

is\_diplomatic\_play\_undecided\_participant

True if country is a undecided participant of any diplomatic play

is\_diplomatic\_play\_participant = bool

country

boolean

is\_direct\_subject\_of

Checks if country in scope is a direct subject (not subject-of-subject) of event target

is\_direct\_subject\_of = country

country

country

is\_employed

Check if the pop is employed

pop

boolean

is\_enacting\_law

Checks if the scoped country is enacting a specific law type.

is\_enacting\_law = law\_type:law\_conscription

country

law\_type

is\_enforced

Checks if the scoped treaty is enforced on either party

scope:some\_treaty = {
 if = {
  limit = { is\_enforced = yes }
 }
}

treaty

boolean

is\_equal\_exchange

Checks if the scoped treaty has the same articles on both sides

scope:some\_treaty = {
 if = {
  limit = { is\_equal\_exchange = yes }
 }
}

treaty, treaty\_options

boolean

is\_expanding\_institution

Are you expanding an institution

is\_expanding\_institution = <yes>/<no>

country

boolean

is\_female

Check if character is female

is\_female = bool

character

boolean

is\_fleet

Checks if a military formation is Fleet

is\_fleet = bool

military\_formation

boolean

is\_forced\_pact

Check if a diplomatic pact has a forced duration due to reasons such as a sway or obligation

diplomatic\_pact

boolean

is\_forced\_to\_join\_plays

Checks if the scoped country is forced to join the target country's Diplomatic Plays

is\_forced\_to\_join\_plays = c:country

country

country

is\_fulfilled\_by

Check if the scoped treaty is fulfilled by the given country

scope:treaty = {
 is\_fulfilled\_by = c:FRA
}

treaty

country

is\_fully\_mobilized

Checks if the Military Formation is fully mobilized

is\_fully\_mobilized = bool

military\_formation

boolean

is\_game\_paused

Checks if the game is paused

is\_game\_paused = yes/no

none

boolean

is\_gamestate\_tutorial\_active

Is the gamestate tutorial active? See save\_progress\_in\_gamestate in tutorial\_lesson\_chains documentation.

none

boolean

is\_goal\_complete

Check if the journal entry's goal has been met

is\_goal\_complete = yes/no

journal\_entry

boolean

is\_government\_funded

Check if a building is is\_government\_funded

is\_government\_funded = yes/no

building

boolean

is\_heir

Checks whether the scoped character is an heir

is\_heir = yes / no

character

boolean

is\_historical

Check if character is historical

is\_historical = yes/no

character

boolean

is\_holder\_of\_wargoal\_in\_war

Checks if the specified country is the holder of any wargoal in the scoped war

is\_holder\_of\_wargoal\_in\_war = <country>

war

country

is\_home\_country\_for

Checks if a country is the home country for target country

country

country

is\_homeland

Checks if scoped state region is a homeland of target culture

is\_homeland = culture scope/name

state\_region

is\_homeland\_of\_country\_cultures

Checks if state is homeland of any of the target country's primary cultures

is\_homeland\_of\_country\_cultures = <country>

state

country

is\_immortal

checks if the scoped character is immortal

scope:abbath = { is\_immortal = yes }

character

boolean

is\_immune\_to\_revolutions

Checks if the country has been set to be immune to revolutions via set\_immune\_to\_revolutions

Warning: This does not check if the country is naturally immune to revolutions due to for example being a revolutionary country itself, only for the effects of set\_immune\_to\_revolutions.

is\_immune\_to\_revolutions = yes/no

country

boolean

is\_in\_battle

Checks if a Commander is engaged in battle

is\_in\_battle = bool

character

boolean

is\_in\_customs\_union

Check if the country is part of a customs union

country

boolean

is\_in\_customs\_union\_with

Check if the country is in a customs union with scoped country

is\_in\_customs\_union\_with = c:FRA

country

country

is\_in\_exile\_pool

Checks whether the scoped character is in the exile pool

is\_in\_exile\_pool = yes/no

character

boolean

is\_in\_government

True if IG scope is in the government

is\_in\_government = bool

interest\_group

boolean

is\_in\_list

Checks if a target in in a list

none

is\_in\_mild\_starvation

Check if the pop is in mild starvation

is\_in\_mild\_starvation = yes

pop

boolean

is\_in\_power\_bloc

Checks if the country is in a Power Bloc

is\_in\_power\_bloc = yes/no

country

boolean

is\_in\_revolt

Check if a state has any chance to split off into a revolutionary or seceding country

is\_in\_revolt = yes/no.

state

boolean

is\_in\_same\_power\_bloc

Checks if the scoped country is in the same power bloc as the target scoped country

is\_in\_same\_power\_bloc = c:GBR

country

country

is\_in\_severe\_starvation

Check if the pop is in severe starvation

is\_in\_severe\_starvation = yes

pop

boolean

is\_in\_starvation

Check if the pop is in starvation (mild or severe)

is\_in\_starvation = yes

pop

boolean

is\_in\_void

Check if character is in the void

is\_in\_void = bool

character

boolean

is\_in\_war\_together

Checks if country in scope is in war on the same side as event target

is\_in\_war\_together = country

country

country

is\_incorporated

Check if state is incorporated = bool

state

boolean

is\_indirect\_subject\_of

Checks if country in scope is an indirect subject (subject-of-subject) of event target

is\_indirect\_subject\_of = country

country

country

is\_insurrectionary

Check if country, movement or IG is insurrectionary

country, interest\_group, political\_movement

boolean

is\_interest\_active

Is the interest marker active

is\_interest\_active = yes/no

interest\_marker

boolean

is\_interest\_group\_type

Checks if Interest Group is of a certain type

Can also be used on characters directly

is\_interest\_group\_type = x

character, interest\_group

is\_invasion\_stalled

Checks if the invasion is stalled due to wrong commander orders

is\_invasion\_stalled = bool

invasion

boolean

is\_isolated\_from\_market

Check if a state is isolated from its market

state

boolean

is\_junior\_in\_customs\_union

True if country is a junior country in a customs custom

is\_junior\_in\_customs\_union = bool

country

boolean

is\_land\_theater

Checks if a theater is a land theater

is\_land\_theater = bool

theater

boolean

is\_largest\_state\_in\_region

Check if state is the largest in the state region = bool

state

boolean

is\_lens\_open

Checks if a certain lens is open, specified as a lens key. Specify an optional named tab to check if this tab is open.

is\_lens\_open = {
 lens = lens\_key
 tab\_name = tab\_key
}

none

is\_local\_player

True if country scope is a player

is\_local\_player = bool

country

boolean

is\_losing\_power\_rank

Check if the country is in the process of dropping in power ranking

country

boolean

is\_marginal

True if IG scope is marginal

is\_marginal = bool

interest\_group

boolean

is\_market\_reachable\_for\_trade

Check if the scope country can reach the specified market scope for purposes of trade

is\_market\_reachable\_for\_trade = scope:my\_market\_scope

country

market

is\_mass\_migration\_origin

Checks if the scoped country is the origin of mass migration

is\_mass\_migration\_origin = yes

country

boolean

is\_mass\_migration\_origin\_of\_culture

Checks if the scoped country origin of mass migration of specific culture

is\_mass\_migration\_origin\_of\_culture = cu:example\_culture

country

culture

is\_mass\_migration\_target

Mass migration target is state.

any\_state = { owner = ROOT is\_mass\_migration\_target = yes }

state

boolean

is\_mass\_migration\_target\_for\_culture

Scoped state is a mass migration target for the specified culture.

is\_mass\_migration\_target\_for\_culture = scope:my\_culture

state

culture

is\_member\_of\_any\_lobby

Checks if interest group is member of any lobby

is\_member\_of\_any\_lobby = yes

interest\_group

boolean

is\_member\_of\_lobby

Checks if interest group is member of a certain lobby type

is\_member\_of\_lobby = lobby\_type:pro\_country\_lobby

interest\_group

political\_lobby\_type

is\_member\_of\_party

Checks if Interest Group is a member of target party

is\_member\_of\_party = <party>

interest\_group

party

is\_mobilized

Checks if the military formation is mobilized

is\_mobilized = bool

military\_formation

boolean

is\_monarch

Checks if character is a monarch of a country with hereditary succession

is\_monarch = bool

character

boolean

is\_objective\_completed

Is the objective completed for the country in scope?

is\_objective\_completed = yes

none

boolean

is\_on\_front

Checks if a character or military formation has been assigned to a Front and has arrived there

is\_on\_front = yes/no

character, military\_formation

boolean

is\_owed\_obligation\_by

Checks if the scoped country is owed a obligation by the target country

is\_owed\_obligation\_by = c:FRA

country

country

is\_panel\_open

Checks if a certain infopanel is open, specified as an event target (target) or as a string (panel\_name).

Specify an optional named tab (tab\_name) to check if this tab is open.

is\_panel\_open = {
 target = <scope> OR panel\_name = panel\_name
 tab\_name = tab\_name
}

none

is\_party

Checks if the target party is same as scoped party. Will only work on exact same party object, meaning you can't compare across countries.

is\_party = <party>

party

party

is\_party\_type

Checks if the scoped party's type is the specified one

is\_party\_type = party\_type\_database\_key

party

is\_player

True if country scope is a player

is\_player = bool

country

boolean

is\_political\_lobby\_type

Checks political lobby is of a certain lobby type

is\_political\_lobby\_type = political lobby type

political\_lobby

is\_political\_movement\_type

Check if a political movement is a particular type

is\_political\_movement\_type = movement\_to\_enact

political\_movement

is\_pop\_type

Checks if pop is of specified type

is\_pop\_type = poptype

pop

is\_popup\_open

Checks if the specified popup panel is open

is\_popup\_open = popup

none

is\_potential\_treaty\_port

Checks if the scoped state has the potential to become a treaty port for target country

is\_potential\_treaty\_port = c:FRA

state

country

is\_power\_bloc\_leader

Checks if the country is a leader of a Power Bloc

is\_power\_bloc\_leader = yes/no

country

boolean

is\_powerful

True if IG scope is influential

is\_powerful = bool

interest\_group

boolean

is\_primary\_culture\_of

Checks if culture is any of a country's primary cultures

is\_primary\_culture\_of = <country>

culture

country

is\_producing\_prestige\_goods

Checks if scope company is producing prstige goods

is\_producing\_prestige\_goods = yes

company

boolean

is\_production\_method\_active

Checks if the building of a particular type in scoped state has the specified production method active

is\_production\_method\_active = {
 building\_type = <key>
 production\_method = <key>
}

state

is\_progressing

Check if the journal entry is progressing

is\_progressing = yes/no

journal\_entry

boolean

is\_province\_land

Check if the province is on land

province

boolean

is\_reasonable\_law\_for\_petition

Checks if a law is considered reasonable for a government petition to enact it

is\_reasonable\_law\_for\_petition = yes

law

boolean

is\_renegotiation

Checks if the scoped treaty is amending an existing treaty

scope:some\_treaty = {
 if = {
  limit = { is\_renegotiation = yes }
 }
}

treaty, treaty\_options

boolean

is\_researching\_technology

Check if the country is actively researching a tech

is\_researching\_technology = <scope/key/any>

country

is\_researching\_technology\_category

Check if the country is actively researching a tech category

is\_researching\_technology\_category = <key/any>

country

is\_revolutionary

Check if the country, movement or interest group is revolutionary

country, interest\_group, political\_movement

boolean

is\_rightclick\_menu\_open

Checks if the specified rightclick menu is open

is\_rightclick\_menu\_open = <menu\_key>

none

is\_ruler

Checks if character is a ruler/head of state of a country

is\_ruler = bool

character

boolean

is\_same\_interest\_group\_type

Checks if Interest Group is of the same IG type as target

is\_interest\_group\_type = scope:neighbor\_leading\_ig

interest\_group

interest\_group

is\_same\_law\_group\_as

Checks if scope law type is in the same group as the target law type scope

is\_same\_law\_group\_as = <law type scope>

law\_type

law\_type

is\_same\_party\_type

Checks if Party is of the same party type as target

is\_same\_party\_type\_as = scope:neighboring\_party

party

party

is\_sea\_adjacent

Check if state borders a sea region (regular or impassable)

is\_sea\_adjacent = yes/no

state

boolean

is\_secessionist

Check if the country, movement or interest group is secessionist

country, interest\_group, political\_movement

boolean

is\_set

Checks whether the specified scope target has been set (includes being the null object)

is\_set = from.owner.var:cool\_var.mother

none

is\_slave\_state

Check if a state employs or has the potential to employ slaves.

state

boolean

is\_split\_state

Checks if the scoped state is a split state.

scope:example\_state = { is\_split\_state = yes }

state

boolean

is\_state\_region\_land

Check if the state region is on land

state\_region

boolean

is\_state\_religion

Checks if the religion is the state religion in a country

is\_accepted\_religion = <country>

religion

country

is\_strategic\_objective

Checks if the scoped State is a Strategic Objective of a Country

is\_strategic\_objective = <country>

state

country

is\_strongest\_ig\_in\_government

Checks whether the scoped interest group has the most clout out of all interest groups in government

is\_strongest\_ig\_in\_government = yes/no

interest\_group

boolean

is\_subject

True if country is a subject

is\_subject = bool

country

boolean

is\_subject\_of

Checks if country in scope is subject (or subject-of-subject) of event target

is\_subject\_of = country

country

country

is\_subject\_type

Checks the country's subject type

is\_subject\_type = subject type

country

is\_subsidized

Check if a building is being subsidized

is\_subsidized = yes/no

building

boolean

is\_subsistence\_building

Check if a building is a subsistence building

is\_subsistence\_building = yes/no

building

boolean

is\_supporting\_unification\_candidate

Check if scope country is supporting a unification candidate for a specific country formation

is\_supporting\_unification\_candidate = {
 who = c:PRU
 country\_formation = GER
}

country

is\_target\_in\_global\_variable\_list

Checks if a target is in a global variable list

is\_target\_in\_global\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

is\_target\_in\_local\_variable\_list

Checks if a target is in a local variable list

is\_target\_in\_local\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

is\_target\_in\_variable\_list

Checks if a target is in a variable list

is\_target\_in\_variable\_list = {
 name = <variable name>
 target = <event target>
}

none

is\_target\_of\_wargoal

Checks if state is target of any wargoal in wars involving a specific country

has\_war\_goal = <country>

state

country

is\_taxing\_goods

Check if a country is taxing a good

has\_embargo = <goods key/scope>

country

is\_template\_used

Checks if character template has already been used

is\_template\_used = template\_key

none

is\_theme\_selected

Does the player have the theme selected in settings

is\_theme\_selected = <key>

none

is\_tradeable

Check if a goods or market goods is tradeable

is\_tradeable = yes/no

goods, market\_goods

boolean

is\_treaty\_article\_in\_danger

Checks if treaty article is in danger of breaking

is\_treaty\_article\_in\_danger = yes

treaty\_article

boolean

is\_treaty\_port

Checks if the scoped state is a treaty port

is\_treaty\_port = yes/no

state

boolean

is\_tutorial\_active

Is the tutorial active?

none

boolean

is\_tutorial\_lesson\_active

Is this the current tutorial lesson?

is\_tutorial\_lesson\_active = reactive\_advice\_succession

none

is\_tutorial\_lesson\_chain\_completed

Has the tutorial lesson chain with the specified key been finished?

none

is\_tutorial\_lesson\_completed

has the tutorial lesson with the specified name been finished?

none

is\_tutorial\_lesson\_step\_completed

Has the tutorial lesson step been finished?

is\_tutorial\_lesson\_step\_completed = lesson\_key:step\_key

none

is\_under\_colonization

Check if state is under colonization

is\_under\_colonization = yes/no

state

boolean

is\_under\_construction

Checks if building is under construction

is\_under\_construction = bool

building

boolean

is\_unification\_candidate

Check if scope country is a unification candidate for country tag

is\_unification\_candidate = GER

country

is\_violating\_sovereignty\_of

Check if the scoped country is violating the sovereignty of a target country

is\_violating\_sovereignty\_of = c:GBR

country

country

is\_vulnerable\_front

Whether the scoped Front doesn't have any Battalions nor Generals on target side, and the enemy has at least one General.

is\_vulnerable\_front = scope:example\_country

front

country

is\_war

True if the diplomatic play has escalated into war

is\_war = bool

diplomatic\_play

boolean

is\_war\_participant

Check if the target country is participant in a war

war

country

is\_warleader

Check if country is warleader in war

war

country

is\_world\_market\_hub

Check if state is a world market hub

is\_world\_market\_hub = yes

state

boolean

isolated\_states

Compare number of Isolated States

country

value

journal\_entry\_age

Return the age of the journal entry (since activation) in days

journal\_entry\_age > 720

journal\_entry

value

law\_approved\_by

Checks whether the scoped law is approved by an interest group

law\_approved\_by = ig\_rural\_folk

law

law\_enactment\_stance

Compares the stance of the scoped character, movement or interest group has about enactment of the specified law compared to the current active law in the same group

character, interest\_group, political\_movement

law\_is\_available

Checks if a law is available for its country, considering its current situation. Like can\_be\_enacted, but ignores government support

law\_is\_available = yes

law

boolean

law\_progressiveness\_difference

Compares the progressiveness of the scoped law type to the target law type, higher value means greater difference

"law\_progressiveness\_difference( scope:target\_law )" > 50

law\_type

value

law\_stance

Compares the base stance of the scoped character, movement or interest group has about the specified law, ignoring the current active law in the same group

character, interest\_group, political\_movement

leading\_producer\_of

Checks if country is producing the most of a certain good

leading\_producer\_of = g:luxury\_clothes

country

goods

leads\_customs\_union

Check if any other country is part of this country's customs union

leads\_customs\_union = bool

country

boolean

levels\_owned\_by\_country

Compares how many levels a country or investors in that country own in a building

 levels\_owned\_by\_country = {
 target = c:GBR
 value >= 1
}

building

value

leverage\_advantage

Checks that the leverage advantage between power bloc and target country meets condition

power\_bloc = {
 leverage\_advantage = {
  target = c:BEL
  value > 0
 }
}

power\_bloc

value

liberty\_desire

Compare trigger for the Liberty Desire value in a scoped country.

liberty\_desire > 50

country

value

liberty\_desire\_weekly\_progress

Compare trigger for the weekly Liberty Desire progress value in a scoped country.

liberty\_desire\_weekly\_progress > 0

country

value

liberty\_desire\_weekly\_progress\_from\_support\_independence

Compare trigger for the weekly Liberty Desire progress value from the scoped country having their independence supported.

liberty\_desire\_weekly\_progress\_from\_support\_independence > 0

country

value

list\_size

Checks the size of a list

list\_size = {
 name = <list name>
 value >= <value>
}

none

value

literacy\_rate

Checks if a pop, state or country has a certain amount of literacy

literacy = value

country, pop, state

value

lobby\_clout

Compare to total clout of scoped lobby's interest groups

lobby\_clout >= 0.2

political\_lobby

value

lobby\_formation\_reason

Check scope diplomatic catalyst or political lobby scope for lobby formation reason

lobby\_formation\_reason = diplomacy

diplomatic\_catalyst, political\_lobby

local\_variable\_list\_size

Checks the size of a local variable list

local\_variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

loyalist\_fraction

Compares loyalist fraction in pops in state or country, all parameters except value are optional

loyalist\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

country, state

value

loyalty

Compares the loyalty in a given state, i.e. the fraction of Loyalists

loyalty > 0.2

state

value

market\_access

Checks the market access of the scoped state

market\_access > 0.8

state

value

market\_consumption\_share

Compare fraction of scope market consumption coming from target country

market\_consumption\_share = {
 target = <country scope>
 goods = key (optional)
 value > 0.5 }

market

value

market\_exports

Compare total units of exports from scope to target market (if set) or world market in general (if no market specified)

market\_exports = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

market

value

market\_exports\_reliance

Compare fraction of buy orders in scope market that are due to exports to target market (if set) or world market in general (if no market specified)

market\_exports\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

market

value

market\_goods\_buy\_orders

Checks if market goods has the specified number of buy orders

market\_goods\_buy\_orders < <value>

market\_goods

value

market\_goods\_cheaper

Checks if market goods is at least the specified percentage cheaper than base price

market\_goods\_cheaper > <value>

market\_goods

value

market\_goods\_consumption

Checks if market goods has the specified number of total consumption

market\_goods\_consumption < <value>

market\_goods

value

market\_goods\_delta

Checks if market has the specified goods delta (production + imports) - (consumption + exports)

market\_goods\_delta < <value>

market\_goods

value

market\_goods\_export\_share

Checks if market goods exports are the specified fraction of world market exports

market\_goods\_export\_share < <value>

market\_goods

value

market\_goods\_exports

Checks if market goods has the specified number of exports

market\_goods\_exports < <vaue>

market\_goods

value

market\_goods\_has\_goods\_shortage

Check if market goods has a shortage in the market

market\_goods\_has\_goods\_shortage = yes/no

market\_goods

boolean

market\_goods\_import\_share

Checks if market goods imports are the specified fraction of world market imports

market\_goods\_import\_share < <value>

market\_goods

value

market\_goods\_imports

Checks if market goods has the specified number of imports

market\_goods\_imports < <value>

market\_goods

value

market\_goods\_pricier

Checks if market goods is at least the specified percentage more expensive than base price

market\_goods\_pricier > <value>

market\_goods

value

market\_goods\_production

Checks if market goods has the specified number of total production

market\_goods\_production < <value>

market\_goods

value

market\_goods\_sell\_orders

Checks if market goods has the specified number of sell orders

market\_goods\_sell\_orders < <value>

market\_goods

value

market\_goods\_shortage\_ratio

Compares the shortage ratio of a market goods in its market

market\_goods\_shortage\_ratio > 0.33

market\_goods

value

market\_has\_goods\_shortage

Check if market has a shortage on any of its building inputs

market\_has\_goods\_shortage = yes/no

market

boolean

market\_imports

Compare total units of imports from target to scope market

market\_imports = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

market

value

market\_imports\_reliance

Compare fraction of buy orders in scope market that are due to imports from target market (if set) or world market in general (if no market specified)

market\_imports\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

market

value

market\_number\_goods\_shortages

Check how many shortages a market has on any of its building inputs

market\_number\_goods\_shortages >= 2

market

value

market\_number\_goods\_shortages\_with

Check how many shortages a market has on any of its building inputs, plus the ones from the target country

"market\_number\_goods\_shortages\_with(scope:target)" >= 2

market

value

market\_number\_goods\_shortages\_without

Check how many shortages a market has on any of its building inputs, subtracting the ones from the target country

"market\_number\_goods\_shortages\_without(scope:target)" >= 2

market

value

market\_prestige\_goods\_buy\_orders

Compare the buy orders of a specific prestige good type in the market

market\_prestige\_goods\_buy\_orders = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_consumption

Compare the consumption value of a specific prestige good type in the market

market\_prestige\_goods\_consumption = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_delta

Compare the delta (sell orders - buy orders) of a specific prestige good type in the market

market\_prestige\_goods\_delta = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_export\_share

Compare the world market expore share of a specific prestige good type in the market

market\_prestige\_goods\_export\_share = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_exports

Compare the export value of a specific prestige good type in the market

market\_prestige\_goods\_exports = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_import\_share

Compare the world market export share of a specific prestige good type in the market

market\_prestige\_goods\_import\_share = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_imports

Compare the import value of a specific prestige good type in the market

market\_prestige\_goods\_imports = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_production

Compare the production value of a specific prestige good type in the market

market\_prestige\_goods\_production = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_prestige\_goods\_sell\_orders

Compare the sell orders of a specific prestige good type in the market

market\_prestige\_goods\_sell\_orders = {
  type = <prestige\_good\_type>
  value >= <amount>
}

market\_goods

value

market\_production\_share

Compare fraction of scope market production coming from target country

market\_production\_share = {
 target = <country scope>
 goods = key (optional)
 value > 0.5
}

market

value

market\_trade

Compare total units of goods traded between scope market and target market (if set) or world market in general (if no market specified)

market\_trade = {
  target = market scope (optional)
  goods = key (optional)
  value > 100
}

market

value

market\_trade\_reliance

Compare fraction of buy and sell orders in scope market that are due to trade with target market (if set) or world market in general (if no market specified)

market\_trade\_reliance = {
  target = market scope (optional)
  goods = key (optional)
  value > 0.5
}

market

value

max\_contraventions

Check the maximum contraventions for the article type of the scoped article or article\_options

scope:treaty\_article = {
 max\_contraventions >= 1
}

treaty\_article, treaty\_article\_options

value

max\_law\_enactment\_setbacks

Compares to the max number of law enactment setbacks a country can suffer before the law fails

country

value

max\_num\_companies

The limit of how many companies the scoped country is allowed to have

num\_num\_companies > 0

country

value

max\_num\_declared\_interests

Compares the maximum number of declared interests of scoped country

max\_num\_declared\_interests = <value>

country

value

military\_wage\_level

Compares the military wage level of scoped country. `very_low, low, medium, high, very_high`; returns same number as military\_wage\_level\_value trigger

military\_wage\_level = <level\_term>

country

value

military\_wage\_level\_value

Compares the military wage level value of scoped country. `0, 1, 2, 3, 4`

military\_wage\_level\_value = <level\_num>

country

value

month

Compare to current game date month (Jan: 0, Dec: 11)

month > 10

none

value

most\_powerful\_strata

Compares an interest groups most powerful strata

interest\_group

most\_prominent\_revolting\_interest\_group

Checks if the most prominent revolting interest group in the scoped state has the given interest group type. Evaluates false if the scoped state is not in revolt.

most\_prominent\_revolting\_interest\_group = ig\_landowners

state

movement\_can\_cause\_obstinance

Check if the political movement can generate obstinance in states

movement\_can\_cause\_obstinance = yes

political\_movement

boolean

movement\_is\_causing\_obstinance

Check if the political movement is causing obstinance in any state

movement\_is\_causing\_obstinance = no

political\_movement

boolean

movement\_pressure

Compares the pressure of the scoped movement on the target IG

"movement\_pressure( ig:ig\_intelligentsia )" > 0.1

political\_movement

value

nand

a negated AND trigger

Returns false if all contained triggers return true; returns true at first false sub-trigger

none

nationalization\_cost

Compares the total cost of nationalizing all buildings in scope country owned by the government or pops of target country

nationalization\_cost = {
 target = scope:other\_country
 value > 100000
}

country

value

naval\_power\_projection

Compare to a country's total navy power projection

scope:example\_country = { naval\_power\_projection < root.naval\_power\_projection }

country

value

neighbors\_any\_power\_bloc

Checks if the scoped country neighbors any power bloc. Note: the scoped country is not considered to be neighboring the power bloc it belongs to (if any)

neighbors\_any\_power\_bloc = yes/no

country

boolean

neighbors\_member\_of\_same\_power\_bloc

Checks if the scoped country neighbors any other country belonging to the same power bloc

neighbors\_member\_of\_same\_power\_bloc = yes/no

country

boolean

neighbors\_power\_bloc

Checks if the scoped country neighbors the target power bloc. Note: the scoped country is not considered to be neighboring the power bloc it belongs to (if any)

neighbors\_power\_bloc = c:PRU.power\_bloc

country

power\_bloc

night\_value

Night value

none

value

nor

a negated OR trigger

Returns true if all contained triggers return false; returns false at first true trigger

none

not

negates content of trigger

Returns true if all contained triggers return false

none

num\_casualties

Checks the number of total casualties in the scoped war

num\_casualties >= 5000

war

value

num\_companies

How many companies the scoped country has

num\_companies > 0

country

value

num\_contraventions

Check if the scoped article has the given number of contravention from the given country

scope:treaty\_article = {
 num\_contraventions = {
  target = c:FRA
  value > 0
 }
}

treaty\_article

value

num\_country\_casualties

Checks the number of casualties for the target country in the scoped war

num\_country\_casualties = {
 target = country
 value < 5000
}

war

value

num\_country\_dead

Checks the number of dead for the target country in the scoped war

num\_country\_dead = {
 target = country
 value < 5000
}

war

value

num\_country\_wounded

Checks the number of wounded for the target country in the scoped war

num\_country\_wounded = {
 target = country
 value < 5000
}

war

value

num\_cultural\_communities

Check cultural communities in state

num\_cultural\_communities > 5

state

value

num\_dead

Checks the number of total dead in the scoped war

num\_dead >= 5000

war

value

num\_declared\_interests

Compares the number of declared interests of scoped country

num\_declared\_interests = <value>

country

value

num\_diplomatic\_pacts

Compare to a country's number of diplomatic pacts of the specified type

num\_diplomatic\_pacts = {
 type = <key>
 value < 10
}

country

value

num\_front\_casualties

Checks the number of casualties for the target country in the scoped front

num\_front\_casualties = {
 target = country
 value < 5000
}

front

value

num\_front\_dead

Checks the number of dead for the target country in the scoped front

num\_front\_dead = {
 target = country
 value < 5000
}

front

value

num\_front\_wounded

Checks the number of wounded for the target country in the scoped front

num\_front\_wounded = {
 target = country
 value < 5000
}

front

value

num\_investments\_of\_type

Checks the number of buildings levels of a given type the scoped country has outside their own country.

 num\_investments\_of\_type = {
 type = building\_oil\_rig
 value > 5
}

country

value

num\_mobilized\_units\_in\_theater

Determines the number of mobilized units belonging to the scoped theater's owner or their allies, in fronts intersecting the scoped theater

num\_mobilized\_units\_in\_theater > 25

theater

value

num\_political\_lobbies

Compare to number of political lobbies in scoped country or interest group

num\_political\_lobbies > 0

country, interest\_group

value

num\_power\_bloc\_members

Compare the number of members in a power bloc against a value

num\_power\_bloc\_members >= 2

power\_bloc

value

num\_power\_bloc\_states

Compare the number of states in a power bloc against a value

num\_power\_bloc\_states >= 2

power\_bloc

value

num\_provinces\_in\_theater

Determines the number provinces in the scoped theater

num\_provinces\_in\_theater > 10

theater

value

num\_subjects

Compares the number of subjects of scoped country

num\_subjects = <value>

country

value

num\_taxed\_goods

Compares the number of consumption taxed goods of scoped country

num\_taxed\_goods = <value>

country

value

num\_wounded

Checks the number of total wounded in the scoped war

num\_wounded >= 5000

war

value

number\_of\_claims

The number of claims on foreign states the country has

number\_of\_claims > 0

country

value

number\_of\_possible\_decisions

The number of possible decisions a Country can take

number\_of\_possible\_decisions > 0

country

value

obstinance

Compares state obstinance

obstinance > 0.5

state

value

occupancy

Evaluates a building's current occupancy

occupancy < 0.25

building

value

or

false if all contained triggers are false

Returns true at first true sub-trigger

none

organization

Compares the Organization of the Military Formation in scope

organization <= define:NMilitary|MILITARY\_FORMATION\_ORGANIZATION\_MIN

military\_formation

value

organization\_target

Compares the target Organization of the Military Formation in scope

organization\_target < define:NMilitary|MILITARY\_FORMATION\_ORGANIZATION\_MAX

military\_formation

value

overlord\_can\_decrease\_subject\_autonomy

Check if an overlord can decrease a subject autonomy

overlord\_can\_decrease\_subject\_autonomy = bool

country

boolean

owes\_obligation\_to

Checks if country in scope owes a obligation to event target

owes\_obligation\_to = country

country

country

owns\_entire\_state\_region

Check if country owns entire region

owns\_entire\_state\_region = key

country

owns\_treaty\_port\_in

Does country own the treaty port in assigned state region

owns\_treaty\_port\_in = key

country

play\_participant\_has\_war\_goal\_of\_type\_against

Checks if scope country holds a wargoal of a specific type targeting a specific country in any diplomatic play

play\_participant\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

country

play\_side\_has\_war\_goal\_of\_type\_against

Checks if any country on the same side as scope country in any diplomatic play holds a wargoal of a specific type targeting a specific country

play\_side\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

country

political\_movement\_identity\_support

Compare pop with correct culture/religion identity support of political movement

political\_movement\_identity\_support > 0.5

political\_movement

value

political\_movement\_military\_support

Compare military support of political movement

political\_movement\_military\_support > 0.5

political\_movement

value

political\_movement\_popular\_support

Compare popular support of political movement

political\_movement\_popular\_support > 0.5

political\_movement

value

political\_movement\_radicalism

Compare radicalism of political movement

political\_movement\_radicalism > 50

political\_movement

value

political\_movement\_support

Compare support of political movement

political\_movement\_support > 0.5

political\_movement

value

political\_movement\_wealth\_support

Compare wealth support of political movement

political\_movement\_wealth\_support > 0.5

political\_movement

value

politically\_involved\_ratio

Compares the percentage of politically involved population in a country

politically\_involved\_ratio > 0.25

country

value

pollution\_amount

Compare state region pollution

state\_region

value

pollution\_generation

Compare total pollution generation across all buildings in the state

state

value

pop\_acceptance

Compares the scoped pop's acceptance against an acceptance value

pop\_acceptance > 50

pop

value

pop\_employment\_building

Checks if pop is working in a specific building type

pop\_employment\_building = building

pop

pop\_employment\_building\_group

Checks if pop is working in a specific building type

pop\_employment\_building = building

pop

pop\_has\_primary\_culture

Checks if pop's culture is primary

pop\_has\_primary\_culture = bool

pop

boolean

pop\_loyalist\_fraction

Compares the number of Radicals in a pop to its total size

pop\_loyalty\_fraction < 0.5

pop

value

pop\_neutral\_fraction

Compares the number of Neutrals (not Radical, not Loyalist) in a pop to its total size

pop\_neutral\_fraction > 0.95

pop

value

pop\_radical\_fraction

Compares the number of Radicals in a pop to its total size

pop\_radical\_fraction > 0.25

pop

value

pop\_type\_percent\_country

Checks whether the scoped country has <percent> of its population belonging to the specified pop type

scope:example\_country = {
 pop\_type\_percent\_country = {
  pop\_type = <pop\_type>
  percent <compare operator (<,>,= and so on)> <percent>
 }
}

country

value

pop\_type\_percent\_state

Checks that a state's population has a certain percentage of a specific pop type

scope:example\_state = {
 pop\_type\_percent\_state = {
  target = pop\_type:laborer
  value <= 0.8
 }
}

state

value

population\_by\_culture

Compare the current population of a target culture in the scoped state

population\_by\_culture = {
 target = cu:romanian
 value > 2500
}

state

value

potential\_diplomatic\_play\_power\_ratio

Check the expected power ratio between scope country side and target country side in a potential diplomatic play

Attempts to predict which sides involved participants will take (if any) so doesn't just check 'starting strengths'
potential\_diplomatic\_play\_power\_ratio = {
 type = dp\_open\_market
 target = c:BEL
 state = c:BEL.capital
 value > 1.0
}
state and type are optional, will default to dp\_open\_market on target capital

country

value

power\_bloc\_rank

Compare a power bloc's rank

power\_bloc = {
  power\_bloc\_rank <= 2
}

power\_bloc

value

power\_bloc\_share\_gdp

Compare the share of GDP of the country in scope against all its Power Bloc current members, returns -1 if not in a Power Bloc

power\_bloc\_share\_gdp > 0.2

country

value

power\_bloc\_share\_gdp\_with

Compare the share of GDP of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_gdp\_with(scope:country) > 0.2"

country

value

power\_bloc\_share\_gdp\_without

Compare the share of GDP of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_gdp\_without(scope:country) > 0.2"

country

value

power\_bloc\_share\_power\_projection

Compare the share of Power Projection of the country in scope against all its Power Bloc members, returns -1 if not in a Power Bloc

power\_bloc\_share\_power\_projection > 0.2

country

value

power\_bloc\_share\_power\_projection\_with

Compare the share of Power Projection of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_power\_projection\_with(scope:country) > 0.2"

country

value

power\_bloc\_share\_power\_projection\_without

Compare the share of Power Projection of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_power\_projection\_without(scope:country) > 0.2"

country

value

power\_bloc\_share\_prestige

Compare the share of Prestige of the country in scope against all its Power Bloc members, returns -1 if not in a Power Bloc

power\_bloc\_share\_prestige > 0.2

country

value

power\_bloc\_share\_prestige\_with

Compare the share of Prestige of the country in scope against all its Power Bloc members plus an additional country, returns -1 if not in a Power Bloc

"power\_bloc\_share\_prestige\_with(scope:country) > 0.2"

country

value

power\_bloc\_share\_prestige\_without

Compare the share of Prestige of the country in scope against all its Power Bloc members minus one of the members, returns -1 if not in a Power Bloc

"power\_bloc\_share\_prestige\_without(scope:country) > 0.2"

country

value

power\_bloc\_total\_leading\_goods\_producers

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

power\_bloc\_total\_leading\_goods\_producers > 15

power\_bloc

value

power\_bloc\_total\_leading\_goods\_producers\_with

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking, plus one additional country not currently in the power bloc. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

"power\_bloc\_total\_leading\_goods\_producers\_with(scope:target) > 15"

power\_bloc

value

power\_bloc\_total\_leading\_goods\_producers\_without

Compare how many members are among the leading producers for all goods worldwide weighted by their position in the ranking, minus one country that is currently in the power bloc. The top producer is weighted by MIN\_SPOT\_PRESTIGE\_AWARD and then each subsequent position is weighted by one less until. By default this means 1st counts as 3, 2nd as 2 and 3rd as 1. Any other positions as 0.

"power\_bloc\_total\_leading\_goods\_producers\_without(scope:target) > 15"

power\_bloc

value

power\_bloc\_worst\_economic\_dependence

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

power\_bloc

value

power\_bloc\_worst\_economic\_dependence\_with

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

power\_bloc

value

power\_bloc\_worst\_economic\_dependence\_without

If used in a power bloc scope, compare the member with the lowest economic dependence on the leader.

power\_bloc\_worst\_economic\_dependence > 2.2

power\_bloc

value

power\_bloc\_worst\_infamy

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

power\_bloc

value

power\_bloc\_worst\_infamy\_with

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

power\_bloc

value

power\_bloc\_worst\_infamy\_without

If used in a power bloc scope, compare the Infamy value of the member country with the worst (highest) Infamy

power\_bloc\_worst\_infamy > 50

power\_bloc

value

power\_bloc\_worst\_leader\_relations

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

power\_bloc

value

power\_bloc\_worst\_leader\_relations\_with

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

power\_bloc

value

power\_bloc\_worst\_leader\_relations\_without

If used in a power bloc scope, compare the Relations value to the power bloc leader for the member country with the worst (lowest) Relations

power\_bloc\_worst\_leader\_relations > 10

power\_bloc

value

power\_bloc\_worst\_leader\_religion\_population\_fraction

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

power\_bloc

value

power\_bloc\_worst\_leader\_religion\_population\_fraction\_with

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

power\_bloc

value

power\_bloc\_worst\_leader\_religion\_population\_fraction\_without

If used in a power bloc scope, compare the lowest population fraction in a member country that follows the leader's religion.

power\_bloc\_worst\_leader\_religion\_population\_fraction > 10

power\_bloc

value

power\_bloc\_worst\_liberty\_desire

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

power\_bloc

value

power\_bloc\_worst\_liberty\_desire\_with

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

power\_bloc

value

power\_bloc\_worst\_liberty\_desire\_without

If used in a power bloc scope, compare the Liberty Desire value of the member country with the worst (highest) Liberty Desire

power\_bloc\_worst\_liberty\_desire > 75

power\_bloc

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

power\_bloc

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type\_with

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

power\_bloc

value

power\_bloc\_worst\_progressiveness\_difference\_government\_type\_without

If used in a power bloc scope, compare the total Progressiveness value difference for the member country with the worst (highest) difference in progressiveness between their Governance Principles and Distribution of Power Laws compared to the power bloc leader

power\_bloc\_worst\_progressiveness\_compatibility\_government\_type > 100

power\_bloc

value

predicted\_cohesion\_percentage\_with

Predicts the cohesion of a Power Bloc (as a fraction of 1) if it included target country as a member

predicted\_cohesion\_percentage\_with, = {
 target = scope:other\_country
 value > 0.9
}

power\_bloc

value

prefers\_law

Checks if the scoped interest group prefers the specified law to the comparison law

interest\_group

prestige

Compare prestige

country

value

primary\_cultures\_percent\_country

Checks that a country's population has a certain percentage of the country's primary cultures

scope:example\_country = {
 primary\_cultures\_percent\_country >= 0.2
}

country

value

primary\_cultures\_percent\_state

Checks that a state's population has a certain percentage of the country's primary cultures

scope:example\_state = {
 primary\_cultures\_percent\_state >= 0.2
}

state

value

private\_ownership\_fraction

Compares the fraction of this building's levels that are privately owned

 private\_ownership\_fraction > 0.5

building

value

produced\_authority

Compares the produced authority of the scoped country

country

value

produced\_bureaucracy

Compares the produced bureaucracy of the scoped country

country

value

produced\_influence

Compares the produced influence of the scoped country

country

value

progressiveness

Compare the progressiveness of the law type in scope

law\_type:law\_isolationism.progressiveness > 50

law\_type

value

quality\_of\_life

Compares the quality of life of the given pop

quality\_of\_life > 10

pop

value

radical\_fraction

Compares radical fraction in pops in state or country, all parameters except value are optional

radical\_fraction = {
 value = <script\_value>
 pop\_type = <key>
 strata = <key>
 culture = <key/scope>
 religion = <key/scope>
}

country, state

value

radical\_population\_fraction

Compares the fraction of the population that are radicals in a given country

radical\_population\_fraction > 0.2

country

value

real\_month

Compare to current real date month (Jan: 1, Dec: 12)

real\_month > 10

none

value

relative\_authority

Compares the relative production to usage of bureaucracy for the scoped country

country

value

relative\_bureaucracy

Compares relative production to usage of bureaucracy for the scoped country

country

value

relative\_export\_advantage

Compare to state goods relative export advantage

relative\_export\_advantage > 0.1

state\_goods

value

relative\_import\_advantage

Compare to state goodsrelative import advantage

relative\_import\_advantage > 0.1

state\_goods

value

relative\_influence

Compares relative production to usage of influence for the scoped country

country

value

relative\_infrastructure

Compares the infrastructure to infrastructure usage of a state

relative\_infrastructure > 1.1

state

value

religion\_percent\_country

Checks that a country's population has a certain percentage of a specific religion

scope:example\_country = {
 religion\_percent\_country = {
  target = rel:catholic
  value <= 0.8
 }
}

country

value

religion\_percent\_state

Checks that a state's population has a certain percentage of a specific religion

scope:example\_state = {
 religion\_percent\_state = {
  target = rel:catholic
  value <= 0.8
 }
}

state

value

remaining\_undepleted

Check remaining amount of resource, like gold mines in a state

remaining\_undepleted = {
 type = bg\_gold\_mining
 amount > 1
}

state\_region

value

ruler\_can\_have\_command

Checks if the country's government type allows its ruler to have command

ruler\_can\_have\_command = yes/no

country

boolean

save\_temporary\_scope\_as

Saves current scope as a temporary target for use during the trigger execution.

Cannot overwrite already saved scopes

save\_temporary\_scope\_as = <string>

none

save\_temporary\_scope\_value\_as

Saves a numerical or bool value as an arbitrarily-named temporary target to be referenced later in the same trigger.

Cannot overwrite already saved scope values

save\_temporary\_scope\_value\_as = {
 name = <string>
 value = <script\_value>
}

none

scaled\_debt

Compare value to a country's debt relative to debt ceiling

scaled\_debt = value

country

value

scaled\_gold\_reserves

Compare value to a country's gold reserves relative to reserves limit

scaled\_gold\_reserves = value

country

value

scripted\_bar\_progress

Determines the progress of a scripted progress bar

scripted\_bar\_progress = {
 name = progress\_bar\_tag
 value > 0.4
}

journal\_entry

value

self\_ownership\_fraction

Compares the fraction of this building's levels that are owned by the building itself

 self\_ownership\_fraction > 0.5

building

value

shares\_heritage\_trait\_group\_with\_any\_primary\_culture

Checks if culture shares any trait group marked as 'heritage' with any of a country's primary cultures

shares\_heritage\_trait\_group\_with\_any\_primary\_culture = <country>

culture

country

shares\_heritage\_trait\_group\_with\_culture

Checks if culture shares any trait group marked as 'heritage' with another culture

shares\_heritage\_trait\_group\_with\_culture = <culture>

culture

culture

shares\_heritage\_trait\_group\_with\_religion

Checks if the religion shares any heritage trait group with another religion

shares\_heritage\_trait\_group\_with\_religion = <religion>

religion

religion

shares\_heritage\_trait\_group\_with\_state\_religion

Checks if the religion shares any heritage trait group with a country's state religion

shares\_heritage\_trait\_group\_with\_state\_religion = <country>

culture

country

shares\_heritage\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'heritage' with any of a country's primary cultures

shares\_heritage\_trait\_with\_any\_primary\_culture = <country>

culture

country

shares\_heritage\_trait\_with\_culture

Checks if culture shares any trait marked as 'heritage' with another culture

shares\_heritage\_trait\_with\_culture = <culture>

culture

culture

shares\_heritage\_trait\_with\_religion

Checks if religion shares any trait marked as 'heritage' with another religion

shares\_heritage\_trait\_with\_religion = <religion>

religion

religion

shares\_heritage\_trait\_with\_state\_religion

Checks if the religion shares any heritage with a country's religion

shares\_heritage\_trait\_with\_state\_religion = <country>

religion

country

shares\_language\_trait\_group\_with\_any\_primary\_culture

Checks if culture shares any trait group marked as 'language' with any of a country's primary cultures

shares\_language\_trait\_group\_with\_any\_primary\_culture = <country>

culture

country

shares\_language\_trait\_group\_with\_culture

Checks if culture shares any trait group marked as 'language' with another culture

shares\_language\_trait\_group\_with\_culture = <culture>

culture

culture

shares\_language\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'language' with any of a country's primary cultures

shares\_language\_trait\_with\_any\_primary\_culture = <country>

culture

country

shares\_language\_trait\_with\_culture

Checks if culture shares any trait marked as 'language' with another culture

shares\_language\_trait\_with\_culture = <culture>

culture

culture

shares\_tradition\_trait\_with\_any\_primary\_culture

Checks if culture shares any trait marked as 'tradition' with any of a country's primary cultures

shares\_language\_trait\_with\_any\_primary\_culture = <country>

culture

country

shares\_tradition\_trait\_with\_culture

Checks if culture shares any trait marked as 'tradition' with another culture

shares\_language\_trait\_with\_culture = <culture>

culture

culture

should\_set\_wargoal

Check if the country is lacking a primary wargoal in any diplomatic play it is part of

country

boolean

should\_show\_nudity

can nudity be shown?

should\_show\_nudity = yes/no

none

boolean

shrinking\_institution

Checks if the institution is shrinking

expanding\_institution = <scope/key>

country

size\_weighted\_lost\_battles\_fraction

Determines the fraction of battles the target country has lost in the target war, weighted by manpower size of all battles in the war

size\_weighted\_lost\_battles\_fraction = {
 target = scope:war
 value > 0.3
}
add = "size\_weighted\_lost\_battles\_fraction(scope:war)"

country

value

sol\_ranking

Compares a Country's Standard of Living Ranking (position)

sol\_ranking > 42

country

value

standard\_of\_living

Compares the standard of living of a given pop

standard\_of\_living > 10

pop

value

starting\_manpower

Compares the starting manpower of a battle side

starting\_manpower > 25000

battle\_side

value

state\_cultural\_acceptance

Checks how accepted a pop of the target culture is in the scoped state

 state\_cultural\_acceptance = {
 target = cu:french
 value >= 50

state

value

state\_exports

Compare total units of exports from scope to target state (if set) or world state in general (if no state specified)

state\_exports = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

state

value

state\_goods\_cheaper

Checks if state goods is at least the specified percentage cheaper than base price

state\_goods\_cheaper > <value>

state\_goods

value

state\_goods\_consumption

Checks if state goods has the specified number of total consumption

state\_goods\_consumption < <value>

state\_goods

value

state\_goods\_delta

Checks if state has the specified goods delta (production - consumption)

state\_goods\_delta < <value>

state\_goods

value

state\_goods\_has\_local\_goods\_shortage

Check if state goods has a shortage in a state, but NOT in the whole market

state\_goods\_has\_local\_goods\_shortage = yes/no

state\_goods

boolean

state\_goods\_pricier

Checks if state goods is at least the specified percentage more expensive than base price

state\_goods\_pricier > <value>

state\_goods

value

state\_goods\_production

Checks if state goods has the specified number of total production

state\_goods\_production < <value>

state\_goods

value

state\_has\_building\_group\_levels

Checks the sum of building levels for a building group in a state

 state\_has\_building\_group\_levels = {
 type = bg\_heavy\_industry
 value >= 100
}

state

value

state\_has\_building\_levels

Checks the sum of building levels for a state

state\_has\_building\_levels >= 10

state

value

state\_has\_building\_type\_levels

Checks the sum of building levels for a building type in a state

 state\_has\_building\_type\_levels = {
 target = bt:building\_barracks
 value >= 100
}

state

value

state\_has\_goods\_shortage

Check if state has a shortage on any of its building inputs

state\_has\_goods\_shortage = yes/no

state

boolean

state\_has\_national\_awakening

Checks if the scoped state is in a state region with an active national awakening map marker

state\_has\_national\_awakening = yes/no

state

boolean

state\_imports

Compare total units of imports from target to scope state

state\_imports = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

state

value

state\_is\_eligible\_as\_mass\_migration\_target

Check if state can receive a mass migration at all.

any\_state = { state\_is\_eligible\_as\_mass\_migration\_target = yes }

state

boolean

state\_population

Checks the total population of the scoped state

state\_population <= 250000

state

value

state\_religious\_acceptance

Checks how accepted a pop of the given religion is in the the scoped state

 state\_religious\_acceptance = {
 target = rel:catholic
 value >= 50

state

value

state\_trade

Compare total units of goods traded between scope state and target state (if set) or world state in general (if no state specified)

state\_trade = {
 target = state scope (optional)
 goods = key (optional)
 value >= 100
}

state

value

state\_unemployment\_rate

Checks the unemployment rate (percentage) in the scoped state

state\_unemployment\_rate > 0.1

state

value

strata

Checks the strata of the scoped pop

strata >= upper/middle/lower

pop

value

subject\_can\_increase\_autonomy

Check if a subject can increase its autonomy

subject\_can\_increase\_autonomy = bool

country

boolean

supply\_network\_strength

Compares the country's supply network strength (can exceed 1)

supply\_network\_strength > 1.1

country

value

switch

Switch on a trigger for the evaluation of another trigger with an optional fallback trigger.

switch = {
 trigger = simple\_assign\_trigger
 case\_1 = { <triggers> }
 case\_2 = { <triggers> }
 case\_n = { <triggers> }
 fallback = { <triggers> }

none

taking\_loans

Check if the country is currently running a weekly deficit and taking loans to compensate

country

boolean

target\_cohesion\_number

Compare the target Cohesion as a numeric value

target\_cohesion\_number > 50

power\_bloc

value

target\_cohesion\_percentage

Compare the target Cohesion as a percentage of the maximum of a Power Bloc in scope

target\_cohesion\_percentage > 0.5

power\_bloc

value

target\_is

Checks who the target of a diplomatic play is

target\_is = country

diplomatic\_play

country

tax\_capacity

Checks the taxation capacity of the scoped state

tax\_capacity > 50

state

value

tax\_capacity\_usage

Checks the taxation capacity usage of the scoped state

tax\_capacity\_usage > 50

state

value

tax\_level

Compares the overall tax level of scoped country. `very_low, low, medium, high, very_high`; returns same number as tax\_level\_value trigger

tax\_level = <level\_term>

country

value

tax\_level\_value

Compares the overall tax level integer value of scoped country. `0, 1, 2, 3, 4`

income\_tax\_level\_value = <level\_num>

country

value

tenure\_in\_current\_power\_bloc\_days

Compare the number of days the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_days > 720

country

value

tenure\_in\_current\_power\_bloc\_months

Compare the number of (whole) months the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_months > 20

country

value

tenure\_in\_current\_power\_bloc\_weeks

Compare the number of (whole) weeks the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_weeks > 12

country

value

tenure\_in\_current\_power\_bloc\_years

Compare the number of (whole) years the country in scope has been a member of their current Power Bloc

tenure\_in\_current\_power\_bloc\_years > 5

country

value

total\_population

Compares the total population of a given country

total\_population > 100000

country

value

total\_population\_including\_subjects

Compares the total population in the scope country and its subjects

total\_population\_including\_subjects > 40000000

country

value

total\_population\_including\_subjects\_share

Compares the total population in the scope country and its subjects' share of the global population

total\_population\_including\_subjects\_share > 0.4

country

value

total\_population\_share

Compares the total population of a given country's share of the global population

total\_population\_share > 0.02

country

value

total\_size

Compares the total size of the scoped pop

total\_size >= 3000

pop

value

total\_urbanization

Compares the total urbanization of a given state

total\_urbanization > 5

state

value

total\_used\_principle\_levels

Compare a power bloc's total number of active principle levels

power\_bloc = {
 total\_used\_principle\_levels >= 10
}

power\_bloc

value

trait\_value

Compares the character's total trait value

trait\_value < experience\_level

character

value

transfer\_money\_gross\_income

Does the country have this amount of gross income (income before expenses) from money transfer treaties

transfer\_money\_gross\_income > 1000

country

value

transfer\_money\_net\_income

Does the country have this amount of net income (income after expenses) from money transfer treaties

transfer\_money\_net\_income > 1000

country

value

trigger\_else

Evaluates the display\_triggers if the triggers of preceding 'trigger\_if' or 'trigger\_else\_if' are not met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else = { <display\_triggers> }

none

trigger\_else\_if

Evaluates the display\_triggers if the triggers of the preceding \`trigger\_if\` or \`trigger\_else\_if\` are not met and its own trigger of the limit is met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}
trigger\_else\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

none

trigger\_if

Evaluates the display\_triggers if the triggers of the limit are met

trigger\_if = {
 limit = { <triggers> }
 <display\_triggers>
}

none

turmoil

Compares the turmoil in a given state, i.e. the fraction of Radicals

turmoil > 0.2

state

value

unit\_formation\_has\_commander

Checks if the formation of the scoped combat unit has the provided character as one of its commanders

unit\_formation\_has\_commander = <character>

new\_combat\_unit

character

used\_principle\_slots

Compare a power bloc's used principle slot count

power\_bloc = {
  used\_principle\_slots >= 2
}

power\_bloc

value

variable\_list\_size

Checks the size of a variable list

variable\_list\_size = {
 name = <variable name>
 target >= <value>
}

none

war\_exhaustion\_from\_acceptance\_of\_dead

Determines the war exhaustion a country gets from their degree of cultural acceptance of manpower killed in the war, regardless of what side they were on

war\_exhaustion\_from\_acceptance\_of\_dead = {
 target = root
 value >= 0.4
}

war

value

war\_has\_active\_peace\_deal

True if the war has a proposed peace deal

war\_has\_active\_peace\_deal = bool

war

boolean

war\_participant\_has\_war\_goal\_of\_type\_against

Checks if scope country holds a wargoal of a specific type targeting a specific country in any war

war\_participant\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

country

war\_side\_has\_war\_goal\_of\_type\_against

Checks if any country on the same side as scope country in any war holds a wargoal of a specific type targeting a specific country

war\_side\_has\_war\_goal\_of\_type\_against = {
 type = <wargoal type key>
 target = <country scope>
}

country

was\_exiled

Checks whether the scoped character was exiled

was\_exiled = yes/no

character

boolean

was\_formed\_from

Check if a formed country previously had a specific definition

was\_formed\_from = <tag>

country

wealth

Checks if a pop has a certain amount of wealth

wealth = value

pop

value

weekly\_net\_fixed\_income

Does the country have this amount of weekly income after fixed expenses

weekly\_net\_fixed\_income > 1000

country

value

weekly\_profit

Checks whether the profits the building has made this week are above or below a given value

weekly\_profit >= 20.0

building

value

weighted\_calc\_true\_if

Returns true if the sum of weights of fulfilled sub-triggers amount to the specified sum

weighted\_calc\_true\_if = {
 amount = 10
 5 = { <trigger> }
 15 = { <trigger> }
 7 = { <trigger> }
}

none

workforce

Compares the workforce size of the scoped pop

workforce >= 1000

pop

value

world\_market\_access

Checks the world market access of the scoped state

world\_market\_access > 0.8

state

value

world\_market\_delta

Checks if goods has the specified number of exports minus imports in the world market

world\_market\_delta < <value>

goods

value

world\_market\_exports

Checks if goods has the specified number of exports in the world market

world\_market\_exports < <value>

goods

value

world\_market\_imports

Checks if goods has the specified number of imports in the world market

world\_market\_imports < <value>

goods

value

would\_accept\_diplomatic\_action

Checks if a country would accept a diplomatic action proposed by another country

would\_accept\_diplomatic\_action = {
 actor = <country scope>
 type = <action type key>
 first\_state = <state scope, optional>
 second\_state = <state scope, optional>
 modify\_acceptance = <script value, optional>
}
if set, modify\_acceptance will be added to their acceptance score, to allow for checking if they would accept with an obligation, etc

country

year

Compares the current year of the game

year > 1850

none

value

years\_of\_service

Compares the commander's years of service

years\_of\_service > 1

character

value

## References\[[edit](/index.php?title=Trigger&veaction=edit&section=33 "Edit section: References") | [edit source](/index.php?title=Trigger&action=edit&section=33 "Edit section: References")\]

---

-   To update these tables, see [Module:Script docs/Triggers](/Module:Script_docs/Triggers "Module:Script docs/Triggers")

**[Modding](/Modding "Modding")**

Documentation

[Defines](/Defines "Defines") • [Effects](/Effect "Effect") • [Event targets](/Event_target "Event target") • [Scopes](/Scope "Scope") • Triggers

[Macros](/Macro "Macro") • [Modifier types](/Modifier_types "Modifier types") • [On actions](/On_actions "On actions") • [Script value](/Script_value "Script value") • [Variables](/Variable "Variable")

[GUI script](/GUI_script "GUI script") • [Localization](/Localization "Localization")

Scripted content

[Decisions](/Decision_modding "Decision modding") • [Events](/Event_modding "Event modding") • [History](/History_modding "History modding") • [Journal](/Journal_modding "Journal modding") • [Modifiers](/Modifier_modding "Modifier modding") • [Objectives](/index.php?title=Objective_modding&action=edit&redlink=1 "Objective modding (page does not exist)") • [Scripted gui](/Scripted_gui "Scripted gui") • [Customizable localization](/Localization#Customizable_Localization "Localization")

Scripted types

[Buildings](/Building_modding "Building modding") • [Characters](/Character_modding "Character modding") • [Concepts](/index.php?title=Concept_modding&action=edit&redlink=1 "Concept modding (page does not exist)") • [Countries](/Country_modding "Country modding") • [Culture](/Culture_modding "Culture modding") • [Decrees](/Decree_modding "Decree modding") • [Diplomacy](/Diplomacy_modding "Diplomacy modding") • [Goods](/Goods_modding "Goods modding") • [Institutions](/Institution_modding "Institution modding") • [Interest groups](/Interest_group_modding "Interest group modding") • [Laws](/Law_modding "Law modding") • [Parties](/index.php?title=Party_modding&action=edit&redlink=1 "Party modding (page does not exist)") • [Pops](/Pop_modding "Pop modding") • [Power blocs](/Power_bloc_modding "Power bloc modding") • [Religion](/Religion_modding "Religion modding") • [Subject types](/index.php?title=Subject_type_modding&action=edit&redlink=1 "Subject type modding (page does not exist)") • [Technology](/Technology_modding "Technology modding") • [Treaties](/Treaty_modding "Treaty modding") • [War goals](/War_goal_modding "War goal modding")

Map

[Map](/Map_modding "Map modding") • [Geographic regions](/Geographic_region_modding "Geographic region modding") • [States](/State_modding "State modding")

Graphics

[3D Models](/Model_modding "Model modding") • [Interface](/Interface_modding "Interface modding") • [Graphical Assets](/Graphical_asset_modding "Graphical asset modding") • [Fonts](/index.php?title=Font_modding&action=edit&redlink=1 "Font modding (page does not exist)") • [Flags](/Flag_modding "Flag modding")

Audio

[Music](/index.php?title=Music_modding&action=edit&redlink=1 "Music modding (page does not exist)") • [Sound](/Sound_modding "Sound modding")

Other

[AI](/AI_modding "AI modding") • [Console commands](/Console_commands "Console commands") • [Checksum](/Checksum "Checksum") • [Mods](/Mod "Mod") • [Mod compatibility](/Mod_compatibility "Mod compatibility") • [Mod structure](/Mod_structure "Mod structure") • [Scripted tests](/Scripted_test "Scripted test") • [Troubleshooting](/index.php?title=Mod_troubleshooting&action=edit&redlink=1 "Mod troubleshooting (page does not exist)")

Guides

[Mod translation](/Mod_translation "Mod translation") • [New country modding](/New_country_modding "New country modding") • [Save-game editing](/Save-game_editing "Save-game editing") • [State modding guide](/State_modding_guide "State modding guide")

Retrieved from "[https://vic3.paradoxwikis.com/index.php?title=Trigger&oldid=34516](https://vic3.paradoxwikis.com/index.php?title=Trigger&oldid=34516)"

[Categories](/Special:Categories "Special:Categories"):

-   [Potentially outdated](/Category:Potentially_outdated "Category:Potentially outdated")
-   [1.10](/Category:1.10 "Category:1.10")
-   [Modding](/Category:Modding "Category:Modding")

-   This page was last edited on 6 March 2026, at 19:16.
-   Content is available under [Attribution-ShareAlike 3.0](https://central.paradoxwikis.com/Central:Copyrights "central:Central:Copyrights") unless otherwise noted.

-   [Privacy policy](/Victoria_3_Wiki:Privacy_policy)
-   [About Victoria 3 Wiki](/Victoria_3_Wiki:About)
-   [Disclaimers](/Victoria_3_Wiki:General_disclaimer)
-   [Mobile view](https://vic3.paradoxwikis.com/index.php?title=Trigger&mobileaction=toggle_view_mobile)

-   [![Attribution-ShareAlike 3.0](https://licensebuttons.net/l/by-sa/3.0/88x31.png)](https://creativecommons.org/licenses/by-sa/3.0/)
-   [![Powered by MediaWiki](/resources/assets/poweredby_mediawiki_88x31.png)](https://www.mediawiki.org/)

[![](https://forum.paradoxplaza.com/forum/styles/paradox/paradox/logotype_white.svg)](https://www.paradoxinteractive.com)

Games

-   [Discover](https://www.paradoxinteractive.com/our-games/discover "Discover")
-   [Our Brands](https://www.paradoxinteractive.com/our-games/our-brands "Our Brands")
-   [Browse](https://www.paradoxinteractive.com/our-games/all-games "Browse")
-   [Play on Paradox technology](https://play.paradoxplaza.com/ "Paradox Launcher")

Community

-   [Paradox Forums](https://forum.paradoxplaza.com/forum/index.php "Paradox Forums")
-   [Paradox Wikis](https://paradoxwikis.com/ "Paradox Wikis")
-   [Support](https://support.paradoxplaza.com/ "Support")

About

-   [News](https://www.paradoxinteractive.com/media/press-releases/news "News")
-   [About us](https://www.paradoxinteractive.com/our-company "About us")
-   [Careers](http://career.paradoxplaza.com/ "Careers")
-   [Join our playtests](https://www.paradoxplaza.com/playtest-static-info-ca.html "Join our playtests")
-   [Media contact](https://www.paradoxinteractive.com/contact/review-codes-and-product-pr "Media contact")

Social Media

-   [](https://www.facebook.com/ParadoxInteractive)
-   [](https://twitter.com/PdxInteractive)
-   [](https://www.youtube.com/user/Paradoxplaza)
-   [](https://www.twitch.tv/paradoxinteractive)
-   [](https://www.instagram.com/explore/locations/241319129/paradox-interactive/)
-   [](https://open.spotify.com/artist/75N2nC2KNgaQ1e6bGs0wyc)

-   [Legal Information](https://legal.paradoxplaza.com/ "Legal Information")
-   [EU Online Dispute Resolution](https://ec.europa.eu/consumers/odr/main/index.cfm?event=main.home.chooseLanguage "EU Online Dispute Resolution")
-   [Report Illegal Content](https://dsa.paradoxinteractive.com/ "Report Illegal Content")

-   [Terms & Policies](https://legal.paradoxplaza.com/ "Terms & Policies")
-   [Legal Information](https://www.paradoxplaza.com/legal-static-info-ca.html "Legal Information")
-   [EU Online Dispute Resolution](https://ec.europa.eu/consumers/odr/main/index.cfm?event=main.home.chooseLanguage "EU Online Dispute Resolution")
-   [Report Illegal Content](https://dsa.paradoxinteractive.com/ "Report Illegal Content")
-   [Frequently Asked Questions](https://www.paradoxplaza.com/faq-static-info-ca.html "Frequently Asked Questions")
-   [Paradox Interactive corporate website](http://www.paradoxinteractive.com/ "Paradox Interactive corporate website")