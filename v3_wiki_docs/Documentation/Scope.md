 Scope - Victoria 3 Wiki              

[Forum list](https://forum.paradoxplaza.com/forum/forums/) [Trending](https://forum.paradoxplaza.com/forum/threads/trending) [Latest](https://forum.paradoxplaza.com/forum/threads/latest) [New posts](https://forum.paradoxplaza.com/forum/threads/newest)

Paradox

[Store](https://www.paradoxplaza.com/) [Mods](https://mods.paradoxplaza.com/) [Forum](https://forum.paradoxplaza.com/forum/forums/) [Launcher](https://play.paradoxplaza.com/) [PDXCON 2019](https://pdxcon.paradoxplaza.com/?utm_source=pdxplaza-owned&utm_medium=web-owned&utm_content=topmenu-banner&utm_campaign=pc18_pdxcon_20190412_cawe_ann)

[

Paradox Wikis

](/Victoria_3_Wiki)

Victoria 3 Wiki

Active Wikis

[Age of Wonders 4](https://aow4.paradoxwikis.com/) [Empire of Sin](https://eos.paradoxwikis.com/Empire_of_Sin_Wiki) [Cities: Skylines 2](https://cs2.paradoxwikis.com/Cities_Skylines_II_Wiki) [Crusader Kings 3](https://ck3.paradoxwikis.com/Crusader_Kings_III_Wiki) [Europa Universalis 5](https://eu5.paradoxwikis.com/Europa_Universalis_5_Wiki) [Hearts of Iron 4](https://hoi4.paradoxwikis.com/Hearts_of_Iron_4_Wiki) [Hunter: The Reckoning](https://htr.paradoxwikis.com/) [Imperator: Rome](https://imperator.paradoxwikis.com/Imperator_Wiki) [Millennia](https://millennia.paradoxwikis.com/Millennia_Wiki) [Prison Architect](https://prisonarchitect.paradoxwikis.com/) [Stellaris](https://stellaris.paradoxwikis.com/Stellaris_Wiki) [Surviving Mars](https://survivingmars.paradoxwikis.com/Surviving_Mars_Wiki) [Surviving the Aftermath](https://sta.paradoxwikis.com/Surviving_The_Aftermath_Wiki) [Werewolf: the Apocalypse](https://wta.paradoxwikis.com/Werewolf_The_Apocalypse_Wiki) [Vampire: The Masquerade](https://vtm.paradoxwikis.com/VTM_Wiki) [Victoria 3](https://vic3.paradoxwikis.com/Victoria_3_Wiki)

Legacy Wikis

[AoW: Planetfall](https://aowplanetfall.paradoxwikis.com/AoW_Planetfall_Wiki) [Cities: Skylines](https://skylines.paradoxwikis.com/Cities:_Skylines_Wiki) [Crusader Kings 2](https://ck2.paradoxwikis.com/Crusader_Kings_II_Wiki) [Arsenal of Democracy](https://aod.paradoxwikis.com/Main_Page) [Europa Universalis 2](https://eu2.paradoxwikis.com/Main_Page) [Europa Universalis 3](https://eu3.paradoxwikis.com/Europa_Universalis_3_Wiki) [Europa Universalis 4](https://eu4.paradoxwikis.com/Europa_Universalis_4_Wiki) [Europa Universalis: Rome](https://eurome.paradoxwikis.com/Europa_Universalis:_Rome_Wiki) [Hearts of Iron 2](https://hoi2.paradoxwikis.com/Main_Page) [Hearts of Iron 3](https://hoi3.paradoxwikis.com/Hearts_of_Iron_3_Wiki) [Tyranny](https://tyranny.paradoxwikis.com/Tyranny_Wiki) [Victoria 1](https://vic1.paradoxwikis.com/Main_Page) [Victoria 2](https://vic2.paradoxwikis.com/Victoria_2_Wiki)

### Search

   

### Personal tools

Log in

### Personal tools

-   [English](#)
-   Not logged in
-   [Talk](/Special:MyTalk "Discussion about edits from this IP address [n]")
-   [Contributions](/Special:MyContributions "A list of edits made from this IP address [y]")
-   [Log in](/index.php?title=Special:UserLogin&returnto=Scope "You are encouraged to log in; however, it is not mandatory [o]")

## Navigation menu

### Namespaces

-   [Page](/Scope "View the content page [c]")
-   [Discussion](/Talk:Scope "Discussion about the content page [t]")

 English

### Views

-   [Read](/Scope)
-   [Edit](/index.php?title=Scope&veaction=edit "Edit this page [v]")
-   [Edit source](/index.php?title=Scope&action=edit "Edit this page [e]")
-   [View history](/index.php?title=Scope&action=history "Past revisions of this page [h]")

 More

[](/Victoria_3_Wiki "Visit the main page")

### Navigation

-   [Victoria 3 Wiki](/Victoria_3_Wiki "Visit the main page [z]")
-   [Recent changes](/Special:RecentChanges "A list of recent changes in the wiki [r]")
-   [Random page](/Special:Random "Load a random page [x]")

### Paradox Wikis

-   [\- Main portal](https://paradoxwikis.com/)
-   [\- Wikis discord](https://discord.gg/rUWSHuUwnY)
-   [\- Contact us](/Contact_us)
-   [\- Style guidelines](/Victoria_3_Wiki:Style)

### V3 links

-   [\- Forums](https://forum.paradoxplaza.com/forum/forums/victoria-3.1095/)
-   [\- Game discord](https://discord.com/invite/victoriagamePDX)
-   [\- Mods](https://mods.paradoxplaza.com/)
-   [\- Store](https://www.paradoxinteractive.com/games/victoria-3/about?utm_source=pdxwiki-owned&utm_medium=social-owned&utm_content=link&utm_campaign=vic_vic_20220504_past_noty)
-   [\- Twitch](https://www.twitch.tv/paradoxinteractive/)

### Tools

-   [What links here](/Special:WhatLinksHere/Scope "A list of all wiki pages that link here [j]")
-   [Related changes](/Special:RecentChangesLinked/Scope "Recent changes in pages linked from this page [k]")
-   [Special pages](/Special:SpecialPages "A list of all special pages [q]")
-   [Printable version](javascript:print\(\); "Printable version of this page [p]")
-   [Permanent link](/index.php?title=Scope&oldid=34515 "Permanent link to this revision of this page")
-   [Page information](/index.php?title=Scope&action=info "More information about this page")

### braze

# Scope

From Victoria 3 Wiki

[Jump to navigation](#mw-head) [Jump to search](#searchInput)

 

Please help with verifying or updating older sections of this article.  
At least some were last verified for [version](/Victoria_3_Wiki:Versioning "Victoria 3 Wiki:Versioning") 1.10.

**Scopes** are game objects used with most [effects](/Effect "Effect") and [triggers](/Trigger "Trigger"). Scopes generally use the format `scope = { contents }` or `script_function = scope`. Scopes are set in a few ways. Most scripted content sets a certain scope, such as `country` or `state` as the base or **root** scope. Within effects and triggers, [iterators](#iterators) and [event targets](/Event_target "Event target") also can set scopes.

Most scopes are a particular instance of a game object, determined when the scope is called. Certain scopes refer to game object *types* instead; these generally have the word type in their script name.

## Contents

-   [1 Scopes and scripting](#Scopes_and_scripting)
    -   [1.1 Objects and types](#Objects_and_types)
-   [2 Base scope](#Base_scope)
-   [3 Iterators](#Iterators)
    -   [3.1 Trigger iterators](#Trigger_iterators)
    -   [3.2 Effect iterators](#Effect_iterators)
        -   [3.2.1 Multiple random scopes](#Multiple_random_scopes)
-   [4 Event targets](#Event_targets)
    -   [4.1 Dot chains](#Dot_chains)
    -   [4.2 Scope assertion](#Scope_assertion)
-   [5 Common scope examples](#Common_scope_examples)
-   [6 Scope types](#Scope_types)
-   [7 References](#References)

## Scopes and scripting\[[edit](/index.php?title=Scope&veaction=edit&section=1 "Edit section: Scopes and scripting") | [edit source](/index.php?title=Scope&action=edit&section=1 "Edit section: Scopes and scripting")\]

Most scripting is done "in scope" meaning that an effect or trigger must be run in a relevant scoped object in order to correctly check or affect the gamestate. For example, an effect that changes a character's ideology only functions if it is in a character scope. Similarly, a trigger that checks national debt does not function outside of a country scope.

Some effects and triggers work in multiple scope types, and others do not require a specific scope at all, meaning they can be used nearly anywhere. Scopeless script is sometimes referred to as "global", "any", or "none" scope. In the [effect](/Effect "Effect"), [trigger](/Trigger "Trigger"), and [event target](/Event_target "Event target") tables, script that does not require a specific scope is noted with `none`.

### Objects and types\[[edit](/index.php?title=Scope&veaction=edit&section=2 "Edit section: Objects and types") | [edit source](/index.php?title=Scope&action=edit&section=2 "Edit section: Objects and types")\]

Scopes are generally objects, instances of a type. Types are global definitions, setting the base characteristics of their objects, such as default primary cultures and capital in a country definition. For example, an individual building is an object, and the definition of that builind is a type. Most types have limited or no scoping available. Some objects do not have a distinct type or are themselves a type, such as cultures and religions. Types generally have a limited set of effects and triggers that can be run in their scope, and they generally cannot hold variables or static modifiers.

## Base scope\[[edit](/index.php?title=Scope&veaction=edit&section=3 "Edit section: Base scope") | [edit source](/index.php?title=Scope&action=edit&section=3 "Edit section: Base scope")\]

Each scripted element in the game files that runs effects or triggers may contain a base scope, which is typically callable with `root`. Some may contain additional scopes, typically as a saved scope.

## Iterators\[[edit](/index.php?title=Scope&veaction=edit&section=4 "Edit section: Iterators") | [edit source](/index.php?title=Scope&action=edit&section=4 "Edit section: Iterators")\]

**Iterators** – also called lists – are [effects](/Effect "Effect") or [triggers](/Trigger "Trigger") which iterate through all eligible scopes of a certain type, returning them for checking triggers or executing effects.

There are four types of iterators, one for triggers and three for effects.

Prefix

Description

any\_<name>

-   Trigger scope, checks that any returned scope returns true for contained triggers.
    -   Can use `count op value` or `percent op value` to check a specified amount or ratio
    -   With `count = all` or `percent = 1`, it requires all scopes to return true
    -   Can use `filter = { <triggers> }` with `count` or `percent` to limit returned scopes

every\_<name>

-   Effect scope, executes effects on all returned scopes
    -   Can use `limit = { <triggers> }` to narrow the scopes returned

ordered\_<name>

-   Effect scope, executes effects on returned scopes, by default only first scope in ordering is returned
    -   Uses `order_by = value` to determine the ordering, ordered from most positive to most negative
    -   Can use `limit = { <triggers> }` to narrow the scopes returned
    -   Can use `position = int` to select a different position in order, 0-indexed
    -   Can use `min = int` and `max = value` to limit which and how many scopes are returned in the ordering
    -   Can use `check_range_bounds = no` to prevent error logging if the number of returned scopes is less than the range betweem min and max

random\_<name>

-   Effect scope, executes effects on a single random returned scope
    -   Can use `limit = { <triggers> }` to narrow the scopes returned
    -   Can use `weight = { mtth_blocks }` to weight the random selection

### Trigger iterators\[[edit](/index.php?title=Scope&veaction=edit&section=5 "Edit section: Trigger iterators") | [edit source](/index.php?title=Scope&action=edit&section=5 "Edit section: Trigger iterators")\]

*See also: [Trigger](/Trigger "Trigger")*

List of iterator triggers

Trigger

Description

Example

Scopes

Targets

any\_active\_law

Iterate through all active laws in a country

any\_active\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_active\_party

Iterate through all active political parties in a country

any\_active\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_character

Iterate through all characters globally

any\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_exile\_pool

Iterate through characters in the exile pool

any\_character\_in\_exile\_pool = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_character\_in\_void

Iterate through characters in the void

any\_character\_in\_void = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

character

any\_civil\_war

Iterate through all civil wars related to the scoped country

any\_civil\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

civil\_war

any\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

any\_cobelligerent\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

any\_cobelligerent\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_combat\_unit

Iterate through all combat units of input scope

any\_combat\_unit = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

any\_company

Iterate through all companies in a country

any\_company = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

company

any\_country

Iterate through all countries globally

any\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_decentralized\_country

Iterate through all countries that are decentralized

any\_decentralized\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

country

any\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

any\_diplomatic\_catalyst = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_catalyst

any\_diplomatic\_play

Iterate through all diplomatic plays globally

any\_diplomatic\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

diplomatic\_play

any\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

any\_diplomatically\_relevant\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_direct\_subject

Any country directly below current in hierarchy

any\_direct\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

any\_enemy\_in\_diplo\_play = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

any\_enemy\_in\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

any\_harvest\_condition = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

harvest\_condition

any\_in\_global\_list

Iterate through all items in global list.

any\_in\_global\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_hierarchy

Any country in current hierarchy, including current

any\_in\_hierarchy = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_in\_list

Iterate through all items in list.

any\_in\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_in\_local\_list

Iterate through all items in local list.

any\_in\_local\_list = {
 list = name / variable = name
 <count=num/all> / <percent=fixed\_point>
 <triggers>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

any\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

any\_influenced\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

interest\_group

any\_interest\_group

Iterate through all interest groups in a country

any\_interest\_group = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_group

any\_law

Iterate through all laws in a country

any\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

law

any\_lobby\_member

Iterate through all interest group members of a lobby

any\_lobby\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_lobby

interest\_group

any\_market

Iterate through all markets globally

any\_market = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

market

any\_market\_goods

Iterate through all active (market) goods in a market

any\_market\_goods = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market

market\_goods

any\_member

Iterate through all interest group members of a party

any\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

party

interest\_group

any\_military\_formation

Iterate through all military formations currently present at input scope

any\_military\_formation = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, hq

military\_formation

any\_neighbouring\_state

Iterate through all states neighbouring a state region

any\_neighbouring\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state, state\_region, strategic\_region

state

any\_overlord\_or\_above

Any country above current in hierarchy

any\_overlord\_or\_above = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_participant

Any of two participants of the diplomatic pact in a scope

any\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_pact

country

any\_political\_lobby

Iterate through all political lobbies in a country or interest group

any\_political\_lobby = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, interest\_group

political\_lobby

any\_political\_movement

Iterate through all political movements in a country

any\_political\_movement = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

political\_movement

any\_potential\_party

Iterate through all potential political parties in a country

any\_potential\_party = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

party

any\_power\_bloc

Iterate through all power blocs

any\_power\_bloc = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

power\_bloc

any\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

any\_power\_bloc\_member = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

power\_bloc

country

any\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

any\_preferred\_law = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

interest\_group

law

any\_primary\_culture

Primary cultures of the scoped country or country definition

any\_primary\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, country\_definition

culture

any\_province

Iterate through all Provinces in the scoped State

any\_province = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

province

state

any\_rival\_country

Any country that is being rivaled by the country in a scope

any\_rival\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_rivaling\_country

Any country that is rivaling the country in a scope

any\_rivaling\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

any\_scope\_admiral = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_ally

Iterate through all allies to a: country

any\_scope\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_article

Iterate through articles of the scoped treaty

any\_scope\_article = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty

treaty\_article

any\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

any\_scope\_article\_option = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

treaty, treaty\_options

treaty\_article\_options

any\_scope\_building

Iterate through all buildings in a: state, country

any\_scope\_building = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

building

any\_scope\_character

Iterate through all characters in a: country, interest group, or front

any\_scope\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, strategic region)

any\_scope\_country = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

market, state\_region, strategic\_region

country

any\_scope\_culture

Iterate through all cultures in the scope

any\_scope\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state

culture

any\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

any\_scope\_diplomatic\_pact = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

diplomatic\_pact

any\_scope\_front

Iterate through all Fronts related to the scoped War

any\_scope\_front = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

front

any\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

any\_scope\_general = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

any\_scope\_held\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

interest\_marker

any\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

any\_scope\_initiator\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_interest\_marker

Iterate through all interest markers in a: country, strategic region

any\_scope\_interest\_marker = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, state\_region, strategic\_region

interest\_marker

any\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

any\_scope\_play\_involved = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_politician

Iterate through all politicians in a: country or interest group

any\_scope\_politician = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, interest\_group, military\_formation

character

any\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

any\_scope\_pop = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, culture, interest\_group, state

pop

any\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

any\_scope\_regional\_hqs = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

company

building

any\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

any\_scope\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country, front, state\_region, strategic\_region, theater

state

any\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

any\_scope\_target\_ally = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

diplomatic\_play

country

any\_scope\_theater

Iterate through all theaters in a: country

any\_scope\_theater = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

theater

any\_scope\_treaty

Iterate through in force treaties binding the scoped country

any\_scope\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

treaty

any\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

any\_scope\_violate\_sovereignty\_interested\_parties = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

any\_scope\_violate\_sovereignty\_wars = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_scope\_war

Iterate through all wars related to the scope

any\_scope\_war = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

war

any\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

any\_sea\_node\_adjacent\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

state

state

any\_state

Iterate through all states globally

any\_state = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state

any\_state\_region

Iterate through all state regions

any\_state\_region = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

state\_region

any\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

any\_strategic\_objective = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

state

any\_subject\_of\_subject

Any country below direct subjects in hierarchy

any\_subject\_of\_subject = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_subject\_or\_below

Any country below current in hierarchy

any\_subject\_or\_below = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

country

any\_supporting\_character

Iterate through all characters that support the scoped political movement

any\_supporting\_character = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

political\_movement

character

any\_treaty

Iterate through treaties (both in force and drafts)

any\_treaty = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

none

treaty

any\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

any\_valid\_mass\_migration\_culture = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

country

culture

any\_war\_participant

Iterate through all participants in a war

any\_war\_participant = {
  filter = { <triggers> } (optional)
  <count=num/all> /
  <percent=fixed\_point> (optional)
  <triggers>
}

war

country

Trigger iterators all start with `any_` and return true if any scope meets the contained triggers. This behavior can be changed with the parameter `count op int value` or `percent op value [0-1]` when then requires at least the specified number or percent of the scope to meet the contained triggers. These use the usual [comparison operators](/Trigger#Comparison_triggers "Trigger").

If `count` or `percent` are used, another parameter `filter` can be used to limit which scopes are considered.

When negated, trigger iterators behave like `count = all` is set by default, such that it returns true if no scope meets the contained triggers; actually setting `count = all`, however, inverts the behavior back to returning true if any scope does not meet the contained triggers.

### Effect iterators\[[edit](/index.php?title=Scope&veaction=edit&section=6 "Edit section: Effect iterators") | [edit source](/index.php?title=Scope&action=edit&section=6 "Edit section: Effect iterators")\]

*See also: [Effect](/Effect "Effect")*

List of iterator effects

Effect

Description

Example

Scopes

Targets

every\_active\_law

Iterate through all active laws in a country

every\_active\_law = {
  limit = { <triggers> } #optional
  <effects>
}

country

law

every\_active\_party

Iterate through all active political parties in a country

every\_active\_party = {
  limit = { <triggers> } #optional
  <effects>
}

country

party

every\_character

Iterate through all characters globally

every\_character = {
  limit = { <triggers> } #optional
  <effects>
}

none

character

every\_character\_in\_exile\_pool

Iterate through characters in the exile pool

every\_character\_in\_exile\_pool = {
  limit = { <triggers> } #optional
  <effects>
}

none

character

every\_character\_in\_void

Iterate through characters in the void

every\_character\_in\_void = {
  limit = { <triggers> } #optional
  <effects>
}

none

character

every\_civil\_war

Iterate through all civil wars related to the scoped country

every\_civil\_war = {
  limit = { <triggers> } #optional
  <effects>
}

country

civil\_war

every\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

every\_cobelligerent\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

every\_cobelligerent\_in\_war = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_combat\_unit

Iterate through all combat units of input scope

every\_combat\_unit = {
  limit = { <triggers> } #optional
  <effects>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

every\_company

Iterate through all companies in a country

every\_company = {
  limit = { <triggers> } #optional
  <effects>
}

country

company

every\_country

Iterate through all countries globally

every\_country = {
  limit = { <triggers> } #optional
  <effects>
}

none

country

every\_decentralized\_country

Iterate through all countries that are decentralized

every\_decentralized\_country = {
  limit = { <triggers> } #optional
  <effects>
}

none

country

every\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

every\_diplomatic\_catalyst = {
  limit = { <triggers> } #optional
  <effects>
}

country

diplomatic\_catalyst

every\_diplomatic\_play

Iterate through all diplomatic plays globally

every\_diplomatic\_play = {
  limit = { <triggers> } #optional
  <effects>
}

none

diplomatic\_play

every\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

every\_diplomatically\_relevant\_country = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_direct\_subject

Any country directly below current in hierarchy

every\_direct\_subject = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

every\_enemy\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

every\_enemy\_in\_war = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

every\_harvest\_condition = {
  limit = { <triggers> } #optional
  <effects>
}

country, state, state\_region, strategic\_region

harvest\_condition

every\_in\_global\_list

Iterate through all items in global list.

every\_in\_global\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

every\_in\_hierarchy

Any country in current hierarchy, including current

every\_in\_hierarchy = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_in\_list

Iterate through all items in list.

every\_in\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

every\_in\_local\_list

Iterate through all items in local list.

every\_in\_local\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

every\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

every\_influenced\_interest\_group = {
  limit = { <triggers> } #optional
  <effects>
}

political\_movement

interest\_group

every\_interest\_group

Iterate through all interest groups in a country

every\_interest\_group = {
  limit = { <triggers> } #optional
  <effects>
}

country

interest\_group

every\_law

Iterate through all laws in a country

every\_law = {
  limit = { <triggers> } #optional
  <effects>
}

country

law

every\_lobby\_member

Iterate through all interest group members of a lobby

every\_lobby\_member = {
  limit = { <triggers> } #optional
  <effects>
}

political\_lobby

interest\_group

every\_market

Iterate through all markets globally

every\_market = {
  limit = { <triggers> } #optional
  <effects>
}

none

market

every\_market\_goods

Iterate through all active (market) goods in a market

every\_market\_goods = {
  limit = { <triggers> } #optional
  <effects>
}

market

market\_goods

every\_member

Iterate through all interest group members of a party

every\_member = {
  limit = { <triggers> } #optional
  <effects>
}

party

interest\_group

every\_military\_formation

Iterate through all military formations currently present at input scope

every\_military\_formation = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, hq

military\_formation

every\_neighbouring\_state

Iterate through all states neighbouring current scope  
Note British spelling

every\_neighbouring\_state = {
  limit = { <triggers> } #optional
  <effects>
}

country, state, state\_region, strategic\_region

state

every\_overlord\_or\_above

Any country above current in hierarchy

every\_overlord\_or\_above = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_participant

Any of two participants of the diplomatic pact in a scope

every\_participant = {
  limit = { <triggers> } #optional
  <effects>
}

diplomatic\_pact

country

every\_political\_lobby

Iterate through all political lobbies in a country or interest group

every\_political\_lobby = {
  limit = { <triggers> } #optional
  <effects>
}

country, interest\_group

political\_lobby

every\_political\_movement

Iterate through all political movements in a country

every\_political\_movement = {
  limit = { <triggers> } #optional
  <effects>
}

country

political\_movement

every\_potential\_party

Iterate through all potential political parties in a country

every\_potential\_party = {
  limit = { <triggers> } #optional
  <effects>
}

country

party

every\_power\_bloc

Iterate through all power blocs

every\_power\_bloc = {
  limit = { <triggers> } #optional
  <effects>
}

none

power\_bloc

every\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

every\_power\_bloc\_member = {
  limit = { <triggers> } #optional
  <effects>
}

power\_bloc

country

every\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

every\_preferred\_law = {
  limit = { <triggers> } #optional
  <effects>
}

interest\_group

law

every\_primary\_culture

Primary cultures of the scoped country or country definition

every\_primary\_culture = {
  limit = { <triggers> } #optional
  <effects>
}

country, country\_definition

culture

every\_province

Iterate through all Provinces in the scoped State

every\_province = {
  limit = { <triggers> } #optional
  <effects>
}

province

state

every\_rival\_country

Any country that is being rivaled by the country in a scope

every\_rival\_country = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_rivaling\_country

Any country that is rivaling the country in a scope

every\_rivaling\_country = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

every\_scope\_admiral = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

every\_scope\_ally

Iterate through all allies to a: country

every\_scope\_ally = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_scope\_article

Iterate through articles of the scoped treaty

every\_scope\_article = {
  limit = { <triggers> } #optional
  <effects>
}

treaty

treaty\_article

every\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

every\_scope\_article\_option = {
  limit = { <triggers> } #optional
  <effects>
}

treaty, treaty\_options

treaty\_article\_options

every\_scope\_building

Iterate through all buildings in a: state, country

every\_scope\_building = {
  limit = { <triggers> } #optional
  <effects>
}

country, state

building

every\_scope\_character

Iterate through all characters in a: country, interest group, or front

every\_scope\_character = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

every\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, state\_region, strategic region)

every\_scope\_country = {
  limit = { <triggers> } #optional
  <effects>
}

market, state\_region, strategic\_region

country

every\_scope\_culture

Iterate through all cultures in the scope

every\_scope\_culture = {
  limit = { <triggers> } #optional
  <effects>
}

country, state

culture

every\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

every\_scope\_diplomatic\_pact = {
  limit = { <triggers> } #optional
  <effects>
}

country

diplomatic\_pact

every\_scope\_front

Iterate through all Fronts related to the scoped War

every\_scope\_front = {
  limit = { <triggers> } #optional
  <effects>
}

war

front

every\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

every\_scope\_general = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

every\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

every\_scope\_held\_interest\_marker = {
  limit = { <triggers> } #optional
  <effects>
}

country

interest\_marker

every\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

every\_scope\_initiator\_ally = {
  limit = { <triggers> } #optional
  <effects>
}

diplomatic\_play

country

every\_scope\_interest\_marker

Iterate through all interest markers in a: country, state\_region, strategic region

every\_scope\_interest\_marker = {
  limit = { <triggers> } #optional
  <effects>
}

country, state\_region, strategic\_region

interest\_marker

every\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

every\_scope\_play\_involved = {
  limit = { <triggers> } #optional
  <effects>
}

diplomatic\_play

country

every\_scope\_politician

Iterate through all politicians in a: country or interest group

every\_scope\_politician = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

every\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

every\_scope\_pop = {
  limit = { <triggers> } #optional
  <effects>
}

country, culture, interest\_group, state

pop

every\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

every\_scope\_regional\_hqs = {
  limit = { <triggers> } #optional
  <effects>
}

company

building

every\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

every\_scope\_state = {
  limit = { <triggers> } #optional
  <effects>
}

country, front, state\_region, strategic\_region, theater

state

every\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

every\_scope\_target\_ally = {
  limit = { <triggers> } #optional
  <effects>
}

diplomatic\_play

country

every\_scope\_theater

Iterate through all theaters in a: country

every\_scope\_theater = {
  limit = { <triggers> } #optional
  <effects>
}

country

theater

every\_scope\_treaty

Iterate through in force treaties binding the scoped country

every\_scope\_treaty = {
  limit = { <triggers> } #optional
  <effects>
}

country

treaty

every\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

every\_scope\_violate\_sovereignty\_interested\_parties = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

every\_scope\_violate\_sovereignty\_wars = {
  limit = { <triggers> } #optional
  <effects>
}

country

war

every\_scope\_war

Iterate through all wars related to the scope

every\_scope\_war = {
  limit = { <triggers> } #optional
  <effects>
}

country

war

every\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

every\_sea\_node\_adjacent\_state = {
  limit = { <triggers> } #optional
  <effects>
}

state

state

every\_state

Iterate through all states globally

every\_state = {
  limit = { <triggers> } #optional
  <effects>
}

none

state

every\_state\_region

Iterate through all state regions

every\_state\_region = {
  limit = { <triggers> } #optional
  <effects>
}

none

state\_region

every\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

every\_strategic\_objective = {
  limit = { <triggers> } #optional
  <effects>
}

country

state

every\_subject\_of\_subject

Any country below direct subjects in hierarchy

every\_subject\_of\_subject = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_subject\_or\_below

Any country below current in hierarchy

every\_subject\_or\_below = {
  limit = { <triggers> } #optional
  <effects>
}

country

country

every\_supporting\_character

Iterate through all characters that support the scoped political movement

every\_supporting\_character = {
  limit = { <triggers> } #optional
  <effects>
}

political\_movement

character

every\_treaty

Iterate through treaties (both in force and drafts)

every\_treaty = {
  limit = { <triggers> } #optional
  <effects>
}

none

treaty

every\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

every\_valid\_mass\_migration\_culture = {
  limit = { <triggers> } #optional
  <effects>
}

country

culture

every\_war\_participant

Iterate through all participants in a war

every\_war\_participant = { limit = { <triggers> } <effects> }

war

country

ordered\_active\_law

Iterate through all active laws in a country

ordered\_active\_law = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

law

ordered\_active\_party

Iterate through all active political parties in a country

ordered\_active\_party = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

party

ordered\_character

Iterate through all characters globally

ordered\_character = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

character

ordered\_character\_in\_exile\_pool

Iterate through characters in the exile pool

ordered\_character\_in\_exile\_pool = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

character

ordered\_character\_in\_void

Iterate through characters in the void

ordered\_character\_in\_void = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

character

ordered\_civil\_war

Iterate through all civil wars related to the scoped country

ordered\_civil\_war = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

civil\_war

ordered\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

ordered\_cobelligerent\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

ordered\_cobelligerent\_in\_war = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_combat\_unit

Iterate through all combat units of input scope

ordered\_combat\_unit = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

ordered\_company

Iterate through all companies in a country

ordered\_company = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

company

ordered\_country

Iterate through all countries globally

ordered\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

country

ordered\_decentralized\_country

Iterate through all countries that are decentralized

ordered\_decentralized\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

country

ordered\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

ordered\_diplomatic\_catalyst = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

diplomatic\_catalyst

ordered\_diplomatic\_play

Iterate through all diplomatic plays globally

ordered\_diplomatic\_play = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

diplomatic\_play

ordered\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

ordered\_diplomatically\_relevant\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_direct\_subject

Any country directly below current in hierarchy

ordered\_direct\_subject = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

ordered\_enemy\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

ordered\_enemy\_in\_war = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

ordered\_harvest\_condition = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, state, state\_region, strategic\_region

harvest\_condition

ordered\_in\_global\_list

Iterate through all items in global list. list = name or variable = name

ordered\_in\_global\_list	= {
  list = name or variable = name
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; minimum value of ordering comparison to consider
  max = script\_value #optional, default inf; maximum value of ordering comparison to consider
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

ordered\_in\_hierarchy

Any country in current hierarchy, including current

ordered\_in\_hierarchy = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_in\_list

Iterate through all items in list.

ordered\_in\_list	= {
  list = name or variable = name
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; minimum value of ordering comparison to consider
  max = script\_value #optional, default inf; maximum value of ordering comparison to consider
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

ordered\_in\_local\_list

Iterate through all items in local list. list = name or variable = name

ordered\_in\_local\_list	= {
  list = name or variable = name
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; minimum value of ordering comparison to consider
  max = script\_value #optional, default inf; maximum value of ordering comparison to consider
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

ordered\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

ordered\_influenced\_interest\_group = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

political\_movement

interest\_group

ordered\_interest\_group

Iterate through all interest groups in a country

ordered\_interest\_group = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

interest\_group

ordered\_law

Iterate through all laws in a country

ordered\_law = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

law

ordered\_lobby\_member

Iterate through all interest group members of a lobby

ordered\_lobby\_member = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

political\_lobby

interest\_group

ordered\_market

Iterate through all markets globally

ordered\_market = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

market

ordered\_market\_goods

Iterate through all active (market) goods in a market

ordered\_market\_goods = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

market

market\_goods

ordered\_member

Iterate through all interest group members of a party

ordered\_member = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

party

interest\_group

ordered\_military\_formation

Iterate through all military formations currently present at input scope

ordered\_military\_formation = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, hq

military\_formation

ordered\_neighbouring\_state

Iterate through all states neighbouring a state region

ordered\_neighbouring\_state = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, state, state\_region, strategic\_region

state

ordered\_overlord\_or\_above

Any country above current in hierarchy

ordered\_overlord\_or\_above = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_participant

Any of two participants of the diplomatic pact in a scope

ordered\_participant = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

diplomatic\_pact

country

ordered\_political\_lobby

Iterate through all political lobbies in a country or interest group

ordered\_political\_lobby = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, interest\_group

political\_lobby

ordered\_political\_movement

Iterate through all political movements in a country

ordered\_political\_movement = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

political\_movement

ordered\_potential\_party

Iterate through all potential political parties in a country

ordered\_potential\_party = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

party

ordered\_power\_bloc

Iterate through all power blocs

ordered\_power\_bloc = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

power\_bloc

ordered\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

ordered\_power\_bloc\_member = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

power\_bloc

country

ordered\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

ordered\_preferred\_law = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

interest\_group

law

ordered\_primary\_culture

Primary cultures of the scoped country or country definition

ordered\_primary\_culture = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, country\_definition

culture

ordered\_province

Iterate through all Provinces in the scoped State

ordered\_province = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

province

state

ordered\_rival\_country

Any country that is being rivaled by the country in a scope

ordered\_rival\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_rivaling\_country

Any country that is rivaling the country in a scope

ordered\_rivaling\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

ordered\_scope\_admiral = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, interest\_group, military\_formation

character

ordered\_scope\_ally

Iterate through all allies to a: country

ordered\_scope\_ally = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_scope\_article

Iterate through articles of the scoped treaty

ordered\_scope\_article = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

treaty

treaty\_article

ordered\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

ordered\_scope\_article\_option = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

treaty, treaty\_options

treaty\_article\_options

ordered\_scope\_building

Iterate through all buildings in a: state, country

ordered\_scope\_building = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, state

building

ordered\_scope\_character

Iterate through all characters in a: country, interest group, or front

ordered\_scope\_character = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, interest\_group, military\_formation

character

ordered\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, state\_region, strategic region)

ordered\_scope\_country = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

market, state\_region, strategic\_region

country

ordered\_scope\_culture

Iterate through all cultures in the scope

ordered\_scope\_culture = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, state

culture

ordered\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

ordered\_scope\_diplomatic\_pact = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

diplomatic\_pact

ordered\_scope\_front

Iterate through all Fronts related to the scoped War

ordered\_scope\_front = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

war

front

ordered\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

ordered\_scope\_general = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, interest\_group, military\_formation

character

ordered\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

ordered\_scope\_held\_interest\_marker = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

interest\_marker

ordered\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

ordered\_scope\_initiator\_ally = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

diplomatic\_play

country

ordered\_scope\_interest\_marker

Iterate through all interest markers in a: country, state\_region, strategic region

ordered\_scope\_interest\_marker = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, state\_region, strategic\_region

interest\_marker

ordered\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

ordered\_scope\_play\_involved = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

diplomatic\_play

country

ordered\_scope\_politician

Iterate through all politicians in a: country or interest group

ordered\_scope\_politician = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, interest\_group, military\_formation

character

ordered\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

ordered\_scope\_pop = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, culture, interest\_group, state

pop

ordered\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

ordered\_scope\_regional\_hqs = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

company

building

ordered\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

ordered\_scope\_state = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country, front, state\_region, strategic\_region, theater

state

ordered\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

ordered\_scope\_target\_ally = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

diplomatic\_play

country

ordered\_scope\_theater

Iterate through all theaters in a: country

ordered\_scope\_theater = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

theater

ordered\_scope\_treaty

Iterate through in force treaties binding the scoped country

ordered\_scope\_treaty = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

treaty

ordered\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

ordered\_scope\_violate\_sovereignty\_interested\_parties = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

ordered\_scope\_violate\_sovereignty\_wars = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

war

ordered\_scope\_war

Iterate through all wars related to the scope

ordered\_scope\_war = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

war

ordered\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

ordered\_sea\_node\_adjacent\_state = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

state

state

ordered\_state

Iterate through all states globally

ordered\_state = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

state

ordered\_state\_region

Iterate through all state regions

ordered\_state\_region = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

state\_region

ordered\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

ordered\_strategic\_objective = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

state

ordered\_subject\_of\_subject

Any country below direct subjects in hierarchy

ordered\_subject\_of\_subject = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_subject\_or\_below

Any country below current in hierarchy

ordered\_subject\_or\_below = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

country

ordered\_supporting\_character

Iterate through all characters that support the scoped political movement

ordered\_supporting\_character = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

political\_movement

character

ordered\_treaty

Iterate through treaties (both in force and drafts)

ordered\_treaty = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

none

treaty

ordered\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

ordered\_valid\_mass\_migration\_culture = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

country

culture

ordered\_war\_participant

Iterate through all participants in a war

ordered\_war\_participant = {
  limit = { <triggers> } #optional
  order\_by = script\_value #determines ordering of scopes by comparison
  position = int #optional, default 0; selects scope from ordering, 0-indexed
  min = int #optional, default 0; sets "top" position to return, 0-indexed
  max = script\_value #optional, default 1/inf; number of scopes to return, 1 if min isn't specified, inf if it is.
  check\_range\_bounds = no #optional; If you don't want an error logged if the list is smaller than the min/max
  <effects>
}

war

country

random\_active\_law

Iterate through all active laws in a country

random\_active\_law = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

law

random\_active\_party

Iterate through all active political parties in a country

random\_active\_party = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

party

random\_character

Iterate through all characters globally

random\_character = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

character

random\_character\_in\_exile\_pool

Iterate through characters in the exile pool

random\_character\_in\_exile\_pool = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

character

random\_character\_in\_void

Iterate through characters in the void

random\_character\_in\_void = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

character

random\_civil\_war

Iterate through all civil wars related to the scoped country

random\_civil\_war = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

civil\_war

random\_cobelligerent\_in\_diplo\_play

Iterate through all co-belligerents of scope country in all diplomatic plays (includes wars)

random\_cobelligerent\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_cobelligerent\_in\_war

Iterate through all co-belligerents of scope country in all wars

random\_cobelligerent\_in\_war = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_combat\_unit

Iterate through all combat units of input scope

random\_combat\_unit = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

battle, building, front, hq, military\_formation

new\_combat\_unit

random\_company

Iterate through all companies in a country

random\_company = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

company

random\_country

Iterate through all countries globally

random\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

country

random\_decentralized\_country

Iterate through all countries that are decentralized

random\_decentralized\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

country

random\_diplomatic\_catalyst

Iterate through all diplomatic catalysts in the recent memory of a country

random\_diplomatic\_catalyst = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

diplomatic\_catalyst

random\_diplomatic\_play

Iterate through all diplomatic plays globally

random\_diplomatic\_play = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

diplomatic\_play

random\_diplomatically\_relevant\_country

Iterate through all diplomatically relevant countries of a country scope

random\_diplomatically\_relevant\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_direct\_subject

Any country directly below current in hierarchy

random\_direct\_subject = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_enemy\_in\_diplo\_play

Iterate through all enemies of scope country in all diplomatic plays (includes wars)

random\_enemy\_in\_diplo\_play = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_enemy\_in\_war

Iterate through all enemies of scope country in all wars

random\_enemy\_in\_war = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_harvest\_condition

Iterate through all harvest conditions affecting a state, state region, strategic region, or country

random\_harvest\_condition = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, state, state\_region, strategic\_region

harvest\_condition

random\_in\_global\_list

Iterate through all items in global list.

random\_in\_global\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  weight = { mtth } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

random\_in\_hierarchy

Any country in current hierarchy, including current

random\_in\_hierarchy = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_in\_list

Iterate through all items in list.

random\_in\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  weight = { mtth } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

random\_in\_local\_list

Iterate through all items in local list.

random\_in\_local\_list = {
  list = name or variable = name
  limit = { <triggers> } (optional)
  weight = { mtth } (optional)
  <effects>
}
Use "list" for lists created by add\_to\_(temporary)\_list
Use "variable" for lists created by add\_to\_(global/local)\_variable\_list

none

random\_influenced\_interest\_group

Iterate through all interest groups influenced by a political movement

random\_influenced\_interest\_group = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

political\_movement

interest\_group

random\_interest\_group

Iterate through all interest groups in a country

random\_interest\_group = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

interest\_group

random\_law

Iterate through all laws in a country

random\_law = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

law

random\_lobby\_member

Iterate through all interest group members of a lobby

random\_lobby\_member = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

political\_lobby

interest\_group

random\_market

Iterate through all markets globally

random\_market = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

market

random\_market\_goods

Iterate through all active (market) goods in a market

random\_market\_goods = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

market

market\_goods

random\_member

Iterate through all interest group members of a party

random\_member = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

party

interest\_group

random\_military\_formation

Iterate through all military formations currently present at input scope

random\_military\_formation = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, hq

military\_formation

random\_neighbouring\_state

Iterate through all states neighbouring a state region

random\_neighbouring\_state = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, state, state\_region, strategic\_region

state

random\_overlord\_or\_above

Any country above current in hierarchy

random\_overlord\_or\_above = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_participant

Any of two participants of the diplomatic pact in a scope

random\_participant = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

diplomatic\_pact

country

random\_political\_lobby

Iterate through all political lobbies in a country or interest group

random\_political\_lobby = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, interest\_group

political\_lobby

random\_political\_movement

Iterate through all political movements in a country

random\_political\_movement = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

political\_movement

random\_potential\_party

Iterate through all potential political parties in a country

random\_potential\_party = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

party

random\_power\_bloc

Iterate through all power blocs

random\_power\_bloc = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

power\_bloc

random\_power\_bloc\_member

Iterate through all members of the scoped power bloc including the leader

random\_power\_bloc\_member = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

power\_bloc

country

random\_preferred\_law

Iterate through all active and possible laws in an interest group's country, ordered by how much they prefer that law

random\_preferred\_law = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

interest\_group

law

random\_primary\_culture

Primary cultures of the scoped country or country definition

random\_primary\_culture = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, country\_definition

culture

random\_province

Iterate through all Provinces in the scoped State

random\_province = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

province

state

random\_rival\_country

Any country that is being rivaled by the country in a scope

random\_rival\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_rivaling\_country

Any country that is rivaling the country in a scope

random\_rivaling\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_scope\_admiral

Iterate through all admirals in a: country, interest group, or military formation

random\_scope\_admiral = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

random\_scope\_ally

Iterate through all allies to a: country

random\_scope\_ally = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_scope\_article

Iterate through articles of the scoped treaty

random\_scope\_article = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

treaty

treaty\_article

random\_scope\_article\_option

Iterate through article\_options of the scoped treaty or treaty\_options

random\_scope\_article\_option = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

treaty, treaty\_options

treaty\_article\_options

random\_scope\_building

Iterate through all buildings in a: state, country

random\_scope\_building = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, state

building

random\_scope\_character

Iterate through all characters in a: country, interest group, or front

random\_scope\_character = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

random\_scope\_country

Iterate through all countries with a presence in the supported scope (currently: market, state\_region, strategic region)

random\_scope\_country = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

market, state\_region, strategic\_region

country

random\_scope\_culture

Iterate through all cultures in the scope

random\_scope\_culture = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, state

culture

random\_scope\_diplomatic\_pact

Any diplomatic pact of the country in a scope

random\_scope\_diplomatic\_pact = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

diplomatic\_pact

random\_scope\_front

Iterate through all Fronts related to the scoped War

random\_scope\_front = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

war

front

random\_scope\_general

Iterate through all generals in a: country, interest group, front, or military formation

random\_scope\_general = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

random\_scope\_held\_interest\_marker

Iterate through all interest markers held by a country

random\_scope\_held\_interest\_marker = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

interest\_marker

random\_scope\_initiator\_ally

Iterate through all allies to an initiator in a: diplomatic play

random\_scope\_initiator\_ally = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

diplomatic\_play

country

random\_scope\_interest\_marker

Iterate through all interest markers in a: country, state\_region, strategic region

random\_scope\_interest\_marker = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, state\_region, strategic\_region

interest\_marker

random\_scope\_play\_involved

Iterate through all involved in a: diplomatic play

random\_scope\_play\_involved = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

diplomatic\_play

country

random\_scope\_politician

Iterate through all politicians in a: country or interest group

random\_scope\_politician = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, interest\_group, military\_formation

character

random\_scope\_pop

Iterate through all pops in a: country, state, interest group, culture

random\_scope\_pop = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, culture, interest\_group, state

pop

random\_scope\_regional\_hqs

Iterate through regional HQs of the scoped company

random\_scope\_regional\_hqs = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

company

building

random\_scope\_state

Iterate through all states including provinces from a: country, state\_region, theater, or front

random\_scope\_state = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country, front, state\_region, strategic\_region, theater

state

random\_scope\_target\_ally

Iterate through all allies to a target in a: diplomatic play

random\_scope\_target\_ally = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

diplomatic\_play

country

random\_scope\_theater

Iterate through all theaters in a: country

random\_scope\_theater = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

theater

random\_scope\_treaty

Iterate through in force treaties binding the scoped country

random\_scope\_treaty = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

treaty

random\_scope\_violate\_sovereignty\_interested\_parties

Iterate through all countries that would be interested if country in scope has their sovereignty violated

random\_scope\_violate\_sovereignty\_interested\_parties = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_scope\_violate\_sovereignty\_wars

Iterate through all relevant wars if target country had their sovereignty violated by scoped country

random\_scope\_violate\_sovereignty\_wars = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

war

random\_scope\_war

Iterate through all wars related to the scope

random\_scope\_war = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

war

random\_sea\_node\_adjacent\_state

Iterate through all states that share a sea node with a state

random\_sea\_node\_adjacent\_state = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

state

state

random\_state

Iterate through all states globally

random\_state = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

state

random\_state\_region

Iterate through all state regions

random\_state\_region = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

state\_region

random\_strategic\_objective

Iterate through all Strategic Objective states from the scoped Country

random\_strategic\_objective = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

state

random\_subject\_or\_below

Any country below current in hierarchy

random\_subject\_or\_below = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

country

random\_supporting\_character

Iterate through all characters that support the scoped political movement

random\_supporting\_character = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

political\_movement

character

random\_treaty

Iterate through treaties (both in force and drafts)

random\_treaty = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

none

treaty

random\_valid\_mass\_migration\_culture

Lists for cultures in the scoped country that are valid for mass migration

random\_valid\_mass\_migration\_culture = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

country

culture

random\_war\_participant

Iterate through all participants in a war

random\_war\_participant = {
  limit = { <triggers> } #optional
  weight = { <mtth blocks> } #optional
  <effects>
}

war

country

Effect iterators begin with `every_`, `ordered_`, and `random_`. `every_` iterators apply their effects to all returned scopes, `ordered_` iterators order their returned scopes by a value comparison and apply their effects to the first in the ordering, and `random_` iterators apply their effects to a single random returned scope.

All three can use the parameter `limit` to apply triggers that limit which scopes are returned. If the limit triggers are specific enough, such that they return only a single scope, all three types of iterator are effectively identical.

`ordered_` iterators can return a scope besides the first by using the parameter `position = int` which is 0-indexed. Additionally, the parameters `min = int` and `max = value` can be used to limit how many and which scopes are returned in the ordering. `min` sets the "top" position, using the same 0-indexing as `position`, while `max` determines the number of scopes to return, from the floored value given. If not specified, `min = 0` and `max = 1`. Setting `min` without setting `max` considers `max` as equal to the iterator's list size. Use `check_range_bounds = no` to prevent error logging when using min or max, otherwise using a min or max higher than the list size gives and error.

`random_` iterators can weight the returned scopes so that certain scopes are more likely to be selected. This uses `weight = { <mtth blocks> }`. Without a modified weighting, all returned scopes are equally weighted.

#### Multiple random scopes\[[edit](/index.php?title=Scope&veaction=edit&section=7 "Edit section: Multiple random scopes") | [edit source](/index.php?title=Scope&action=edit&section=7 "Edit section: Multiple random scopes")\]

Often it is useful to return multiple random or semi-random scopes. This can be implemented in a few different ways. The most straightforward is multiple uses of a `random_` iterator. To ensure that the same scope is not returned twice, set a variable on each scope as part of its effects and check that the variable has not been set in order to return the next scope.

random\_country = {
    limit = {
        NOT = { has\_variable = blocker\_var }
        <other limit triggers>
    }
    set\_variable = blocker\_var
    <effects>
}
<repeat above as desired>

This style can be made more succinct by using a [scripted effect](/Macro "Macro") or a [while](/Effect#while "Effect") loop, so that the actual effect only needs to be defined once.

An alternative approach uses a single `ordered_` iterator with a semi-random ordering. This ensures each returned scope is unique without having to check for variables, as well as keeping the amount of script used to a minimum. It makes use of the [script value](/Script_value "Script value") function `modulo` to give a semi-random ordering.

ordered\_country = {
    limit = { } # if desired
    order\_by = {
        value = gdp
        modulo = { 1 10 }
    }
    max = 10
    <effects>
}

This example returns 10 countries which are ordered by a randomized modulo of GDP. The `modulo` range can be set to any values desired and can also used `fixed_range` or `integer_range` in order to make use of other scripted values, and other values can be used as the basis, such as total population, prestige, or any typically unique value set.

## Event targets\[[edit](/index.php?title=Scope&veaction=edit&section=8 "Edit section: Event targets") | [edit source](/index.php?title=Scope&action=edit&section=8 "Edit section: Event targets")\]

*Main article: [Event target](/Event_target "Event target")*

List of contextual scope event targets

Event target

Description

From scope

To scope

active\_law

Scope to the scoped country's active law in the named law group (active\_law:lawgroup\_trade\_policy)

country

law

ai\_army\_comparison

Scope to comparative army strength between two countries (example: value = ai\_army\_comparison:root)

country

value

ai\_gdp\_comparison

Scope to comparative GDP between two countries (example: value = ai\_army\_comparison:root)

country

value

ai\_ideological\_opinion

Scope to AI ideological opinion of scope country on target country (example: value = ai\_ideological\_opinion:root)

country

value

ai\_navy\_comparison

Scope to comparative navy strength between two countries (example: value = ai\_navy\_comparison:root)

country

value

ai\_state\_value

Scope to target country AI desire to own scope state (example: value = ai\_state\_value:root)

state

value

ai\_subject\_value

Scope to AI desire of target country to have scope country as a subject (example: value = ai\_subject\_value:root)

country

value

ai\_transit\_rights\_value

Scope to AI desire of to have transit rights through target country (example: value = ai\_transit\_rights\_value:root)

country

value

ai\_treaty\_export\_value

Scope to target country AI desire to export scoped market good (example: value = ai\_market\_export\_value:root)

market\_goods

value

ai\_treaty\_fairness

Scope to AI evaluation of how fair (to both sides) it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_fairness\_score:root)

country

value

ai\_treaty\_import\_value

Scope to target country AI desire to import scoped market good through treaties (example: value = ai\_market\_import\_value:root)

market\_goods

value

ai\_treaty\_port\_value

Scope to target country AI desire to have a treaty port in scope state (example: value = ai\_treaty\_port\_value:root)

state

value

ai\_treaty\_trade\_value

Scope to target country AI desire to import and export scoped market good (example: value = ai\_market\_trade\_value:root)

market\_goods

value

ai\_treaty\_value

Scope to AI evaluation of how favorable to itself it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_value:root)

country

value

amended\_treaty

Scope to the treaty the scoped treaty is amending

treaty, treaty\_options

treaty

army\_size

Number of battalions of scope country, excluding currently raised conscripts.

country

value

army\_size\_including\_conscripts

Number of battalions of scope country, including currently raised conscripts.

country

value

attacker\_side

Scope from a battle to the BattleSide corresponding to the attacker

battle

battle\_side

attacker\_warleader

Scope to the attacker warleader of a war

war

country

average\_defense

Returns the average defense of units on Front on the same side as Country

front

value

average\_expected\_sol

Get the average expected Standard of Living of the scope

country, state

value

average\_offense

Returns the average offense of units on Front on the same side as Country

front

value

average\_sol

Get the average Standard of Living of the scope

country, state

value

b

Scope to the building of specified key in the state, example: b:building\_port

state

building

base\_price

Get the base price of the scoped good

goods

value

battle

Scope from a battle side to the battle itself

battle\_side

battle

binding\_period

Get the original binding period in days when the treaty was signed for the scoped treaty

treaty, treaty\_options

value

building

Scope to the related building

new\_combat\_unit

building

building\_levels

Get the total number of non-subsistence building levels of the scoped country

country

value

cached\_ai\_coastal\_population

Scope to total coastal population recorded in AI spending variables (example: value = cached\_ai\_coastal\_population)

country

value

cached\_ai\_incorporated\_coastal\_population

Scope to total incorporated coastal population recorded in AI spending variables (example: value = cached\_ai\_incorporated\_coastal\_population)

country

value

cached\_ai\_incorporated\_population

Scope to total incorporated population recorded in AI spending variables (example: value = cached\_ai\_incorporated\_population)

country

value

cached\_ai\_overseas\_subject\_population

Scope to total population in direct non-adjacent subjects recorded in AI spending variables (example: value = cached\_ai\_overseas\_subject\_population)

country

value

cached\_ai\_subject\_population

Scope to total population in direct subjects recorded in AI spending variables (example: value = cached\_ai\_subject\_population)

country

value

cached\_ai\_total\_population

Scope to total population recorded in AI spending variables (example: value = cached\_ai\_total\_population)

country

value

cached\_ai\_unincorporated\_coastal\_population

Scope to total unincorporated coastal population recorded in AI spending variables (example: value = cached\_ai\_unincorporated\_coastal\_population)

country

value

cached\_ai\_unincorporated\_population

Scope to total unincorporated population recorded in AI spending variables (example: value = cached\_ai\_unincorporated\_population)

country

value

capital

Scope to the capital state of a country

country

state

civil\_war

Scope to the civil war started by the scoped (revolutionary) political movement.

political\_movement

civil\_war

civil\_war\_origin\_country

Scope to the origin country of a civil war country

country

country

colonial\_growth\_per\_colony

Scope to the country's colonial growth per colony value (example: colonial\_growth\_per\_colony > 0)

country

value

combat\_width

Scope to combat width multiplier of scope province

province

value

command\_limit\_num\_units

Get the baseline number of units from their military formation a commander would normally get, depending on their command limit

character

value

commander\_military\_formation

Scope to the Military Formation of a Character.

character

military\_formation

company

Scope to the scoped country's company of the named company type (company:company\_rheinmetall)

country

company

compare\_complex\_value

A comparison trigger that needs a parsable string parameter that will return its value in the context it is used eg: scope:root.number\_of(armies)

varies

value

compare\_value

A comparison trigger that will return its value in the context it is used eg: root.gold

varies

value

controller

Scope to the controller of the object

province, state

country

country

Scope to the country of a military formation

military\_formation

country

country\_definition

Scope to country's definition

country

country\_definition

credit

Scope to the current amount of max credit the country can take

country

value

culture

Scope to pop's or character's culture

character, new\_combat\_unit, political\_movement, pop

culture

current\_hq

Returns the current HQ of the scoped military formation (may not be valid if the formation is not currently stationed in an HQ)

military\_formation

hq

currently\_active\_law\_in\_group

Scope from a law to the currently active law in that group

law

law

currently\_enacting\_law

Scope from a country to the law that they're currently enacting

country

law

decree\_cost

Scope to a state's cost for a certain decree

authority > decree\_cost:decree\_road\_maintaintenance

state

value

defender\_side

Scope from a battle to the BattleSide corresponding to the defender

battle

battle\_side

defender\_warleader

Scope to the defender warleader of a war

war

country

defense

Returns the unit's defense rating (including impact from their commander)

new\_combat\_unit

value

demoralized

Returns the unit's number of demoralized manpower

new\_combat\_unit

value

diplomatic\_pact\_other\_country

Scope to the other country of the diplomatic pact in scope, example: diplomatic\_pact\_other\_country(root)

diplomatic\_pact

country

diplomatic\_play

Scope from a war to its diplomatic play

war

diplomatic\_play

enforced\_on\_country

Scope to the enforced\_on\_country of an enforced treaty

treaty

country

enforcer\_country

Scope to the enforcer\_country of an enforced treaty

treaty

country

executive

Scope from a company to its executive character

company

character

expenses

Get the current weekly expenses of the scoped country

country

value

first\_country

Scope to the first\_country of any of:

1.  diplomatic pact
2.  treaty
3.  treaty options
4.  treaty article's treaty
5.  treaty article options (for mutual article types only)

diplomatic\_pact, treaty, treaty\_article, treaty\_article\_options, treaty\_options

country

fixed\_expenses

Get the current weekly fixed expenses of the scoped country

country

value

front

Scope to a front from various scopes

-   From a battle to the front where it takes place
-   From a character to the front where they are at or travelling towards
-   From a military formation to the front it is currently at
-   From a province to the front that contains it
-   From an invasion to the invasion front

battle, character, invasion, military\_formation, province

front

front\_length

Returns the number of provinces in Front

front

value

gdp

Get the scoped country's yearly predicted GDP

country, state

value

get\_ruler\_for

Scopes from country to a character that would be ruler, under the given transfer of power type

-   get\_ruler\_for:parliamentary\_elective = { effects... }

country

character

goal\_value

Scope to the journal entry's goal value

journal\_entry

value

goods

Scope to the goods traded by a trade route

market\_goods, state\_goods

goods

government\_size

Get the scoped country's current number of Parties & independent IGs in government

country

value

heir

Scope to the heir of a country

country

character

highest\_ranked\_commander

Scope from a military formation to its highest ranking commander character

military\_formation

character

home\_country

Scopes to the home country of an exile

character, pop

country

home\_hq

Returns the home HQ of the scoped military formation

military\_formation

hq

ideology

Dual use

1.  Scope to a ideology from its name (ideology:ideology\_anarchist)
2.  Scope to the ideology of the scoped character

character, none

ideology

ig

Scope to the interest group of specified key belonging to the country, example: ig:ig\_armed\_forces

country, political\_movement

interest\_group

imposed\_law

Scope from a country to the law currently imposed on them (enacting or not)

country

law

imposer\_of\_law

Scope from a country to the country currently imposing a law on them

country, law

country

income

Get the current weekly income of the scoped country

country

value

income\_transfer\_expenses

Scope to the amount of money scope country is transferring to other countries via treaties & pacts (example:income\_transfer\_expenses >= 100)

country

value

income\_transfer\_relative\_expenses

Scope to the fraction of their income country is transferring to other countries via treaties & pacts (example:income\_transfer\_relative\_expenses >= 0.2)

country

value

infamy

Scope to infamy of scope country (example: infamy >= 2)

country

value

infamy\_threshold

Scope to infamy level threshold for comparison purposes (infamy\_threshold:infamous)

none

value

initiator

Scope to the initiator of the object

diplomatic\_play

country

input\_building\_type

Scope to the building type input in a treaty article

treaty\_article, treaty\_article\_options

building\_type

input\_company

Scope to the company input in a treaty article

treaty\_article, treaty\_article\_options

company

input\_country

Scope to the country input in a treaty article

treaty\_article, treaty\_article\_options

country

input\_goods

Scope to the goods input in a treaty article

treaty\_article, treaty\_article\_options

goods

input\_law

Scope to the law input in a treaty article

treaty\_article, treaty\_article\_options

law\_type

input\_market\_goods

Scope to the market goods input in a treaty article (example: input\_market\_goods:root)

treaty\_article, treaty\_article\_options

market\_goods

input\_quantity

Scope to the quantity input in a treaty article

treaty\_article, treaty\_article\_options

value

input\_state

Scope to the state input in a treaty article

treaty\_article, treaty\_article\_options

state

input\_strategic\_region

Scope to the strategic region input in a treaty article

treaty\_article, treaty\_article\_options

strategic\_region

institution

Scope to a country's institution from its name (institution:institution\_health\_system)

country

institution

interest\_group

Scope to the interest group of the character!

character

interest\_group

interest\_group\_type

Scope to the type of the scoped character's interest group

character

interest\_group\_type

invasion

Returns the invasion of the scoped front if any

front

invasion

invasion\_attacker

If used in an invasion scope, scope to the country executing the invasion.

invasion

country

invasion\_defender

If used in an invasion scope, scope to the country the attacked state is controlled by.

invasion

country

investment

Scope to current investment level in an institution (example: investment < investment\_max)

institution

value

investment\_max

Scope to the country's current maximum investment level in an institution (example: investment < investment\_max)

institution

value

investment\_pool\_income

Get the current weekly investment pool income of the scoped country

country

value

je

Scope to the journal entry of specified key belonging to the country, example: je:je\_antarctica

country

journal\_entry

land\_controller\_hq

Returns the province or state controller land HQ

province, state

hq

land\_hq

Returns the province or state owner local land HQ

province, state

hq

leader

Scope to the leader of the interest group

interest\_group

character

legitimacy

Scope to the current amount of legitimacy in country

country

value

level

Scope to the level of a building

building

value

level\_after\_queued\_constructions

Scope to the level of a building after all queued constructions finish

building

value

lobby\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies targeting a specific country (lobby\_foreign\_anti\_clout:root >= 0.2)

country

value

lobby\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies targeting a specific country (lobby\_foreign\_pro\_clout:root >= 0.2)

country

value

lobby\_in\_government\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_anti\_clout:root >= 0.2)

country

value

lobby\_in\_government\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_pro\_clout:root >= 0.2)

country

value

lobby\_join\_weight

Scope to the join weight of an interest group to a particular lobby (lobby\_join\_weight:root >= 5)

interest\_group

value

lobby\_war\_opposition

Scope to the total clout of all lobbies that oppose the scoped war (lobby\_war\_opposition:root >= 0.2)

country

value

lobby\_war\_support

Scope to the total clout of all lobbies that support the scoped war (lobby\_war\_support:root >= 0.2)

country

value

manpower

Returns the unit's manpower

new\_combat\_unit

value

market

Scope to the market of the object

building, country, market, market\_goods, province, state, state\_goods, state\_region

market

market\_capital

Scope to the market capital of a country

country

state

market\_export\_cap

Get the maximum amount of goods the market can export based on it's production of the scoped MarketGoods

market\_goods

value

market\_import\_cap

Get the maximum amount of goods the market can import based on it's consumption of the scoped MarketGoods

market\_goods

value

mass\_migration\_culture

Scope to the mass migration target culture of a state

state

culture

mg

Scope from a market to the MarketGoods with the specified key, (example: mg:tools)

market

market\_goods

migration\_pull

Get the migration pull for a state or the mass migration pull for a country.

country, state

value

military\_expenses

Get the current military expenses (slaves, wages and goods) of the scoped country

country

value

military\_expenses\_share

Get the current share of military expenses (slaves, wages and goods) relative to the total expenses of the scoped country

country

value

mobilization

Returns the unit's mobilization value between 0 and 1

new\_combat\_unit

value

modifier

Scope to the value of the modifier type of specified key belonging to the current object, example: modifier:country\_influence\_add

battle\_side, building, character, country, interest\_group, market, power\_bloc, state

boolean, value

momentum

Get the scoped party's raw momentum

party

value

morale

Returns the unit's morale

new\_combat\_unit

value

most\_desired\_law

Scope to the law type that political movement most desires to be enacted (if any).

political\_movement

law\_type

mutual\_trade\_value\_with\_country

Scope to the total base value of goods traded in trade routes between the two markets (example: mutual\_trade\_value\_with\_country:root >= 1000)

country

value

named\_script\_value

A script value that will calculate and returns its value in the context it is used

none

color, value

national\_awakening\_state\_region

Links from a culture scope to the state region with an active national awakening for that culture

culture

state\_region

naval\_controller\_hq

Returns the province or state controller navy HQ

province

hq

naval\_hq

Returns the province or state owner local navy HQ

province

hq

navy\_size

Number of flotillas of scope country.

country

value

nf

Scope to a Decree of specified key applied to the state, example: nf:decree\_road\_maintaintenance

state

decree

num\_active\_declared\_interests

Scope to the number of active declared interests a country has

num\_active\_declared\_interests > 5

country

value

num\_active\_interests

Scope to the number of active interests a country has num\_active\_interests > 5

country

value

num\_active\_natural\_interests

Scope to the number of active natural interests a country has num\_active\_natural\_interests > 5

country

value

num\_active\_plays

Get the current number of active Diplomatic Plays the scoped country started

country

value

num\_admirals

Scope to the country's number of admirals

country

value

num\_alliances

Get the total number of alliances of the scoped country (example: num\_alliances:root >= 2)

country

value

num\_alliances\_and\_defensive\_pacts\_with\_allies

Scope to the number of alliances target country has with allies of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_allies:root >= 2)

country

value

num\_alliances\_and\_defensive\_pacts\_with\_rivals

Scope to the number of alliances & defensive pacts target country has with rivals of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_rivals:root >= 2)

country

value

num\_battalions

Returns the number of active battalions under a commander

character

value

num\_characters

Scope to the country's number of characters

country

value

num\_colony\_projects

Get the scoped country's current number of incomplete / in progress colony projects

country

value

num\_commanderless\_units

Number of units in a MilitaryFormation which currently don't have a commander due to commanders Command Limit.

-   Obs: defeated commanders do not command units, i.e., the more defeated commanders, the more commanderless units.

military\_formation

value

num\_commanders

Scope to the country's number of commanders

country

value

num\_convoys\_available

Get the scoped country's current number of available convoys

country

value

num\_convoys\_required

Get the scoped country's current number of required convoys

country

value

num\_declared\_interests

Scope to the number of declared interests a country has (including inactive)

num\_declared\_interests > 5

country

value

num\_defending\_battalions

Returns the total number of units in battle on Front on the same side as Country, example: num\_defending\_battalions(root)

front

value

num\_defensive\_pacts

Scope to the number of defensive pacts target country has in total (example: num\_defensive\_pacts >= 2)

country

value

num\_garrison\_units

Returns the number of units garrisoned in a HQ

hq

value

num\_generals

Scope to the country's number of generals

country

value

num\_income\_transfer\_pacts

Scope to the number of income-transferring (to others) pacts target country has in total (example: num\_income\_transfer\_pacts >= 2)

country

value

num\_income\_transfer\_treaty\_articles

Scope to the number of income-transferring (to others) treaty articles target country has in total (example: num\_income\_transfer\_treaty\_articles >= 2)

country

value

num\_income\_transfers

Scope to the number of income-transferring (to others) pacts and treaty articles target country has in total (example: num\_income\_transfers >= 2)

country

value

num\_incorporated\_states

Get the scoped country's current number of incorporated states

country

value

num\_interests

Scope to the number of interests a country has (including inactive)

num\_interests > 5

country

value

num\_mandates

Scope to the number of available mandates of the scoped power bloc (power\_bloc.num\_mandates > 0)

power\_bloc

value

num\_mobilized\_battalions

Returns the number of fully mobilized units under a General

character

value

num\_natural\_interests

Scope to the number of natural interests a country has (including inactive)

-   num\_natural\_interests > 5

country

value

num\_obligations\_earned

Scope from a country to the number of obligations others owe them (=which they have earned)

country

value

num\_pending\_events

Scope to the number of pending events of given category in scope country

-   num\_pending\_events:enactment > 0

country

value

num\_politicians

Scope to the country's number of politicians

country

value

num\_positive\_relations

Get the current number of countries with positive Relation with the scoped country

country

value

num\_provinces

Get the scoped entity's current number of provinces

front, state, state\_region

value

num\_queued\_constructions

Scope to the number of queued constructions target country has in total (example: num\_queued\_constructions >= 2)

country

value

num\_queued\_government\_constructions

Scope to the number of queued government constructions target country has in total (example: num\_queued\_government\_constructions >= 2)

country

value

num\_queued\_private\_constructions

Scope to the number of queued private constructions target country has in total (example: num\_queued\_private\_constructions >= 2)

country

value

num\_rivals

Scope to the number of rivals target country has in total (example: num\_rivals >= 2)

country

value

num\_ruling\_igs

Get the scoped country's current number of IGs in government

country

value

num\_shared\_rivals

Scope to the number of shared rivals scope country has with target country (example: num\_shared\_rivals:root >= 2)

country

value

num\_states

Get the scoped country's current number of states

country

value

num\_total\_battalions

Returns the total number of units on Front on the same side as Country, example: num\_total\_battalions(root)

front

value

num\_unincorporated\_states

Get the scoped country's current number of unincorporated states

country

value

num\_units

Get the number of active units for a commander or military formation  
Get the number of active units for military formations in the province that are allied to the specified country

character, military\_formation, province

value

num\_units\_in\_battle

Get the number of active units in battle for a commander or military formation  
Get the number of active units in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

value

num\_units\_not\_in\_battle

Get the number of active units not in battle for a commander or military formation  
Get the number of active units not in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

value

num\_units\_share

Share of units a commander has considering his command limit.

Example: if the character has 30 Command Limit, the fleet has 60 total Command Limit, and the formation has 50 total flotillas, then the character should get 25 flotillas for their battle)

character

value

num\_world\_market\_hub\_trade\_center\_levels

Get the total number of trade center levels associated with scope world market hub state

state

value

offense

Returns the unit's offense rating (including impact from their commander)

new\_combat\_unit

value

opposing\_commander

Scope from a character in battle to the character on the other side of the battle

character

character

opposite\_battle\_side

Scope from a battle side to its opposite

battle\_side

battle\_side

overlord

Scope to the direct overlord of the country in scope

country

country

owner

Scope to the owner country of object

building, character, company, country, decree, institution, interest\_group, interest\_marker, journal\_entry, law, market, market\_goods, military\_formation, new\_combat\_unit, political\_movement, pop, province, state

country

owning\_company

Scope to the company owning the scoped country

country

company

participants

Get the total number of participating countries in the scoped market

market

value

party

Scope to the party that an IG is supporting

interest\_group

party

player\_owed\_obligation\_days\_left

Scope to number of days left on an obligation in diplomatic relations scope (example: obligation\_days\_left >= 2)

diplomatic\_relations

value

political\_movement

-   If used in a character scope, scope to the political movement that character supports.
-   If used in a revolution civil war scope, scope to the movement that started the revolution.
-   If used in an interest group scope, scope to the group's dominant movement.

character, civil\_war

political\_movement

pop\_weight\_modifier\_scale

Get the Pop's weight modifier scale multiplier

pop

value

popularity

Get the scoped character's popularity, normally ranging between -100 and +100

character

value

population\_below\_expected\_sol

Scope to the current percentage of the state's population that are below their expected standard of living (0-1)

state

value

power\_bloc

Scope to the power bloc for the scoped country

country

power\_bloc

power\_bloc\_leader

Scope to the leader country of the scoped power bloc

power\_bloc

country

power\_struggle\_contender

Scope to the contender country if there is a power struggle

power\_bloc

country

prev

The previous scope

none

varies

principal

Get the current debt principal of the country

country

value

prosperity

Scope to the amount of prosperity for scope company (example: prosperity >= 2)

company

value

province

Scope from a battle side to the province in which the battle is being fought

battle\_side

province

py

Scope to the active party of specified key belonging to the country, example: py:communist\_party

country

party

region

Scope to the strategic region of the object!

building, diplomatic\_play, hq, interest\_marker, province, state, state\_region

strategic\_region

region\_state

Scope to the state owned by the target country, (example: region\_state:BIC)

state\_region

state

relations

Scope to relations between two countries (example: relations:root >= 2)

country

value

relations\_change\_rate

Scope to daily change in relations progress between two countries (example: relations\_change\_rate:root > 0)

country

value

religion

Scope to country or country definition's primary religion or pop religion

character, country, country\_definition, political\_movement, pop

religion

remaining\_binding\_period

Get the current remaining binding period in days for the scoped treaty

treaty, treaty\_options

value

root

The head of the current top scope eg: receiver of an event, taker of a decision

none

varies

ruler

Scope to the ruler of a country

country

character

scope\_relations

Scope to relations value in diplomatic relations scope (example: scope\_relations >= 2)

diplomatic\_relations

value

scope\_tension

Scope to tension value in diplomatic relations scope (example: scope\_tension >= 2)

diplomatic\_relations

value

second\_country

Scope to the second\_country of any of:

1.  diplomatic pact
2.  treaty
3.  treaty options
4.  treaty article's treaty
5.  treaty article options (for mutual article types only)

diplomatic\_pact, treaty, treaty\_article, treaty\_article\_options, treaty\_options

country

sg

Scope from a state to the StateGoods with the specified key, example: sg:wood

state

state\_goods

shipping\_lane

Scope to the shipping lane for the military formation, or treaty article (goods transfer)

military\_formation, treaty\_article

shipping\_lanes

slaves\_role

Scope from building type to its slaves role pop type

building\_type

pop\_type

source\_country

Scope to the source\_country of any of:

1.  treaty article (for directed article types only)
2.  treaty article options (for directed article types only)

treaty\_article, treaty\_article\_options

country

state

Scope to the state of the object

building, market, pop, province

state

state\_region

Scope to the state region of a state

state

state\_region

target

Scope to the target of the object

diplomatic\_catalyst, diplomatic\_play, journal\_entry, political\_lobby

varies

target\_country

Scope to the target\_country of any of:

1.  treaty article (for directed article types only)
2.  treaty article options (for directed article types only)

treaty\_article, treaty\_article\_options

country

technology\_being\_researched

Scope to the current technology a country is researching

country

technology

techs\_researched

Scope to the current amount of techs researched of a country

country

value

tension

Scope to tension between two countries (example: tension:root >= 2)

country

value

theater

Scope from a BattleSide or State to its theater

battle\_side, state

theater

this

The current scope

none

varies

top\_overlord

Scope to the top overlord of the country in scope

country

country

total\_export\_value

Scope to the total value of scope country's exports to the world market (example: total\_export\_value >= 1000)

country

value

total\_import\_value

Scope to the total value of scope country's imports from the world market (example: total\_import\_value >= 1000)

country

value

total\_trade\_value

Scope to the total value of scope country's trade with the world market (example: total\_trade\_value >= 1000)

country

value

trade\_center

Scope to the trade center state of the market!

market

state

training\_rate

Scope to the current maximum weekly training rate of a building

building

value

treaty

Scope to the treaty a treaty article belongs to

treaty\_article

treaty

treaty\_exports

Get the current number of treaty (NOT world market) exports of the good to the market of the scoped MarketGoods

market\_goods

value

treaty\_imports

Get the current number of treaty (NOT world market) imports of the good to the market of the scoped MarketGoods

market\_goods

value

type

Scope from an object to its type

building, company, diplomatic\_catalyst, diplomatic\_play, harvest\_condition\_area, institution, interest\_group, law, political\_lobby, political\_movement

building\_type, company\_type, diplomatic\_play\_type, harvest\_condition\_type, institution\_type, interest\_group\_type, law\_type, political\_movement\_type

value

A numeric literal value eg: 1, 5.2, -6

none

value

var

Reference a previous set variable in scope via its name eg: var:mortal\_enemy

varies

varies

war

Scope from a diplomatic play to its war

diplomatic\_play

war

weekly\_prosperity\_change

Scope to the weekly prosperity change for scope company (example: weekly\_prosperity\_change >= 2)

company

value

workplace

Scope to the workplace of a pop

pop

building

List of specified scope event targets

Event target

Description

From scope

To scope

active\_law

Scope to the scoped country's active law in the named law group (active\_law:lawgroup\_trade\_policy)

country

law

ai\_army\_comparison

Scope to comparative army strength between two countries (example: value = ai\_army\_comparison:root)

country

value

ai\_gdp\_comparison

Scope to comparative GDP between two countries (example: value = ai\_army\_comparison:root)

country

value

ai\_ideological\_opinion

Scope to AI ideological opinion of scope country on target country (example: value = ai\_ideological\_opinion:root)

country

value

ai\_navy\_comparison

Scope to comparative navy strength between two countries (example: value = ai\_navy\_comparison:root)

country

value

ai\_state\_value

Scope to target country AI desire to own scope state (example: value = ai\_state\_value:root)

state

value

ai\_subject\_value

Scope to AI desire of target country to have scope country as a subject (example: value = ai\_subject\_value:root)

country

value

ai\_transit\_rights\_value

Scope to AI desire of to have transit rights through target country (example: value = ai\_transit\_rights\_value:root)

country

value

ai\_treaty\_export\_value

Scope to target country AI desire to export scoped market good (example: value = ai\_market\_export\_value:root)

market\_goods

value

ai\_treaty\_fairness

Scope to AI evaluation of how fair (to both sides) it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_fairness\_score:root)

country

value

ai\_treaty\_import\_value

Scope to target country AI desire to import scoped market good through treaties (example: value = ai\_market\_import\_value:root)

market\_goods

value

ai\_treaty\_port\_value

Scope to target country AI desire to have a treaty port in scope state (example: value = ai\_treaty\_port\_value:root)

state

value

ai\_treaty\_trade\_value

Scope to target country AI desire to import and export scoped market good (example: value = ai\_market\_trade\_value:root)

market\_goods

value

ai\_treaty\_value

Scope to AI evaluation of how favorable to itself it considers existing treaty articles with the target country to be (example: value = ai\_treaty\_value:root)

country

value

array\_define

Reference the value of a numeric value in an array define: array\_define:Namespace|Name|Index. Index is 0-based.

none

value

b

Scope to the building of specified key in the state, example: b:building\_port

state

building

bt

Scope to a building type, example: bt:building\_tooling\_workshops

none

building\_type

c

Scope to the country with specified tag, example: c:GBR

none

country

catalyst\_type

Scope to a diplomatic catalyst type from its name (catalyst\_type:catalyst\_relations\_level\_improved)

none

diplomatic\_catalyst\_type

cd

Scope to the country definition with specified tag, example: cd:FRA

none

country\_definition

company

Scope to the scoped country's company of the named company type (company:company\_rheinmetall)

country

company

company\_type

Scope to a company type from its name (company\_type:company\_rheinmetall)

none

company\_type

compare\_complex\_value

A comparison trigger that needs a parsable string parameter that will return its value in the context it is used eg: scope:root.number\_of(armies)

varies

value

cu

Scope to the culture with specified key, example: cu:welsh

none

culture

define

Reference the value of a numeric or color define: define:Namespace|Name

none

value

diplomatic\_pact\_other\_country

Scope to the other country of the diplomatic pact in scope, example: diplomatic\_pact\_other\_country(root)

diplomatic\_pact

country

flag

A localization key, ex. flag:the\_boss

none

flag

g

Scope to the goods with specified key, example: g:iron

none

goods

get\_ruler\_for

Scopes from country to a character that would be ruler, under the given transfer of power type

-   get\_ruler\_for:parliamentary\_elective = { effects... }

country

character

global\_productivity

Scope to global productivity values (example: global\_productivity:high\_threshold)

none

value

global\_var

Reference a previous set global variable via its name eg: global\_var:important\_thing

none

varies

harvest\_condition\_type

Scope to a harvest condition type from its name (harvest\_condition\_type:drought)

none

harvest\_condition\_type

i

Scope to ideology specified by key, example: i:ideology\_patriarchal

none

ideology

identity

Scope to the power bloc identity from its name (identity:key)

none

power\_bloc\_identity

ideology

Dual use

1.  Scope to a ideology from its name (ideology:ideology\_anarchist)
2.  Scope to the ideology of the scoped character

character, none

ideology

ig

Scope to the interest group of specified key belonging to the country, example: ig:ig\_armed\_forces

country, political\_movement

interest\_group

ig\_trait

Scope to the Interest Group trait with specified key, example: ig\_trait:ig\_trait\_xenophobia

none

interest\_group\_trait

ig\_type

Scope to an interest group type, example: ig\_type:ig\_landowners

none

interest\_group\_type

input\_market\_goods

Scope to the market goods input in a treaty article (example: input\_market\_goods:root)

treaty\_article, treaty\_article\_options

market\_goods

institution

Scope to a country's institution from its name (institution:institution\_health\_system)

country

institution

je

Scope to the journal entry of specified key belonging to the country, example: je:je\_antarctica

country

journal\_entry

law\_type

Scope to a law type from its name (law\_type:law\_protected\_speech)

none

law\_type

lobby\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies targeting a specific country (lobby\_foreign\_anti\_clout:root >= 0.2)

country

value

lobby\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies targeting a specific country (lobby\_foreign\_pro\_clout:root >= 0.2)

country

value

lobby\_in\_government\_foreign\_anti\_clout

Scope to the total clout of all anti-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_anti\_clout:root >= 0.2)

country

value

lobby\_in\_government\_foreign\_pro\_clout

Scope to the total clout of all pro-country foreign policy lobbies in government targeting a specific country (lobby\_in\_government\_foreign\_pro\_clout:root >= 0.2)

country

value

lobby\_join\_weight

Scope to the join weight of an interest group to a particular lobby (lobby\_join\_weight:root >= 5)

interest\_group

value

lobby\_type

Scope to a political lobby type from its name (lobby\_type:lobby\_pro\_country)

none

political\_lobby\_type

lobby\_war\_opposition

Scope to the total clout of all lobbies that oppose the scoped war (lobby\_war\_opposition:root >= 0.2)

country

value

lobby\_war\_support

Scope to the total clout of all lobbies that support the scoped war (lobby\_war\_support:root >= 0.2)

country

value

local\_var

Reference a previous set local variable via its name eg: local\_var:person\_of\_interest

none

varies

mg

Scope from a market to the MarketGoods with the specified key, (example: mg:tools)

market

market\_goods

mobilization\_option

Scope to a mobilization option from its name (mobilization\_option:key)

none

mobilization\_option

modifier

Scope to the value of the modifier type of specified key belonging to the current object, example: modifier:country\_influence\_add

battle\_side, building, character, country, interest\_group, market, power\_bloc, state

boolean, value

movement\_type

Scope to a political movement type from its name (movement\_type:movement\_religious\_majority)

none

political\_movement\_type

mutual\_trade\_value\_with\_country

Scope to the total base value of goods traded in trade routes between the two markets (example: mutual\_trade\_value\_with\_country:root >= 1000)

country

value

nf

Scope to a Decree of specified key applied to the state, example: nf:decree\_road\_maintaintenance

state

decree

num\_alliances

Get the total number of alliances of the scoped country (example: num\_alliances:root >= 2)

country

value

num\_alliances\_and\_defensive\_pacts\_with\_allies

Scope to the number of alliances target country has with allies of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_allies:root >= 2)

country

value

num\_alliances\_and\_defensive\_pacts\_with\_rivals

Scope to the number of alliances & defensive pacts target country has with rivals of scope country (example: num\_alliances\_and\_defensive\_pacts\_with\_rivals:root >= 2)

country

value

num\_defending\_battalions

Returns the total number of units in battle on Front on the same side as Country, example: num\_defending\_battalions(root)

front

value

num\_shared\_rivals

Scope to the number of shared rivals scope country has with target country (example: num\_shared\_rivals:root >= 2)

country

value

num\_total\_battalions

Returns the total number of units on Front on the same side as Country, example: num\_total\_battalions(root)

front

value

num\_units

Get the number of active units for a commander or military formation  
Get the number of active units for military formations in the province that are allied to the specified country

character, military\_formation, province

value

num\_units\_in\_battle

Get the number of active units in battle for a commander or military formation  
Get the number of active units in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

value

num\_units\_not\_in\_battle

Get the number of active units not in battle for a commander or military formation  
Get the number of active units not in battle for military formations in the province that are allied to the specified country

character, military\_formation, province

value

p

Scope to province specified by hex key, example: p:x1A6E5B

none

province

play\_type

Scope to a diplomatic play type from its name (play\_type:dp\_default\_treaty\_article)

none

diplomatic\_play\_type

pop\_type

Scope to a pop type from its name (pop\_type:laborers)

none

pop\_type

principle

Scope to the power bloc principle from its name (principle:key)

none

power\_bloc\_principle

principle\_group

Scope to the power bloc principle group from its name (principle\_group:key)

none

power\_bloc\_principle\_group

py

Scope to the active party of specified key belonging to the country, example: py:communist\_party

country

party

rank\_value

Scope to rank value of a particular country rank (rank\_value:great\_power)

none

value

region\_state

Scope to the state owned by the target country, (example: region\_state:BIC)

state\_region

state

rel

Scope to the religion with specified key, example: rel:catholic

none

religion

relations

Scope to relations between two countries (example: relations:root >= 2)

country

value

relations\_change\_rate

Scope to daily change in relations progress between two countries (example: relations\_change\_rate:root > 0)

country

value

relations\_threshold

Scope to relations threshold level for comparison purposes (relations\_threshold:warm)

none

value

s

Scope to state region specified by key, example: s:STATE\_UTAH

none

state\_region

scope

Reference a previously saved scope via its name eg: scope:target

none

varies

sg

Scope from a state to the StateGoods with the specified key, example: sg:wood

state

state\_goods

sr

Scope to the strategic region of specified key, example: sr:region\_persia

none

strategic\_region

tension

Scope to tension between two countries (example: tension:root >= 2)

country

value

tension\_threshold

Scope to tension level threshold for comparison purposes (tension\_threshold:warm)

none

value

unit\_type

Scope to a combat unit type from its name (unit\_type:key)

none

combat\_unit\_type

var

Reference a previous set variable in scope via its name eg: var:mortal\_enemy

varies

varies

**Event targets** are scope identifiers that return a specific scope from context or specification. Event targets can be used to set the scope or as the target of an effect or trigger. When setting the scope, event targets can be used either for triggers or for effects, unlike iterators which are specified only for triggers or effects, but not both.

Scope event targets come in two types, contextual or specified. Contextual event targets almost always must be used in a relevant scope, as they refer to scopes relevant to the current scope, such as the capital state or ruler of a country. Specified event targets are often scopeless as they refer to a specific object. Some specified event targets still require a certain scope as they refer to an instance of a type, rather than a unique object.

Specified scope event targets typically use a colon `:` followed by a script key, such as a building type, country tag, or state region. A few use parentheses instead.

### Dot chains\[[edit](/index.php?title=Scope&veaction=edit&section=9 "Edit section: Dot chains") | [edit source](/index.php?title=Scope&action=edit&section=9 "Edit section: Dot chains")\]

While event targets can be used to create trigger or event blocks, i.e. `event_target = { }`, they can also be used in a "dot chain" of scopes. This allows quickly changing through scopes without having to create a new block for each scope level. Dot chains are not available for iterators.

For example, to check if any country has a capital in a certain strategic region, either of the following methods return the same result:

Without dot chaining

any\_country = {
    capital = {
        region = sr:<region\_name>
    }
}

With dot chaining

any\_country = {
    capital.region = sr:<region\_name>
}

### Scope assertion\[[edit](/index.php?title=Scope&veaction=edit&section=10 "Edit section: Scope assertion") | [edit source](/index.php?title=Scope&action=edit&section=10 "Edit section: Scope assertion")\]

When using event targets for scope, it is good practice to use the assertion operator `?=` which checks that the left side object exists before changing scope or testing equality. With the assertion operator, `X ?= foo` is roughly equivalent to the following script:

(trigger\_)if = {
    limit = { exists = X }
    X = foo
}
(trigger\_)else = {
    always = no
}

`foo` can be another object, to check equality, or a script block with further triggers or effects. If X does not exist, the game ignores everything, and for triggers returns false. Without the assertion, the game may throw errors and behave unexpectedly.

While using `X ?= foo` is best practice in most cases, when debugging, it is better *not* to use it, as then errors are thrown when the expected object does not exist.

## Common scope examples\[[edit](/index.php?title=Scope&veaction=edit&section=11 "Edit section: Common scope examples") | [edit source](/index.php?title=Scope&action=edit&section=11 "Edit section: Common scope examples")\]

WIP

## Scope types\[[edit](/index.php?title=Scope&veaction=edit&section=12 "Edit section: Scope types") | [edit source](/index.php?title=Scope&action=edit&section=12 "Edit section: Scope types")\]

[![Method publicly traded.png](/images/thumb/a/a3/Method_publicly_traded.png/45px-Method_publicly_traded.png)](/File:Method_publicly_traded.png)

Please help improve this article or section by [**expanding it**](https://vic3.paradoxwikis.com/index.php?title=Scope&action=edit) with: definitions and examples.

-   battle
-   battle\_side
-   boolean
-   building
-   building\_type
-   character
-   civil\_war
-   color
-   combat\_unit\_type
-   company
-   company\_type
-   country
-   country\_definition
-   culture
-   decree
-   diplomatic\_catalyst
-   diplomatic\_catalyst\_type
-   diplomatic\_catalyst
-   diplomatic\_pact
-   diplomatic\_play
-   diplomatic\_play\_type
-   diplomatic\_relations
-   flag
-   front
-   goods
-   harvest\_condition\_area
-   harvest\_condition\_type
-   hq
-   ideology
-   institution
-   institution\_type
-   interest\_group
-   interest\_group\_trait
-   interest\_group\_type
-   interest\_marker
-   invasion
-   journal\_entry
-   law
-   law\_type
-   market
-   market\_goods
-   military\_formation
-   mobilization\_option
-   new\_combat\_unit
-   none
-   party
-   political\_lobby
-   political\_lobby\_type
-   political\_movement
-   political\_movement\_type
-   pop
-   pop\_type
-   power\_bloc
-   power\_bloc\_identity
-   power\_bloc\_principle
-   power\_bloc\_principle\_group
-   province
-   religion
-   shipping\_lanes
-   state
-   state\_goods
-   state\_region
-   strategic\_region
-   technology
-   theater
-   treaty
-   treaty\_article
-   treaty\_article
-   treaty\_article\_options
-   treaty\_options
-   value
-   war

## References\[[edit](/index.php?title=Scope&veaction=edit&section=13 "Edit section: References") | [edit source](/index.php?title=Scope&action=edit&section=13 "Edit section: References")\]

**[Modding](/Modding "Modding")**

Documentation

[Defines](/Defines "Defines") • [Effects](/Effect "Effect") • [Event targets](/Event_target "Event target") • Scopes • [Triggers](/Trigger "Trigger")

[Macros](/Macro "Macro") • [Modifier types](/Modifier_types "Modifier types") • [On actions](/On_actions "On actions") • [Script value](/Script_value "Script value") • [Variables](/Variable "Variable")

[GUI script](/GUI_script "GUI script") • [Localization](/Localization "Localization")

Scripted content

[Decisions](/Decision_modding "Decision modding") • [Events](/Event_modding "Event modding") • [History](/History_modding "History modding") • [Journal](/Journal_modding "Journal modding") • [Modifiers](/Modifier_modding "Modifier modding") • [Objectives](/index.php?title=Objective_modding&action=edit&redlink=1 "Objective modding (page does not exist)") • [Scripted gui](/Scripted_gui "Scripted gui") • [Customizable localization](/Localization#Customizable_Localization "Localization")

Scripted types

[Buildings](/Building_modding "Building modding") • [Characters](/Character_modding "Character modding") • [Concepts](/index.php?title=Concept_modding&action=edit&redlink=1 "Concept modding (page does not exist)") • [Countries](/Country_modding "Country modding") • [Culture](/Culture_modding "Culture modding") • [Decrees](/Decree_modding "Decree modding") • [Diplomacy](/Diplomacy_modding "Diplomacy modding") • [Goods](/Goods_modding "Goods modding") • [Institutions](/Institution_modding "Institution modding") • [Interest groups](/Interest_group_modding "Interest group modding") • [Laws](/Law_modding "Law modding") • [Parties](/index.php?title=Party_modding&action=edit&redlink=1 "Party modding (page does not exist)") • [Pops](/Pop_modding "Pop modding") • [Power blocs](/Power_bloc_modding "Power bloc modding") • [Religion](/Religion_modding "Religion modding") • [Subject types](/index.php?title=Subject_type_modding&action=edit&redlink=1 "Subject type modding (page does not exist)") • [Technology](/Technology_modding "Technology modding") • [Treaties](/Treaty_modding "Treaty modding") • [War goals](/War_goal_modding "War goal modding")

Map

[Map](/Map_modding "Map modding") • [Geographic regions](/Geographic_region_modding "Geographic region modding") • [States](/State_modding "State modding")

Graphics

[3D Models](/Model_modding "Model modding") • [Interface](/Interface_modding "Interface modding") • [Graphical Assets](/Graphical_asset_modding "Graphical asset modding") • [Fonts](/index.php?title=Font_modding&action=edit&redlink=1 "Font modding (page does not exist)") • [Flags](/Flag_modding "Flag modding")

Audio

[Music](/index.php?title=Music_modding&action=edit&redlink=1 "Music modding (page does not exist)") • [Sound](/Sound_modding "Sound modding")

Other

[AI](/AI_modding "AI modding") • [Console commands](/Console_commands "Console commands") • [Checksum](/Checksum "Checksum") • [Mods](/Mod "Mod") • [Mod compatibility](/Mod_compatibility "Mod compatibility") • [Mod structure](/Mod_structure "Mod structure") • [Scripted tests](/Scripted_test "Scripted test") • [Troubleshooting](/index.php?title=Mod_troubleshooting&action=edit&redlink=1 "Mod troubleshooting (page does not exist)")

Guides

[Mod translation](/Mod_translation "Mod translation") • [New country modding](/New_country_modding "New country modding") • [Save-game editing](/Save-game_editing "Save-game editing") • [State modding guide](/State_modding_guide "State modding guide")

Retrieved from "[https://vic3.paradoxwikis.com/index.php?title=Scope&oldid=34515](https://vic3.paradoxwikis.com/index.php?title=Scope&oldid=34515)"

[Categories](/Special:Categories "Special:Categories"):

-   [Potentially outdated](/Category:Potentially_outdated "Category:Potentially outdated")
-   [1.10](/Category:1.10 "Category:1.10")
-   [Expand](/Category:Expand "Category:Expand")
-   [Modding](/Category:Modding "Category:Modding")

-   This page was last edited on 6 March 2026, at 19:02.
-   Content is available under [Attribution-ShareAlike 3.0](https://central.paradoxwikis.com/Central:Copyrights "central:Central:Copyrights") unless otherwise noted.

-   [Privacy policy](/Victoria_3_Wiki:Privacy_policy)
-   [About Victoria 3 Wiki](/Victoria_3_Wiki:About)
-   [Disclaimers](/Victoria_3_Wiki:General_disclaimer)
-   [Mobile view](https://vic3.paradoxwikis.com/index.php?title=Scope&mobileaction=toggle_view_mobile)

-   [![Attribution-ShareAlike 3.0](https://licensebuttons.net/l/by-sa/3.0/88x31.png)](https://creativecommons.org/licenses/by-sa/3.0/)
-   [![Powered by MediaWiki](/resources/assets/poweredby_mediawiki_88x31.png)](https://www.mediawiki.org/)

[![](https://forum.paradoxplaza.com/forum/styles/paradox/paradox/logotype_white.svg)](https://www.paradoxinteractive.com)

Games

-   [Discover](https://www.paradoxinteractive.com/our-games/discover "Discover")
-   [Our Brands](https://www.paradoxinteractive.com/our-games/our-brands "Our Brands")
-   [Browse](https://www.paradoxinteractive.com/our-games/all-games "Browse")
-   [Play on Paradox technology](https://play.paradoxplaza.com/ "Paradox Launcher")

Community

-   [Paradox Forums](https://forum.paradoxplaza.com/forum/index.php "Paradox Forums")
-   [Paradox Wikis](https://paradoxwikis.com/ "Paradox Wikis")
-   [Support](https://support.paradoxplaza.com/ "Support")

About

-   [News](https://www.paradoxinteractive.com/media/press-releases/news "News")
-   [About us](https://www.paradoxinteractive.com/our-company "About us")
-   [Careers](http://career.paradoxplaza.com/ "Careers")
-   [Join our playtests](https://www.paradoxplaza.com/playtest-static-info-ca.html "Join our playtests")
-   [Media contact](https://www.paradoxinteractive.com/contact/review-codes-and-product-pr "Media contact")

Social Media

-   [](https://www.facebook.com/ParadoxInteractive)
-   [](https://twitter.com/PdxInteractive)
-   [](https://www.youtube.com/user/Paradoxplaza)
-   [](https://www.twitch.tv/paradoxinteractive)
-   [](https://www.instagram.com/explore/locations/241319129/paradox-interactive/)
-   [](https://open.spotify.com/artist/75N2nC2KNgaQ1e6bGs0wyc)

-   [Legal Information](https://legal.paradoxplaza.com/ "Legal Information")
-   [EU Online Dispute Resolution](https://ec.europa.eu/consumers/odr/main/index.cfm?event=main.home.chooseLanguage "EU Online Dispute Resolution")
-   [Report Illegal Content](https://dsa.paradoxinteractive.com/ "Report Illegal Content")

-   [Terms & Policies](https://legal.paradoxplaza.com/ "Terms & Policies")
-   [Legal Information](https://www.paradoxplaza.com/legal-static-info-ca.html "Legal Information")
-   [EU Online Dispute Resolution](https://ec.europa.eu/consumers/odr/main/index.cfm?event=main.home.chooseLanguage "EU Online Dispute Resolution")
-   [Report Illegal Content](https://dsa.paradoxinteractive.com/ "Report Illegal Content")
-   [Frequently Asked Questions](https://www.paradoxplaza.com/faq-static-info-ca.html "Frequently Asked Questions")
-   [Paradox Interactive corporate website](http://www.paradoxinteractive.com/ "Paradox Interactive corporate website")